
(define-module (helm airbyte workload-api)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public workload-api-0.50.3
  (package
   (name "workload-api")
   (version "0.50.3")
   (source (origin
            (method url-fetch)
            (uri "https://airbytehq.github.io/helm-charts/workload-api-0.50.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to deploy the workload-api service")
   (description "Helm chart to deploy the workload-api service")
   (license #f)))

(define-public workload-api-0.50.2
  (package
   (name "workload-api")
   (version "0.50.2")
   (source (origin
            (method url-fetch)
            (uri "https://airbytehq.github.io/helm-charts/workload-api-0.50.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to deploy the workload-api service")
   (description "Helm chart to deploy the workload-api service")
   (license #f)))

(define-public workload-api-0.50.1
  (package
   (name "workload-api")
   (version "0.50.1")
   (source (origin
            (method url-fetch)
            (uri "https://airbytehq.github.io/helm-charts/workload-api-0.50.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to deploy the workload-api service")
   (description "Helm chart to deploy the workload-api service")
   (license #f)))

(define-public workload-api-0.50.0
  (package
   (name "workload-api")
   (version "0.50.0")
   (source (origin
            (method url-fetch)
            (uri "https://airbytehq.github.io/helm-charts/workload-api-0.50.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to deploy the workload-api service")
   (description "Helm chart to deploy the workload-api service")
   (license #f)))

(define-public workload-api-0.49.24
  (package
   (name "workload-api")
   (version "0.49.24")
   (source (origin
            (method url-fetch)
            (uri "https://airbytehq.github.io/helm-charts/workload-api-0.49.24.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to deploy the workload-api service")
   (description "Helm chart to deploy the workload-api service")
   (license #f)))

(define-public workload-api-0.49.23
  (package
   (name "workload-api")
   (version "0.49.23")
   (source (origin
            (method url-fetch)
            (uri "https://airbytehq.github.io/helm-charts/workload-api-0.49.23.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to deploy the workload-api service")
   (description "Helm chart to deploy the workload-api service")
   (license #f)))

(define-public workload-api-0.49.22
  (package
   (name "workload-api")
   (version "0.49.22")
   (source (origin
            (method url-fetch)
            (uri "https://airbytehq.github.io/helm-charts/workload-api-0.49.22.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to deploy the workload-api service")
   (description "Helm chart to deploy the workload-api service")
   (license #f)))

(define-public workload-api-0.49.21
  (package
   (name "workload-api")
   (version "0.49.21")
   (source (origin
            (method url-fetch)
            (uri "https://airbytehq.github.io/helm-charts/workload-api-0.49.21.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to deploy the workload-api service")
   (description "Helm chart to deploy the workload-api service")
   (license #f)))

(define-public workload-api-0.49.20
  (package
   (name "workload-api")
   (version "0.49.20")
   (source (origin
            (method url-fetch)
            (uri "https://airbytehq.github.io/helm-charts/workload-api-0.49.20.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to deploy the workload-api service")
   (description "Helm chart to deploy the workload-api service")
   (license #f)))

(define-public workload-api-0.49.19
  (package
   (name "workload-api")
   (version "0.49.19")
   (source (origin
            (method url-fetch)
            (uri "https://airbytehq.github.io/helm-charts/workload-api-0.49.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to deploy the workload-api service")
   (description "Helm chart to deploy the workload-api service")
   (license #f)))