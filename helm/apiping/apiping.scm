
(define-module (helm apiping apiping)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public apiping-1.3.6
  (package
   (name "apiping")
   (version "1.3.6")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.3.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.3.5
  (package
   (name "apiping")
   (version "1.3.5")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.3.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.3.4
  (package
   (name "apiping")
   (version "1.3.4")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.3.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.3.3
  (package
   (name "apiping")
   (version "1.3.3")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.3.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.3.2
  (package
   (name "apiping")
   (version "1.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.3.1
  (package
   (name "apiping")
   (version "1.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.3.0
  (package
   (name "apiping")
   (version "1.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.2.1
  (package
   (name "apiping")
   (version "1.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.2.0
  (package
   (name "apiping")
   (version "1.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.1.5
  (package
   (name "apiping")
   (version "1.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.1.4
  (package
   (name "apiping")
   (version "1.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.1.3
  (package
   (name "apiping")
   (version "1.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.1.2
  (package
   (name "apiping")
   (version "1.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.1.1
  (package
   (name "apiping")
   (version "1.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.1.0
  (package
   (name "apiping")
   (version "1.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-1.0.0
  (package
   (name "apiping")
   (version "1.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-1.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-0.2.0
  (package
   (name "apiping")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))

(define-public apiping-0.1.0
  (package
   (name "apiping")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://udhos.github.io/apiping/apiping-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install apiping helm chart into kubernetes.")
   (description "Install apiping helm chart into kubernetes.")
   (license #f)))