
(define-module (helm arzu taikun-lb)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public taikun-lb-0.1.0
  (package
   (name "taikun-lb")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://repo.itera.io/repository/itera//taikun-lb-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for LoadBalancer")
   (description "A Helm chart for LoadBalancer")
   (license #f)))

(define-public taikun-lb-0.3.0
  (package
   (name "taikun-lb")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://repo.itera.io/repository/itera//taikun-lb-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for LoadBalancer")
   (description "A Helm chart for LoadBalancer")
   (license #f)))

(define-public taikun-lb-0.3.1
  (package
   (name "taikun-lb")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://repo.itera.io/repository/itera//taikun-lb-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for LoadBalancer")
   (description "A Helm chart for LoadBalancer")
   (license #f)))

(define-public taikun-lb-0.3.5
  (package
   (name "taikun-lb")
   (version "0.3.5")
   (source (origin
            (method url-fetch)
            (uri "https://repo.itera.io/repository/itera//taikun-lb-0.3.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for LoadBalancer")
   (description "A Helm chart for LoadBalancer")
   (license #f)))

(define-public taikun-lb-0.2.0
  (package
   (name "taikun-lb")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://repo.itera.io/repository/itera//taikun-lb-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for LoadBalancer")
   (description "A Helm chart for LoadBalancer")
   (license #f)))

(define-public taikun-lb-0.3.2
  (package
   (name "taikun-lb")
   (version "0.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://repo.itera.io/repository/itera//taikun-lb-0.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for LoadBalancer")
   (description "A Helm chart for LoadBalancer")
   (license #f)))

(define-public taikun-lb-0.3.3
  (package
   (name "taikun-lb")
   (version "0.3.3")
   (source (origin
            (method url-fetch)
            (uri "https://repo.itera.io/repository/itera//taikun-lb-0.3.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for LoadBalancer")
   (description "A Helm chart for LoadBalancer")
   (license #f)))

(define-public taikun-lb-0.3.4
  (package
   (name "taikun-lb")
   (version "0.3.4")
   (source (origin
            (method url-fetch)
            (uri "https://repo.itera.io/repository/itera//taikun-lb-0.3.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for LoadBalancer")
   (description "A Helm chart for LoadBalancer")
   (license #f)))