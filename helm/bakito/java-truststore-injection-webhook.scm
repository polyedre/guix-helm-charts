
(define-module (helm bakito java-truststore-injection-webhook)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public java-truststore-injection-webhook-0.0.8
  (package
   (name "java-truststore-injection-webhook")
   (version "0.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bakito/helm-charts/releases/download/java-truststore-injection-webhook-0.0.8/java-truststore-injection-webhook-0.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to install the java truststore injection webhook")
   (description "A Helm chart to install the java truststore injection webhook")
   (license #f)))

(define-public java-truststore-injection-webhook-0.0.7
  (package
   (name "java-truststore-injection-webhook")
   (version "0.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bakito/helm-charts/releases/download/java-truststore-injection-webhook-0.0.7/java-truststore-injection-webhook-0.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to install the java truststore injection webhook")
   (description "A Helm chart to install the java truststore injection webhook")
   (license #f)))

(define-public java-truststore-injection-webhook-0.0.6
  (package
   (name "java-truststore-injection-webhook")
   (version "0.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bakito/helm-charts/releases/download/java-truststore-injection-webhook-0.0.6/java-truststore-injection-webhook-0.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to install the java truststore injection webhook")
   (description "A Helm chart to install the java truststore injection webhook")
   (license #f)))

(define-public java-truststore-injection-webhook-0.0.5
  (package
   (name "java-truststore-injection-webhook")
   (version "0.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bakito/helm-charts/releases/download/java-truststore-injection-webhook-0.0.5/java-truststore-injection-webhook-0.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to install the java truststore injection webhook")
   (description "A Helm chart to install the java truststore injection webhook")
   (license #f)))

(define-public java-truststore-injection-webhook-0.0.4
  (package
   (name "java-truststore-injection-webhook")
   (version "0.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bakito/helm-charts/releases/download/java-truststore-injection-webhook-0.0.4/java-truststore-injection-webhook-0.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to install the java truststore injection webhook")
   (description "A Helm chart to install the java truststore injection webhook")
   (license #f)))

(define-public java-truststore-injection-webhook-0.0.2
  (package
   (name "java-truststore-injection-webhook")
   (version "0.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bakito/helm-charts/releases/download/java-truststore-injection-webhook-0.0.2/java-truststore-injection-webhook-0.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to install the java truststore injection webhook")
   (description "A Helm chart to install the java truststore injection webhook")
   (license #f)))