
(define-module (helm banzaicloud-stable uistio)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public uistio-0.0.12
  (package
   (name "uistio")
   (version "0.0.12")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))

(define-public uistio-0.0.11
  (package
   (name "uistio")
   (version "0.0.11")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))

(define-public uistio-0.0.10
  (package
   (name "uistio")
   (version "0.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))

(define-public uistio-0.0.9
  (package
   (name "uistio")
   (version "0.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))

(define-public uistio-0.0.8
  (package
   (name "uistio")
   (version "0.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))

(define-public uistio-0.0.7
  (package
   (name "uistio")
   (version "0.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))

(define-public uistio-0.0.6
  (package
   (name "uistio")
   (version "0.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))

(define-public uistio-0.0.5
  (package
   (name "uistio")
   (version "0.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))

(define-public uistio-0.0.4
  (package
   (name "uistio")
   (version "0.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))

(define-public uistio-0.0.2
  (package
   (name "uistio")
   (version "0.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))

(define-public uistio-0.0.1
  (package
   (name "uistio")
   (version "0.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://kubernetes-charts.banzaicloud.com/charts/uistio-0.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://banzaicloud.com")
   (synopsis "UIstio manages hybrid clusters connected with Istio")
   (description "UIstio manages hybrid clusters connected with Istio")
   (license #f)))