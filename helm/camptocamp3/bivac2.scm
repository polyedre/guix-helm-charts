
(define-module (helm camptocamp3 bivac2)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public bivac2-0.1.1
  (package
   (name "bivac2")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/bivac2-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/camptocamp/bivac")
   (synopsis "backup interface for volumes attached to containers")
   (description "backup interface for volumes attached to containers")
   (license #f)))

(define-public bivac2-0.1.0
  (package
   (name "bivac2")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/bivac2-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/camptocamp/bivac")
   (synopsis "backup interface for volumes attached to containers")
   (description "backup interface for volumes attached to containers")
   (license #f)))

(define-public bivac2-0.0.7
  (package
   (name "bivac2")
   (version "0.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/bivac2-0.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/camptocamp/bivac")
   (synopsis "backup interface for volumes attached to containers")
   (description "backup interface for volumes attached to containers")
   (license #f)))

(define-public bivac2-0.0.6
  (package
   (name "bivac2")
   (version "0.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/bivac2-0.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/camptocamp/bivac")
   (synopsis "backup interface for volumes attached to containers")
   (description "backup interface for volumes attached to containers")
   (license #f)))

(define-public bivac2-0.0.5
  (package
   (name "bivac2")
   (version "0.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/bivac2-0.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/camptocamp/bivac")
   (synopsis "backup interface for volumes attached to containers")
   (description "backup interface for volumes attached to containers")
   (license #f)))

(define-public bivac2-0.0.4
  (package
   (name "bivac2")
   (version "0.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/bivac2-0.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/camptocamp/bivac")
   (synopsis "backup interface for volumes attached to containers")
   (description "backup interface for volumes attached to containers")
   (license #f)))

(define-public bivac2-0.0.3
  (package
   (name "bivac2")
   (version "0.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/bivac2-0.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/camptocamp/bivac")
   (synopsis "backup interface for volumes attached to containers")
   (description "backup interface for volumes attached to containers")
   (license #f)))

(define-public bivac2-0.0.2
  (package
   (name "bivac2")
   (version "0.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/bivac2-0.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/camptocamp/bivac")
   (synopsis "backup interface for volumes attached to containers")
   (description "backup interface for volumes attached to containers")
   (license #f)))

(define-public bivac2-0.0.1
  (package
   (name "bivac2")
   (version "0.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/bivac2-0.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/camptocamp/bivac")
   (synopsis "backup interface for volumes attached to containers")
   (description "backup interface for volumes attached to containers")
   (license #f)))