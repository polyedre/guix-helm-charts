
(define-module (helm camptocamp3 openshift-projects)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public openshift-projects-0.5.3
  (package
   (name "openshift-projects")
   (version "0.5.3")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/openshift-projects-0.5.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Openshift Projects creation with own tiller instances")
   (description "Openshift Projects creation with own tiller instances")
   (license #f)))

(define-public openshift-projects-0.5.2
  (package
   (name "openshift-projects")
   (version "0.5.2")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/openshift-projects-0.5.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Openshift Projects creation with own tiller instances")
   (description "Openshift Projects creation with own tiller instances")
   (license #f)))

(define-public openshift-projects-0.5.1
  (package
   (name "openshift-projects")
   (version "0.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/openshift-projects-0.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Openshift Projects creation with own tiller instances")
   (description "Openshift Projects creation with own tiller instances")
   (license #f)))

(define-public openshift-projects-0.5.0
  (package
   (name "openshift-projects")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/openshift-projects-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Openshift Projects creation with own tiller instances")
   (description "Openshift Projects creation with own tiller instances")
   (license #f)))

(define-public openshift-projects-0.4.0
  (package
   (name "openshift-projects")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/openshift-projects-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Openshift Projects creation with own tiller instances")
   (description "Openshift Projects creation with own tiller instances")
   (license #f)))

(define-public openshift-projects-0.3.0
  (package
   (name "openshift-projects")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/openshift-projects-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Openshift Projects creation with own tiller instances")
   (description "Openshift Projects creation with own tiller instances")
   (license #f)))

(define-public openshift-projects-0.1.3
  (package
   (name "openshift-projects")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/openshift-projects-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Openshift Projects creation with own tiller instances")
   (description "Openshift Projects creation with own tiller instances")
   (license #f)))

(define-public openshift-projects-0.1.2
  (package
   (name "openshift-projects")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/openshift-projects-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Openshift Projects creation with own tiller instances")
   (description "Openshift Projects creation with own tiller instances")
   (license #f)))

(define-public openshift-projects-0.1.1
  (package
   (name "openshift-projects")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/openshift-projects-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Openshift Projects creation with own tiller instances")
   (description "Openshift Projects creation with own tiller instances")
   (license #f)))

(define-public openshift-projects-0.1.0
  (package
   (name "openshift-projects")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://camptocamp.github.io/charts/openshift-projects-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Openshift Projects creation with own tiller instances")
   (description "Openshift Projects creation with own tiller instances")
   (license #f)))