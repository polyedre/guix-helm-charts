
(define-module (helm cloud-native-toolkit console-link-cronjob)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public console-link-cronjob-0.5.3
  (package
   (name "console-link-cronjob")
   (version "0.5.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.5.3/console-link-cronjob-0.5.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.5.1
  (package
   (name "console-link-cronjob")
   (version "0.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.5.1/console-link-cronjob-0.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.5.0
  (package
   (name "console-link-cronjob")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.5.0/console-link-cronjob-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.4.2
  (package
   (name "console-link-cronjob")
   (version "0.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.4.2/console-link-cronjob-0.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.4.1
  (package
   (name "console-link-cronjob")
   (version "0.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.4.1/console-link-cronjob-0.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.4.0
  (package
   (name "console-link-cronjob")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.4.0/console-link-cronjob-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.3.0
  (package
   (name "console-link-cronjob")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.3.0/console-link-cronjob-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.2.1
  (package
   (name "console-link-cronjob")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.2.1/console-link-cronjob-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.2.0
  (package
   (name "console-link-cronjob")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.2.0/console-link-cronjob-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.1.2
  (package
   (name "console-link-cronjob")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.1.2/console-link-cronjob-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.1.1
  (package
   (name "console-link-cronjob")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.1.1/console-link-cronjob-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))

(define-public console-link-cronjob-0.1.0
  (package
   (name "console-link-cronjob")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/cloud-native-toolkit/toolkit-charts/releases/download/console-link-cronjob-0.1.0/console-link-cronjob-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (description "A Helm chart to generate a cron job that watches Routes for annotations and generates ConsoleLinks if they are present")
   (license #f)))