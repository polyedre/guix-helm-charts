
(define-module (helm cno door-agent)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public door-agent-v2.3.3
  (package
   (name "door-agent")
   (version "v2.3.3")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.3.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.3.2
  (package
   (name "door-agent")
   (version "v2.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.3.1
  (package
   (name "door-agent")
   (version "v2.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.3.0
  (package
   (name "door-agent")
   (version "v2.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.2.6
  (package
   (name "door-agent")
   (version "v2.2.6")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.2.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.2.5
  (package
   (name "door-agent")
   (version "v2.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.2.4
  (package
   (name "door-agent")
   (version "v2.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.2.3
  (package
   (name "door-agent")
   (version "v2.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.2.2
  (package
   (name "door-agent")
   (version "v2.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.2.1
  (package
   (name "door-agent")
   (version "v2.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.2.0
  (package
   (name "door-agent")
   (version "v2.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.1.2
  (package
   (name "door-agent")
   (version "v2.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.1.1
  (package
   (name "door-agent")
   (version "v2.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))

(define-public door-agent-v2.1.0
  (package
   (name "door-agent")
   (version "v2.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://beopencloud.github.io/cno/door-agent-v2.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for DOOR agent")
   (description "Chart for DOOR agent")
   (license #f)))