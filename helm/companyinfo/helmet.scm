
(define-module (helm companyinfo helmet)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public helmet-0.11.0
  (package
   (name "helmet")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.11.0/helmet-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.10.1
  (package
   (name "helmet")
   (version "0.10.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.10.1/helmet-0.10.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.10.0
  (package
   (name "helmet")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.10.0/helmet-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.9.1
  (package
   (name "helmet")
   (version "0.9.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.9.1/helmet-0.9.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.9.0
  (package
   (name "helmet")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.9.0/helmet-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.8.0
  (package
   (name "helmet")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.8.0/helmet-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.7.0
  (package
   (name "helmet")
   (version "0.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.7.0/helmet-0.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.6.5
  (package
   (name "helmet")
   (version "0.6.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.6.5/helmet-0.6.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.6.4
  (package
   (name "helmet")
   (version "0.6.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.6.4/helmet-0.6.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.6.3
  (package
   (name "helmet")
   (version "0.6.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.6.3/helmet-0.6.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.6.2
  (package
   (name "helmet")
   (version "0.6.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.6.2/helmet-0.6.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.6.1
  (package
   (name "helmet")
   (version "0.6.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.6.1/helmet-0.6.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.6.0
  (package
   (name "helmet")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.6.0/helmet-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.5.2
  (package
   (name "helmet")
   (version "0.5.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.5.2/helmet-0.5.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logics. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.5.1
  (package
   (name "helmet")
   (version "0.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.5.1/helmet-0.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logic between Company.info charts. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logic between Company.info charts. This chart is not deployable by itself.")
   (license #f)))

(define-public helmet-0.5.0
  (package
   (name "helmet")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/companyinfo/helm-charts/releases/download/helmet-0.5.0/helmet-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/companyinfo/helm-charts/blob/main/charts/helmet")
   (synopsis "Helmet is a library Helm Chart for grouping common logic between Company.info charts. This chart is not deployable by itself.")
   (description "Helmet is a library Helm Chart for grouping common logic between Company.info charts. This chart is not deployable by itself.")
   (license #f)))