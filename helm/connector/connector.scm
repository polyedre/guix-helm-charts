
(define-module (helm connector connector)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public connector-0.1.24
  (package
   (name "connector")
   (version "0.1.24")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.24.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.23
  (package
   (name "connector")
   (version "0.1.23")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.23.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.22
  (package
   (name "connector")
   (version "0.1.22")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.22.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.21
  (package
   (name "connector")
   (version "0.1.21")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.21.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.20
  (package
   (name "connector")
   (version "0.1.20")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.20.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.19
  (package
   (name "connector")
   (version "0.1.19")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.18
  (package
   (name "connector")
   (version "0.1.18")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.17
  (package
   (name "connector")
   (version "0.1.17")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.16
  (package
   (name "connector")
   (version "0.1.16")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.15
  (package
   (name "connector")
   (version "0.1.15")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.14
  (package
   (name "connector")
   (version "0.1.14")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.13
  (package
   (name "connector")
   (version "0.1.13")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.12
  (package
   (name "connector")
   (version "0.1.12")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.11
  (package
   (name "connector")
   (version "0.1.11")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.10
  (package
   (name "connector")
   (version "0.1.10")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))

(define-public connector-0.1.9
  (package
   (name "connector")
   (version "0.1.9")
   (source (origin
            (method url-fetch)
            (uri "https://twingate.github.io/helm-charts/packages/connector-0.1.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.twingate.com")
   (synopsis "Twingate Connector helm chart")
   (description "Twingate Connector helm chart")
   (license #f)))