
(define-module (helm cortex-tenant cortex-tenant)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public cortex-tenant-0.5.0
  (package
   (name "cortex-tenant")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://blind-oracle.github.io/cortex-tenant//cortex-tenant-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm Chart for cortex-tenant")
   (description "A Helm Chart for cortex-tenant")
   (license #f)))

(define-public cortex-tenant-0.4.2
  (package
   (name "cortex-tenant")
   (version "0.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://blind-oracle.github.io/cortex-tenant//cortex-tenant-0.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm Chart for cortex-tenant")
   (description "A Helm Chart for cortex-tenant")
   (license #f)))

(define-public cortex-tenant-0.4.0
  (package
   (name "cortex-tenant")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://blind-oracle.github.io/cortex-tenant//cortex-tenant-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm Chart for cortex-tenant")
   (description "A Helm Chart for cortex-tenant")
   (license #f)))

(define-public cortex-tenant-0.3.2
  (package
   (name "cortex-tenant")
   (version "0.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://blind-oracle.github.io/cortex-tenant//cortex-tenant-0.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm Chart for cortex-tenant")
   (description "A Helm Chart for cortex-tenant")
   (license #f)))

(define-public cortex-tenant-0.3.1
  (package
   (name "cortex-tenant")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://blind-oracle.github.io/cortex-tenant//cortex-tenant-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm Chart for cortex-tenant")
   (description "A Helm Chart for cortex-tenant")
   (license #f)))

(define-public cortex-tenant-0.3.0
  (package
   (name "cortex-tenant")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://blind-oracle.github.io/cortex-tenant//cortex-tenant-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm Chart for cortex-tenant")
   (description "A Helm Chart for cortex-tenant")
   (license #f)))

(define-public cortex-tenant-0.2.1
  (package
   (name "cortex-tenant")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://blind-oracle.github.io/cortex-tenant//cortex-tenant-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm Chart for cortex-tenant")
   (description "A Helm Chart for cortex-tenant")
   (license #f)))

(define-public cortex-tenant-0.2.0
  (package
   (name "cortex-tenant")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://blind-oracle.github.io/cortex-tenant//cortex-tenant-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm Chart for cortex-tenant")
   (description "A Helm Chart for cortex-tenant")
   (license #f)))

(define-public cortex-tenant-0.1.0
  (package
   (name "cortex-tenant")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://blind-oracle.github.io/cortex-tenant//cortex-tenant-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm Chart for cortex-tenant")
   (description "A Helm Chart for cortex-tenant")
   (license #f)))