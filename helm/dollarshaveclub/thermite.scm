
(define-module (helm dollarshaveclub thermite)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public thermite-0.1.17
  (package
   (name "thermite")
   (version "0.1.17")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/dollarshaveclub/helm-charts-public/releases/download/thermite-0.1.17/thermite-0.1.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Removes old and undeployed Amazon Elastic Container Registry images")
   (description "Removes old and undeployed Amazon Elastic Container Registry images")
   (license #f)))

(define-public thermite-0.1.15
  (package
   (name "thermite")
   (version "0.1.15")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/dollarshaveclub/helm-charts-public/releases/download/thermite-0.1.15/thermite-0.1.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Removes old and undeployed Amazon Elastic Container Registry images")
   (description "Removes old and undeployed Amazon Elastic Container Registry images")
   (license #f)))

(define-public thermite-0.1.14
  (package
   (name "thermite")
   (version "0.1.14")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/dollarshaveclub/helm-charts-public/releases/download/thermite-0.1.14/thermite-0.1.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Removes old and undeployed Amazon Elastic Container Registry images")
   (description "Removes old and undeployed Amazon Elastic Container Registry images")
   (license #f)))

(define-public thermite-0.1.13
  (package
   (name "thermite")
   (version "0.1.13")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/dollarshaveclub/helm-charts-public/releases/download/thermite-0.1.13/thermite-0.1.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Removes old and undeployed Amazon Elastic Container Registry images")
   (description "Removes old and undeployed Amazon Elastic Container Registry images")
   (license #f)))

(define-public thermite-0.1.12
  (package
   (name "thermite")
   (version "0.1.12")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/dollarshaveclub/helm-charts-public/releases/download/thermite-0.1.12/thermite-0.1.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Removes old and undeployed Amazon Elastic Container Registry images")
   (description "Removes old and undeployed Amazon Elastic Container Registry images")
   (license #f)))

(define-public thermite-0.1.11
  (package
   (name "thermite")
   (version "0.1.11")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/dollarshaveclub/helm-charts-public/releases/download/thermite-0.1.11/thermite-0.1.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Removes old and undeployed Amazon Elastic Container Registry images")
   (description "Removes old and undeployed Amazon Elastic Container Registry images")
   (license #f)))

(define-public thermite-0.1.10
  (package
   (name "thermite")
   (version "0.1.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/dollarshaveclub/helm-charts-public/releases/download/thermite-0.1.10/thermite-0.1.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Removes old and undeployed Amazon Elastic Container Registry images")
   (description "Removes old and undeployed Amazon Elastic Container Registry images")
   (license #f)))

(define-public thermite-0.1.9
  (package
   (name "thermite")
   (version "0.1.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/dollarshaveclub/helm-charts-public/releases/download/thermite-0.1.9/thermite-0.1.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Removes old and undeployed Amazon Elastic Container Registry images")
   (description "Removes old and undeployed Amazon Elastic Container Registry images")
   (license #f)))

(define-public thermite-0.1.8
  (package
   (name "thermite")
   (version "0.1.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/dollarshaveclub/helm-charts-public/releases/download/thermite-0.1.8/thermite-0.1.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Removes old and undeployed Amazon Elastic Container Registry images")
   (description "Removes old and undeployed Amazon Elastic Container Registry images")
   (license #f)))

(define-public thermite-0.1.7
  (package
   (name "thermite")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/dollarshaveclub/helm-charts-public/releases/download/thermite-0.1.7/thermite-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Removes old and undeployed Amazon Elastic Container Registry images")
   (description "Removes old and undeployed Amazon Elastic Container Registry images")
   (license #f)))