
(define-module (helm drone-ce drone-runner)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public drone-runner-0.5.3
  (package
   (name "drone-runner")
   (version "0.5.3")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.5.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))

(define-public drone-runner-0.5.2
  (package
   (name "drone-runner")
   (version "0.5.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.5.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))

(define-public drone-runner-0.5.1
  (package
   (name "drone-runner")
   (version "0.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))

(define-public drone-runner-0.5.0
  (package
   (name "drone-runner")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))

(define-public drone-runner-0.4.0
  (package
   (name "drone-runner")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))

(define-public drone-runner-0.3.2
  (package
   (name "drone-runner")
   (version "0.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))

(define-public drone-runner-0.3.1
  (package
   (name "drone-runner")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))

(define-public drone-runner-0.3.0
  (package
   (name "drone-runner")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))

(define-public drone-runner-0.2.0
  (package
   (name "drone-runner")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))

(define-public drone-runner-0.1.1
  (package
   (name "drone-runner")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))

(define-public drone-runner-0.1.0
  (package
   (name "drone-runner")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.cryptic.systems/drone/charts/drone-runner-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart for a drone runner based on kubernetes")
   (description "Helm chart for a drone runner based on kubernetes")
   (license #f)))