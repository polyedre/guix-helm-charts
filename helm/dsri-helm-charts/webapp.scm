
(define-module (helm dsri-helm-charts webapp)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public webapp-0.1.18
  (package
   (name "webapp")
   (version "0.1.18")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.18/webapp-0.1.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://artifacthub.io/packages/helm/dsri-helm-charts/webapp")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.17
  (package
   (name "webapp")
   (version "0.1.17")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.17/webapp-0.1.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://artifacthub.io/packages/helm/dsri-helm-charts/webapp")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.16
  (package
   (name "webapp")
   (version "0.1.16")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.16/webapp-0.1.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://artifacthub.io/packages/helm/dsri-helm-charts/webapp")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.15
  (package
   (name "webapp")
   (version "0.1.15")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.15/webapp-0.1.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://artifacthub.io/packages/helm/dsri-helm-charts/webapp")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.13
  (package
   (name "webapp")
   (version "0.1.13")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.13/webapp-0.1.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://artifacthub.io/packages/helm/dsri-helm-charts/webapp")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.12
  (package
   (name "webapp")
   (version "0.1.12")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.12/webapp-0.1.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://artifacthub.io/packages/helm/dsri-helm-charts/webapp")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.11
  (package
   (name "webapp")
   (version "0.1.11")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.11/webapp-0.1.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.10
  (package
   (name "webapp")
   (version "0.1.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.10/webapp-0.1.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.9
  (package
   (name "webapp")
   (version "0.1.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.9/webapp-0.1.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.8
  (package
   (name "webapp")
   (version "0.1.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.8/webapp-0.1.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.7
  (package
   (name "webapp")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.7/webapp-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.6
  (package
   (name "webapp")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.6/webapp-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.5
  (package
   (name "webapp")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.5/webapp-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.4
  (package
   (name "webapp")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.4/webapp-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.3
  (package
   (name "webapp")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.3/webapp-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.2
  (package
   (name "webapp")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.2/webapp-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.1
  (package
   (name "webapp")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.1/webapp-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))

(define-public webapp-0.1.0
  (package
   (name "webapp")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/MaastrichtU-IDS/dsri-helm-charts/releases/download/webapp-0.1.0/webapp-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/MaastrichtU-IDS/dsri-helm-charts")
   (synopsis "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (description "A generic reuseable Helm chart for deploying almost any dockerized application exposing a web interface to a HTTPS route on OpenShift and Kubernetes ")
   (license #f)))