
(define-module (helm evertrust horizon-issuer)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public horizon-issuer-0.0.1
  (package
   (name "horizon-issuer")
   (version "0.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://repo.evertrust.io/repository/charts//horizon-issuer-0.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Issue certificates seamlessly using Horizon and cert-manager.")
   (description "Issue certificates seamlessly using Horizon and cert-manager.")
   (license #f)))

(define-public horizon-issuer-0.0.2
  (package
   (name "horizon-issuer")
   (version "0.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://repo.evertrust.io/repository/charts//horizon-issuer-0.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Issue certificates seamlessly using Horizon and cert-manager.")
   (description "Issue certificates seamlessly using Horizon and cert-manager.")
   (license #f)))

(define-public horizon-issuer-0.0.3
  (package
   (name "horizon-issuer")
   (version "0.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://repo.evertrust.io/repository/charts//horizon-issuer-0.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Issue certificates seamlessly using Horizon and cert-manager.")
   (description "Issue certificates seamlessly using Horizon and cert-manager.")
   (license #f)))

(define-public horizon-issuer-0.0.4
  (package
   (name "horizon-issuer")
   (version "0.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://repo.evertrust.io/repository/charts//horizon-issuer-0.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Issue certificates seamlessly using Horizon and cert-manager.")
   (description "Issue certificates seamlessly using Horizon and cert-manager.")
   (license #f)))

(define-public horizon-issuer-0.1.0
  (package
   (name "horizon-issuer")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://repo.evertrust.io/repository/charts//horizon-issuer-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Issue certificates seamlessly using Horizon and cert-manager.")
   (description "Issue certificates seamlessly using Horizon and cert-manager.")
   (license #f)))

(define-public horizon-issuer-0.1.1
  (package
   (name "horizon-issuer")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://repo.evertrust.io/repository/charts//horizon-issuer-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Issue certificates seamlessly using Horizon and cert-manager.")
   (description "Issue certificates seamlessly using Horizon and cert-manager.")
   (license #f)))

(define-public horizon-issuer-0.1.2
  (package
   (name "horizon-issuer")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://repo.evertrust.io/repository/charts//horizon-issuer-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Issue certificates seamlessly using Horizon and cert-manager.")
   (description "Issue certificates seamlessly using Horizon and cert-manager.")
   (license #f)))

(define-public horizon-issuer-0.1.3
  (package
   (name "horizon-issuer")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://repo.evertrust.io/repository/charts//horizon-issuer-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Issue certificates seamlessly using Horizon and cert-manager.")
   (description "Issue certificates seamlessly using Horizon and cert-manager.")
   (license #f)))

(define-public horizon-issuer-0.2.0
  (package
   (name "horizon-issuer")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://repo.evertrust.io/repository/charts//horizon-issuer-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Issue certificates seamlessly using Horizon and cert-manager.")
   (description "Issue certificates seamlessly using Horizon and cert-manager.")
   (license #f)))