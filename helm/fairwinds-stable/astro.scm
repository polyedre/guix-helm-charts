
(define-module (helm fairwinds-stable astro)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public astro-1.0.14
  (package
   (name "astro")
   (version "1.0.14")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.13
  (package
   (name "astro")
   (version "1.0.13")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.12
  (package
   (name "astro")
   (version "1.0.12")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.11
  (package
   (name "astro")
   (version "1.0.11")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.10
  (package
   (name "astro")
   (version "1.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.9
  (package
   (name "astro")
   (version "1.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.8
  (package
   (name "astro")
   (version "1.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.7
  (package
   (name "astro")
   (version "1.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.6
  (package
   (name "astro")
   (version "1.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.5
  (package
   (name "astro")
   (version "1.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.4
  (package
   (name "astro")
   (version "1.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.3
  (package
   (name "astro")
   (version "1.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.2
  (package
   (name "astro")
   (version "1.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))

(define-public astro-1.0.1
  (package
   (name "astro")
   (version "1.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.fairwinds.com/stable/astro-1.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Emit datadog monitors based on kubernetes state.")
   (description "Emit datadog monitors based on kubernetes state.")
   (license #f)))