
(define-module (helm flagger flagger)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public flagger-1.37.0
  (package
   (name "flagger")
   (version "1.37.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.37.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.36.1
  (package
   (name "flagger")
   (version "1.36.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.36.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.36.0
  (package
   (name "flagger")
   (version "1.36.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.36.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.35.0
  (package
   (name "flagger")
   (version "1.35.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.35.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.34.0
  (package
   (name "flagger")
   (version "1.34.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.34.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.33.0
  (package
   (name "flagger")
   (version "1.33.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.33.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.32.0
  (package
   (name "flagger")
   (version "1.32.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.32.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.31.0
  (package
   (name "flagger")
   (version "1.31.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.31.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.30.0
  (package
   (name "flagger")
   (version "1.30.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.30.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.29.0
  (package
   (name "flagger")
   (version "1.29.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.29.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.28.0
  (package
   (name "flagger")
   (version "1.28.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.28.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.27.0
  (package
   (name "flagger")
   (version "1.27.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.27.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.26.0
  (package
   (name "flagger")
   (version "1.26.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.26.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.25.0
  (package
   (name "flagger")
   (version "1.25.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.25.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.24.1
  (package
   (name "flagger")
   (version "1.24.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.24.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.24.0
  (package
   (name "flagger")
   (version "1.24.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.24.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.23.0
  (package
   (name "flagger")
   (version "1.23.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.23.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.22.2
  (package
   (name "flagger")
   (version "1.22.2")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.22.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.22.1
  (package
   (name "flagger")
   (version "1.22.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.22.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.22.0
  (package
   (name "flagger")
   (version "1.22.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.22.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.21.0
  (package
   (name "flagger")
   (version "1.21.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.21.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.20.0
  (package
   (name "flagger")
   (version "1.20.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.20.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.19.0
  (package
   (name "flagger")
   (version "1.19.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.19.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.18.0
  (package
   (name "flagger")
   (version "1.18.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.18.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.17.0
  (package
   (name "flagger")
   (version "1.17.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.17.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.16.1
  (package
   (name "flagger")
   (version "1.16.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.16.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.16.0
  (package
   (name "flagger")
   (version "1.16.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.16.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.15.0
  (package
   (name "flagger")
   (version "1.15.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.15.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.14.0
  (package
   (name "flagger")
   (version "1.14.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.14.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.13.0
  (package
   (name "flagger")
   (version "1.13.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.13.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.12.1
  (package
   (name "flagger")
   (version "1.12.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.12.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.12.0
  (package
   (name "flagger")
   (version "1.12.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.12.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.11.0
  (package
   (name "flagger")
   (version "1.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.10.0
  (package
   (name "flagger")
   (version "1.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.9.0
  (package
   (name "flagger")
   (version "1.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.8.0
  (package
   (name "flagger")
   (version "1.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.7.0
  (package
   (name "flagger")
   (version "1.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.6.4
  (package
   (name "flagger")
   (version "1.6.4")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.6.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.6.3
  (package
   (name "flagger")
   (version "1.6.3")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.6.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.6.2
  (package
   (name "flagger")
   (version "1.6.2")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.6.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.6.1
  (package
   (name "flagger")
   (version "1.6.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.6.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.6.0
  (package
   (name "flagger")
   (version "1.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.5.0
  (package
   (name "flagger")
   (version "1.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.4.2
  (package
   (name "flagger")
   (version "1.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.4.1
  (package
   (name "flagger")
   (version "1.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.4.0
  (package
   (name "flagger")
   (version "1.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.3.0
  (package
   (name "flagger")
   (version "1.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.2.0
  (package
   (name "flagger")
   (version "1.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.1.0
  (package
   (name "flagger")
   (version "1.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.0.1
  (package
   (name "flagger")
   (version "1.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-1.0.0
  (package
   (name "flagger")
   (version "1.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-1.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-0.27.0
  (package
   (name "flagger")
   (version "0.27.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.27.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-0.26.0
  (package
   (name "flagger")
   (version "0.26.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.26.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-0.25.0
  (package
   (name "flagger")
   (version "0.25.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.25.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-0.24.0
  (package
   (name "flagger")
   (version "0.24.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.24.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-0.23.0
  (package
   (name "flagger")
   (version "0.23.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.23.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-0.22.0
  (package
   (name "flagger")
   (version "0.22.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.22.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-0.21.0
  (package
   (name "flagger")
   (version "0.21.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.21.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-0.20.4
  (package
   (name "flagger")
   (version "0.20.4")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.20.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-0.20.3
  (package
   (name "flagger")
   (version "0.20.3")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.20.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a progressive delivery operator for Kubernetes")
   (description "Flagger is a progressive delivery operator for Kubernetes")
   (license #f)))

(define-public flagger-0.20.2
  (package
   (name "flagger")
   (version "0.20.2")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.20.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.20.1
  (package
   (name "flagger")
   (version "0.20.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.20.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.20.0
  (package
   (name "flagger")
   (version "0.20.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.20.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.19.0
  (package
   (name "flagger")
   (version "0.19.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.19.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.18.6
  (package
   (name "flagger")
   (version "0.18.6")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.18.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.18.5
  (package
   (name "flagger")
   (version "0.18.5")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.18.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.18.4
  (package
   (name "flagger")
   (version "0.18.4")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.18.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.18.2
  (package
   (name "flagger")
   (version "0.18.2")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.18.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.18.1
  (package
   (name "flagger")
   (version "0.18.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.18.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.18.0
  (package
   (name "flagger")
   (version "0.18.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.18.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, Linkerd, App Mesh, Gloo or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.17.0
  (package
   (name "flagger")
   (version "0.17.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.17.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.16.0
  (package
   (name "flagger")
   (version "0.16.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.16.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.15.0
  (package
   (name "flagger")
   (version "0.15.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.15.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.14.1
  (package
   (name "flagger")
   (version "0.14.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.14.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.14.0
  (package
   (name "flagger")
   (version "0.14.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.14.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.13.2
  (package
   (name "flagger")
   (version "0.13.2")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.13.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.13.1
  (package
   (name "flagger")
   (version "0.13.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.13.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.13.0
  (package
   (name "flagger")
   (version "0.13.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.13.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio, App Mesh or NGINX routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.12.0
  (package
   (name "flagger")
   (version "0.12.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.12.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.11.1
  (package
   (name "flagger")
   (version "0.11.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.11.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.11.0
  (package
   (name "flagger")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.10.0
  (package
   (name "flagger")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.9.0
  (package
   (name "flagger")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.8.0
  (package
   (name "flagger")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.7.0
  (package
   (name "flagger")
   (version "0.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.6.0
  (package
   (name "flagger")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.5.1
  (package
   (name "flagger")
   (version "0.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.5.0
  (package
   (name "flagger")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.4.1
  (package
   (name "flagger")
   (version "0.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.4.0
  (package
   (name "flagger")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.3.0
  (package
   (name "flagger")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.2.0
  (package
   (name "flagger")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.1.2
  (package
   (name "flagger")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://flagger.app")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.1.1
  (package
   (name "flagger")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stefanprodan/flagger")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))

(define-public flagger-0.1.0
  (package
   (name "flagger")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/flagger-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stefanprodan/flagger")
   (synopsis "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (description "Flagger is a Kubernetes operator that automates the promotion of canary deployments using Istio routing for traffic shifting and Prometheus metrics for canary analysis.")
   (license #f)))