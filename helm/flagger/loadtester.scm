
(define-module (helm flagger loadtester)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public loadtester-0.32.0
  (package
   (name "loadtester")
   (version "0.32.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.32.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.31.0
  (package
   (name "loadtester")
   (version "0.31.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.31.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.30.0
  (package
   (name "loadtester")
   (version "0.30.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.30.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.29.0
  (package
   (name "loadtester")
   (version "0.29.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.29.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.28.1
  (package
   (name "loadtester")
   (version "0.28.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.28.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.28.0
  (package
   (name "loadtester")
   (version "0.28.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.28.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.27.0
  (package
   (name "loadtester")
   (version "0.27.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.27.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.26.0
  (package
   (name "loadtester")
   (version "0.26.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.26.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.25.0
  (package
   (name "loadtester")
   (version "0.25.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.25.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.24.0
  (package
   (name "loadtester")
   (version "0.24.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.24.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.22.0
  (package
   (name "loadtester")
   (version "0.22.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.22.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.21.0
  (package
   (name "loadtester")
   (version "0.21.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.21.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.20.0
  (package
   (name "loadtester")
   (version "0.20.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.20.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.19.2
  (package
   (name "loadtester")
   (version "0.19.2")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.19.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.19.1
  (package
   (name "loadtester")
   (version "0.19.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.19.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.19.0
  (package
   (name "loadtester")
   (version "0.19.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.19.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.18.0
  (package
   (name "loadtester")
   (version "0.18.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.18.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.17.0
  (package
   (name "loadtester")
   (version "0.17.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.17.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.16.0
  (package
   (name "loadtester")
   (version "0.16.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.16.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.14.0
  (package
   (name "loadtester")
   (version "0.14.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.14.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.13.0
  (package
   (name "loadtester")
   (version "0.13.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.13.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.12.1
  (package
   (name "loadtester")
   (version "0.12.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.12.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.12.0
  (package
   (name "loadtester")
   (version "0.12.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.12.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.11.0
  (package
   (name "loadtester")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.9.0
  (package
   (name "loadtester")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.8.0
  (package
   (name "loadtester")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.7.0
  (package
   (name "loadtester")
   (version "0.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.6.0
  (package
   (name "loadtester")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey and bojand/ghz that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.4.1
  (package
   (name "loadtester")
   (version "0.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.4.0
  (package
   (name "loadtester")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.3.0
  (package
   (name "loadtester")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.2.0
  (package
   (name "loadtester")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))

(define-public loadtester-0.1.0
  (package
   (name "loadtester")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://flagger.app/loadtester-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://docs.flagger.app")
   (synopsis "Flagger's load testing services based on rakyll/hey that generates traffic during canary analysis when configured as a webhook.")
   (description "Flagger's load testing services based on rakyll/hey that generates traffic during canary analysis when configured as a webhook.")
   (license #f)))