
(define-module (helm gabe565 matrimony)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public matrimony-0.6.2
  (package
   (name "matrimony")
   (version "0.6.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.6.2/matrimony-0.6.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony/")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.6.1
  (package
   (name "matrimony")
   (version "0.6.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.6.1/matrimony-0.6.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony/")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.6.0
  (package
   (name "matrimony")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.6.0/matrimony-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony/")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.5.0
  (package
   (name "matrimony")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.5.0/matrimony-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony/")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.4.3
  (package
   (name "matrimony")
   (version "0.4.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.4.3/matrimony-0.4.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.4.2
  (package
   (name "matrimony")
   (version "0.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.4.2/matrimony-0.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.4.1
  (package
   (name "matrimony")
   (version "0.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.4.1/matrimony-0.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.4.0
  (package
   (name "matrimony")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.4.0/matrimony-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.3.4
  (package
   (name "matrimony")
   (version "0.3.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.3.4/matrimony-0.3.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.3.3
  (package
   (name "matrimony")
   (version "0.3.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.3.3/matrimony-0.3.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.3.2
  (package
   (name "matrimony")
   (version "0.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.3.2/matrimony-0.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.3.1
  (package
   (name "matrimony")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.3.1/matrimony-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.3.0
  (package
   (name "matrimony")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.3.0/matrimony-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.2.1
  (package
   (name "matrimony")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.2.1/matrimony-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.2.0
  (package
   (name "matrimony")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.2.0/matrimony-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/gabe565/charts/tree/main/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.1.7
  (package
   (name "matrimony")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.1.7/matrimony-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/gabe565/charts/tree/main/charts/matrimony")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.1.6
  (package
   (name "matrimony")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.1.6/matrimony-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.1.5
  (package
   (name "matrimony")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.1.5/matrimony-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.1.4
  (package
   (name "matrimony")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.1.4/matrimony-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.1.3
  (package
   (name "matrimony")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.1.3/matrimony-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.1.2
  (package
   (name "matrimony")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.1.2/matrimony-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.1.1
  (package
   (name "matrimony")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.1.1/matrimony-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))

(define-public matrimony-0.1.0
  (package
   (name "matrimony")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/matrimony-0.1.0/matrimony-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Self-hosted wedding site configured via YAML")
   (description "Self-hosted wedding site configured via YAML")
   (license #f)))