
(define-module (helm gabe565 transsmute)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public transsmute-1.0.0
  (package
   (name "transsmute")
   (version "1.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-1.0.0/transsmute-1.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute/")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.5.2
  (package
   (name "transsmute")
   (version "0.5.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.5.2/transsmute-0.5.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute/")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.5.1
  (package
   (name "transsmute")
   (version "0.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.5.1/transsmute-0.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute/")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.5.0
  (package
   (name "transsmute")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.5.0/transsmute-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute/")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.4.0
  (package
   (name "transsmute")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.4.0/transsmute-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute/")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.3.6
  (package
   (name "transsmute")
   (version "0.3.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.3.6/transsmute-0.3.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.3.5
  (package
   (name "transsmute")
   (version "0.3.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.3.5/transsmute-0.3.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.3.4
  (package
   (name "transsmute")
   (version "0.3.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.3.4/transsmute-0.3.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.3.3
  (package
   (name "transsmute")
   (version "0.3.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.3.3/transsmute-0.3.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.3.2
  (package
   (name "transsmute")
   (version "0.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.3.2/transsmute-0.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.3.1
  (package
   (name "transsmute")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.3.1/transsmute-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.3.0
  (package
   (name "transsmute")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.3.0/transsmute-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.2.1
  (package
   (name "transsmute")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.2.1/transsmute-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://charts.gabe565.com/charts/transsmute")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.2.0
  (package
   (name "transsmute")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.2.0/transsmute-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/gabe565/charts/tree/main/charts/transsmute")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.1.8
  (package
   (name "transsmute")
   (version "0.1.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.1.8/transsmute-0.1.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/gabe565/charts/tree/main/charts/transsmute")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.1.7
  (package
   (name "transsmute")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.1.7/transsmute-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.1.6
  (package
   (name "transsmute")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.1.6/transsmute-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.1.5
  (package
   (name "transsmute")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.1.5/transsmute-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.1.4
  (package
   (name "transsmute")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.1.4/transsmute-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.1.3
  (package
   (name "transsmute")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.1.3/transsmute-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.1.2
  (package
   (name "transsmute")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.1.2/transsmute-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.1.1
  (package
   (name "transsmute")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.1.1/transsmute-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))

(define-public transsmute-0.1.0
  (package
   (name "transsmute")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gabe565/charts/releases/download/transsmute-0.1.0/transsmute-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Transsmute builds RSS feeds for websites that don't provide them.")
   (description "Transsmute builds RSS feeds for websites that don't provide them.")
   (license #f)))