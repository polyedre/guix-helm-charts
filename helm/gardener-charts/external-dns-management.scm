
(define-module (helm gardener-charts external-dns-management)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public external-dns-management-0.18.4
  (package
   (name "external-dns-management")
   (version "0.18.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.18.4/external-dns-management-0.18.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.18.3
  (package
   (name "external-dns-management")
   (version "0.18.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.18.3/external-dns-management-0.18.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.18.2
  (package
   (name "external-dns-management")
   (version "0.18.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.18.2/external-dns-management-0.18.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.18.1
  (package
   (name "external-dns-management")
   (version "0.18.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.18.1/external-dns-management-0.18.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.18.0
  (package
   (name "external-dns-management")
   (version "0.18.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.18.0/external-dns-management-0.18.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.17.1
  (package
   (name "external-dns-management")
   (version "0.17.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.17.1/external-dns-management-0.17.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.17.0
  (package
   (name "external-dns-management")
   (version "0.17.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.17.0/external-dns-management-0.17.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.16.1
  (package
   (name "external-dns-management")
   (version "0.16.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.16.1/external-dns-management-0.16.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.16.0
  (package
   (name "external-dns-management")
   (version "0.16.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.16.0/external-dns-management-0.16.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.10
  (package
   (name "external-dns-management")
   (version "0.15.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.10/external-dns-management-0.15.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.9
  (package
   (name "external-dns-management")
   (version "0.15.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.9/external-dns-management-0.15.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.8
  (package
   (name "external-dns-management")
   (version "0.15.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.8/external-dns-management-0.15.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.7
  (package
   (name "external-dns-management")
   (version "0.15.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.7/external-dns-management-0.15.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.6
  (package
   (name "external-dns-management")
   (version "0.15.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.6/external-dns-management-0.15.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.5
  (package
   (name "external-dns-management")
   (version "0.15.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.5/external-dns-management-0.15.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.4
  (package
   (name "external-dns-management")
   (version "0.15.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.4/external-dns-management-0.15.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.3
  (package
   (name "external-dns-management")
   (version "0.15.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.3/external-dns-management-0.15.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.2
  (package
   (name "external-dns-management")
   (version "0.15.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.2/external-dns-management-0.15.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.1
  (package
   (name "external-dns-management")
   (version "0.15.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.1/external-dns-management-0.15.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.15.0
  (package
   (name "external-dns-management")
   (version "0.15.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.15.0/external-dns-management-0.15.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.14.2
  (package
   (name "external-dns-management")
   (version "0.14.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.14.2/external-dns-management-0.14.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.14.1
  (package
   (name "external-dns-management")
   (version "0.14.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.14.1/external-dns-management-0.14.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.14.0
  (package
   (name "external-dns-management")
   (version "0.14.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.14.0/external-dns-management-0.14.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.13.3
  (package
   (name "external-dns-management")
   (version "0.13.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.13.3/external-dns-management-0.13.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.13.2
  (package
   (name "external-dns-management")
   (version "0.13.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.13.2/external-dns-management-0.13.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.13.1
  (package
   (name "external-dns-management")
   (version "0.13.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.13.1/external-dns-management-0.13.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.13.0
  (package
   (name "external-dns-management")
   (version "0.13.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.13.0/external-dns-management-0.13.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.12.7
  (package
   (name "external-dns-management")
   (version "0.12.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.12.7/external-dns-management-0.12.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.12.6
  (package
   (name "external-dns-management")
   (version "0.12.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.12.6/external-dns-management-0.12.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.12.5
  (package
   (name "external-dns-management")
   (version "0.12.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.12.5/external-dns-management-0.12.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.12.4
  (package
   (name "external-dns-management")
   (version "0.12.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.12.4/external-dns-management-0.12.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.12.3
  (package
   (name "external-dns-management")
   (version "0.12.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.12.3/external-dns-management-0.12.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.12.2
  (package
   (name "external-dns-management")
   (version "0.12.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.12.2/external-dns-management-0.12.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.12.1
  (package
   (name "external-dns-management")
   (version "0.12.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.12.1/external-dns-management-0.12.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.12.0
  (package
   (name "external-dns-management")
   (version "0.12.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.12.0/external-dns-management-0.12.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.11.5
  (package
   (name "external-dns-management")
   (version "0.11.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.11.5/external-dns-management-0.11.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.11.4
  (package
   (name "external-dns-management")
   (version "0.11.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.11.4/external-dns-management-0.11.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.11.3
  (package
   (name "external-dns-management")
   (version "0.11.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.11.3/external-dns-management-0.11.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.11.2
  (package
   (name "external-dns-management")
   (version "0.11.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.11.2/external-dns-management-0.11.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.11.1
  (package
   (name "external-dns-management")
   (version "0.11.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.11.1/external-dns-management-0.11.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.11.0
  (package
   (name "external-dns-management")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.11.0/external-dns-management-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.10.7
  (package
   (name "external-dns-management")
   (version "0.10.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.10.7/external-dns-management-0.10.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.10.6
  (package
   (name "external-dns-management")
   (version "0.10.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.10.6/external-dns-management-0.10.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.10.5
  (package
   (name "external-dns-management")
   (version "0.10.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.10.5/external-dns-management-0.10.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.10.4
  (package
   (name "external-dns-management")
   (version "0.10.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.10.4/external-dns-management-0.10.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))

(define-public external-dns-management-0.10.3
  (package
   (name "external-dns-management")
   (version "0.10.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gardener-community/gardener-charts/releases/download/external-dns-management-0.10.3/external-dns-management-0.10.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helmchart for external-dns-management")
   (description "A helmchart for external-dns-management")
   (license #f)))