
(define-module (helm hawk hawk)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public hawk-1.1.5
  (package
   (name "hawk")
   (version "1.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/PrivacyEngineering/hawk-helm-charts/releases/download/hawk-1.1.5/hawk-1.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (description "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (license #f)))

(define-public hawk-1.1.4
  (package
   (name "hawk")
   (version "1.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/PrivacyEngineering/hawk-helm-charts/releases/download/hawk-1.1.4/hawk-1.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (description "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (license #f)))

(define-public hawk-1.1.3
  (package
   (name "hawk")
   (version "1.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/PrivacyEngineering/hawk-helm-charts/releases/download/hawk-1.1.3/hawk-1.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (description "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (license #f)))

(define-public hawk-1.1.2
  (package
   (name "hawk")
   (version "1.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/PrivacyEngineering/hawk-helm-charts/releases/download/hawk-1.1.2/hawk-1.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (description "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (license #f)))

(define-public hawk-1.1.1
  (package
   (name "hawk")
   (version "1.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/PrivacyEngineering/hawk-helm-charts/releases/download/hawk-1.1.1/hawk-1.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (description "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (license #f)))

(define-public hawk-1.1.0
  (package
   (name "hawk")
   (version "1.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/PrivacyEngineering/hawk-helm-charts/releases/download/hawk-1.1.0/hawk-1.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (description "The Hawk Framework provides a way of tracking the dataflow between applications and allows for GDPR related tags to be added to the data references.  It also features an analytical dashboard about the GDPR related information and integration for using the ratio of GDPR-tagged data in e.g. Flagger Canary releases. ")
   (license #f)))

(define-public hawk-1.0.3
  (package
   (name "hawk")
   (version "1.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/PrivacyEngineering/hawk-helm-charts/releases/download/hawk-1.0.3/hawk-1.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for Kubernetes")
   (description "A Helm chart for Kubernetes")
   (license #f)))

(define-public hawk-1.0.2
  (package
   (name "hawk")
   (version "1.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/PrivacyEngineering/hawk-helm-charts/releases/download/hawk-1.0.2/hawk-1.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for Kubernetes")
   (description "A Helm chart for Kubernetes")
   (license #f)))