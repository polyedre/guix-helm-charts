
(define-module (helm helm-generic app)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public app-0.13.0
  (package
   (name "app")
   (version "0.13.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.13.0/app-0.13.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy (almost) all your services")
   (description "A Helm chart to deploy (almost) all your services")
   (license #f)))

(define-public app-0.12.0
  (package
   (name "app")
   (version "0.12.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.12.0/app-0.12.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy (almost) all your services")
   (description "A Helm chart to deploy (almost) all your services")
   (license #f)))

(define-public app-0.11.0
  (package
   (name "app")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.11.0/app-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart which provides a DRY application deployment mechanism")
   (description "A Helm chart which provides a DRY application deployment mechanism")
   (license #f)))

(define-public app-0.10.2
  (package
   (name "app")
   (version "0.10.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.10.2/app-0.10.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart which provides a DRY application deployment mechanism")
   (description "A Helm chart which provides a DRY application deployment mechanism")
   (license #f)))

(define-public app-0.10.1
  (package
   (name "app")
   (version "0.10.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.10.1/app-0.10.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart which provides a DRY application deployment mechanism")
   (description "A Helm chart which provides a DRY application deployment mechanism")
   (license #f)))

(define-public app-0.10.0
  (package
   (name "app")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.10.0/app-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart which provides a DRY application deployment mechanism")
   (description "A Helm chart which provides a DRY application deployment mechanism")
   (license #f)))

(define-public app-0.9.0
  (package
   (name "app")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.9.0/app-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart which provides a DRY application deployment mechanism")
   (description "A Helm chart which provides a DRY application deployment mechanism")
   (license #f)))

(define-public app-0.8.0
  (package
   (name "app")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.8.0/app-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart which provides a DRY application deployment mechanism")
   (description "A Helm chart which provides a DRY application deployment mechanism")
   (license #f)))

(define-public app-0.7.1
  (package
   (name "app")
   (version "0.7.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.7.1/app-0.7.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart which provides a DRY application deployment mechanism")
   (description "A Helm chart which provides a DRY application deployment mechanism")
   (license #f)))

(define-public app-0.6.1
  (package
   (name "app")
   (version "0.6.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.6.1/app-0.6.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart which provides a DRY application deployment mechanism")
   (description "A Helm chart which provides a DRY application deployment mechanism")
   (license #f)))

(define-public app-0.6.0
  (package
   (name "app")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/aahemm/helm-microservice/releases/download/v0.6.0/app-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart which provides a DRY application deployment mechanism")
   (description "A Helm chart which provides a DRY application deployment mechanism")
   (license #f)))