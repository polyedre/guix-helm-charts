
(define-module (helm heywood8-helm-charts postgres-backup-local)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public postgres-backup-local-0.1.13
  (package
   (name "postgres-backup-local")
   (version "0.1.13")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/heywood8/helm-charts/releases/download/postgres-backup-local-0.1.13/postgres-backup-local-0.1.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Backup postgres database on kubernetes persistent volume claim")
   (description "Backup postgres database on kubernetes persistent volume claim")
   (license #f)))

(define-public postgres-backup-local-0.1.12
  (package
   (name "postgres-backup-local")
   (version "0.1.12")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/heywood8/helm-charts/releases/download/postgres-backup-local-0.1.12/postgres-backup-local-0.1.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Backup postgres database on kubernetes persistent volume claim")
   (description "Backup postgres database on kubernetes persistent volume claim")
   (license #f)))

(define-public postgres-backup-local-0.1.11
  (package
   (name "postgres-backup-local")
   (version "0.1.11")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/heywood8/helm-charts/releases/download/postgres-backup-local-0.1.11/postgres-backup-local-0.1.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Backup postgres database on kubernetes persistent volume claim")
   (description "Backup postgres database on kubernetes persistent volume claim")
   (license #f)))

(define-public postgres-backup-local-0.1.10
  (package
   (name "postgres-backup-local")
   (version "0.1.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/heywood8/helm-charts/releases/download/postgres-backup-local-0.1.10/postgres-backup-local-0.1.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Backup postgres database on kubernetes persistent volume claim")
   (description "Backup postgres database on kubernetes persistent volume claim")
   (license #f)))

(define-public postgres-backup-local-0.1.9
  (package
   (name "postgres-backup-local")
   (version "0.1.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/heywood8/helm-charts/releases/download/postgres-backup-local-0.1.9/postgres-backup-local-0.1.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Backup postgres database on kubernetes persistent volume claim")
   (description "Backup postgres database on kubernetes persistent volume claim")
   (license #f)))

(define-public postgres-backup-local-0.1.8
  (package
   (name "postgres-backup-local")
   (version "0.1.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/heywood8/helm-charts/releases/download/postgres-backup-local-0.1.8/postgres-backup-local-0.1.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Backup postgres database on kubernetes persistent volume claim")
   (description "Backup postgres database on kubernetes persistent volume claim")
   (license #f)))

(define-public postgres-backup-local-0.1.7
  (package
   (name "postgres-backup-local")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/heywood8/helm-charts/releases/download/postgres-backup-local-0.1.7/postgres-backup-local-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Backup postgres database on kubernetes persistent volume claim")
   (description "Backup postgres database on kubernetes persistent volume claim")
   (license #f)))

(define-public postgres-backup-local-0.1.6
  (package
   (name "postgres-backup-local")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/heywood8/helm-charts/releases/download/postgres-backup-local-0.1.6/postgres-backup-local-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Backup postgres database on kubernetes persistent volume claim")
   (description "Backup postgres database on kubernetes persistent volume claim")
   (license #f)))