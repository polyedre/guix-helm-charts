
(define-module (helm incubator druid)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public druid-0.2.18
  (package
   (name "druid")
   (version "0.2.18")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "DEPRECATED Apache Druid is a high performance real-time analytics database.")
   (description "DEPRECATED Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.17
  (package
   (name "druid")
   (version "0.2.17")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.16
  (package
   (name "druid")
   (version "0.2.16")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.14
  (package
   (name "druid")
   (version "0.2.14")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.13
  (package
   (name "druid")
   (version "0.2.13")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.12
  (package
   (name "druid")
   (version "0.2.12")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.11
  (package
   (name "druid")
   (version "0.2.11")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.10
  (package
   (name "druid")
   (version "0.2.10")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.9
  (package
   (name "druid")
   (version "0.2.9")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.8
  (package
   (name "druid")
   (version "0.2.8")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.7
  (package
   (name "druid")
   (version "0.2.7")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.6
  (package
   (name "druid")
   (version "0.2.6")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.5
  (package
   (name "druid")
   (version "0.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.4
  (package
   (name "druid")
   (version "0.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.3
  (package
   (name "druid")
   (version "0.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.2
  (package
   (name "druid")
   (version "0.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://druid.apache.org/")
   (synopsis "Apache Druid is a high performance real-time analytics database.")
   (description "Apache Druid is a high performance real-time analytics database.")
   (license #f)))

(define-public druid-0.2.1
  (package
   (name "druid")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://druid.io/")
   (synopsis "Apache Druid (incubating) is a high performance analytics data store for event-driven data.")
   (description "Apache Druid (incubating) is a high performance analytics data store for event-driven data.")
   (license #f)))

(define-public druid-0.2.0
  (package
   (name "druid")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://druid.io/")
   (synopsis "Apache Druid (incubating) is a high performance analytics data store for event-driven data.")
   (description "Apache Druid (incubating) is a high performance analytics data store for event-driven data.")
   (license #f)))

(define-public druid-0.1.1
  (package
   (name "druid")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://druid.io/")
   (synopsis "Apache Druid (incubating) is a high performance analytics data store for event-driven data.")
   (description "Apache Druid (incubating) is a high performance analytics data store for event-driven data.")
   (license #f)))

(define-public druid-0.1.0
  (package
   (name "druid")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.helm.sh/incubator/packages/druid-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://druid.io/")
   (synopsis "Apache Druid (incubating) is a high performance analytics data store for event-driven data.")
   (description "Apache Druid (incubating) is a high performance analytics data store for event-driven data.")
   (license #f)))