
(define-module (helm ipsec-vpn ipsec-vpn-server)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public ipsec-vpn-server-0.0.26
  (package
   (name "ipsec-vpn-server")
   (version "0.0.26")
   (source (origin
            (method url-fetch)
            (uri "https://gitlab.com/api/v4/projects/43498617/packages/helm/stable/charts/ipsec-vpn-server-0.0.26.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (description "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (license #f)))

(define-public ipsec-vpn-server-0.0.13
  (package
   (name "ipsec-vpn-server")
   (version "0.0.13")
   (source (origin
            (method url-fetch)
            (uri "https://gitlab.com/api/v4/projects/43498617/packages/helm/stable/charts/ipsec-vpn-server-0.0.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (description "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (license #f)))

(define-public ipsec-vpn-server-0.0.12
  (package
   (name "ipsec-vpn-server")
   (version "0.0.12")
   (source (origin
            (method url-fetch)
            (uri "https://gitlab.com/api/v4/projects/43498617/packages/helm/stable/charts/ipsec-vpn-server-0.0.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (description "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (license #f)))

(define-public ipsec-vpn-server-0.0.11
  (package
   (name "ipsec-vpn-server")
   (version "0.0.11")
   (source (origin
            (method url-fetch)
            (uri "https://gitlab.com/api/v4/projects/43498617/packages/helm/stable/charts/ipsec-vpn-server-0.0.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (description "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (license #f)))

(define-public ipsec-vpn-server-0.0.9
  (package
   (name "ipsec-vpn-server")
   (version "0.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://gitlab.com/api/v4/projects/43498617/packages/helm/stable/charts/ipsec-vpn-server-0.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (description "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (license #f)))

(define-public ipsec-vpn-server-0.0.8
  (package
   (name "ipsec-vpn-server")
   (version "0.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://gitlab.com/api/v4/projects/43498617/packages/helm/stable/charts/ipsec-vpn-server-0.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (description "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (license #f)))

(define-public ipsec-vpn-server-0.0.7
  (package
   (name "ipsec-vpn-server")
   (version "0.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://gitlab.com/api/v4/projects/43498617/packages/helm/stable/charts/ipsec-vpn-server-0.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (description "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (license #f)))

(define-public ipsec-vpn-server-0.0.3
  (package
   (name "ipsec-vpn-server")
   (version "0.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://gitlab.com/api/v4/projects/43498617/packages/helm/stable/charts/ipsec-vpn-server-0.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (description "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (license #f)))

(define-public ipsec-vpn-server-0.0.2
  (package
   (name "ipsec-vpn-server")
   (version "0.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://gitlab.com/api/v4/projects/43498617/packages/helm/stable/charts/ipsec-vpn-server-0.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (description "Deploy IPsec VPN server inside K8s with optional sealed-secrets")
   (license #f)))