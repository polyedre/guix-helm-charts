
(define-module (helm jenkins-x kubernetes-replicator)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public kubernetes-replicator-1.0.15
  (package
   (name "kubernetes-replicator")
   (version "1.0.15")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))

(define-public kubernetes-replicator-1.0.14
  (package
   (name "kubernetes-replicator")
   (version "1.0.14")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))

(define-public kubernetes-replicator-1.0.13
  (package
   (name "kubernetes-replicator")
   (version "1.0.13")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))

(define-public kubernetes-replicator-1.0.12
  (package
   (name "kubernetes-replicator")
   (version "1.0.12")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))

(define-public kubernetes-replicator-1.0.11
  (package
   (name "kubernetes-replicator")
   (version "1.0.11")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))

(define-public kubernetes-replicator-1.0.10
  (package
   (name "kubernetes-replicator")
   (version "1.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))

(define-public kubernetes-replicator-1.0.9
  (package
   (name "kubernetes-replicator")
   (version "1.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))

(define-public kubernetes-replicator-1.0.8
  (package
   (name "kubernetes-replicator")
   (version "1.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))

(define-public kubernetes-replicator-1.0.7
  (package
   (name "kubernetes-replicator")
   (version "1.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))

(define-public kubernetes-replicator-1.0.6
  (package
   (name "kubernetes-replicator")
   (version "1.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))

(define-public kubernetes-replicator-1.0.5
  (package
   (name "kubernetes-replicator")
   (version "1.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://jenkins-x-charts.github.io/v2//charts/kubernetes-replicator-1.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Controller for replicating secrets+configmaps across namespaces")
   (description "Controller for replicating secrets+configmaps across namespaces")
   (license #f)))