
(define-module (helm jupyter-jsc jupyterhub-outpost)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public jupyterhub-outpost-0.9.9
  (package
   (name "jupyterhub-outpost")
   (version "0.9.9")
   (source (origin
            (method url-fetch)
            (uri "https://kaas.pages.jsc.fz-juelich.de/helm-charts/jupyterhub-outpost-0.9.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Start single-user server via JupyterHub on multiple systems")
   (description "Start single-user server via JupyterHub on multiple systems")
   (license #f)))

(define-public jupyterhub-outpost-0.9.8
  (package
   (name "jupyterhub-outpost")
   (version "0.9.8")
   (source (origin
            (method url-fetch)
            (uri "https://kaas.pages.jsc.fz-juelich.de/helm-charts/jupyterhub-outpost-0.9.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Start single-user server via JupyterHub on multiple systems")
   (description "Start single-user server via JupyterHub on multiple systems")
   (license #f)))

(define-public jupyterhub-outpost-0.9.7
  (package
   (name "jupyterhub-outpost")
   (version "0.9.7")
   (source (origin
            (method url-fetch)
            (uri "https://kaas.pages.jsc.fz-juelich.de/helm-charts/jupyterhub-outpost-0.9.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Start single-user server via JupyterHub on multiple systems")
   (description "Start single-user server via JupyterHub on multiple systems")
   (license #f)))

(define-public jupyterhub-outpost-0.9.6
  (package
   (name "jupyterhub-outpost")
   (version "0.9.6")
   (source (origin
            (method url-fetch)
            (uri "https://kaas.pages.jsc.fz-juelich.de/helm-charts/jupyterhub-outpost-0.9.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Start single-user server via JupyterHub on multiple systems")
   (description "Start single-user server via JupyterHub on multiple systems")
   (license #f)))

(define-public jupyterhub-outpost-0.9.5
  (package
   (name "jupyterhub-outpost")
   (version "0.9.5")
   (source (origin
            (method url-fetch)
            (uri "https://kaas.pages.jsc.fz-juelich.de/helm-charts/jupyterhub-outpost-0.9.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Start single-user server via JupyterHub on multiple systems")
   (description "Start single-user server via JupyterHub on multiple systems")
   (license #f)))

(define-public jupyterhub-outpost-0.9.4
  (package
   (name "jupyterhub-outpost")
   (version "0.9.4")
   (source (origin
            (method url-fetch)
            (uri "https://kaas.pages.jsc.fz-juelich.de/helm-charts/jupyterhub-outpost-0.9.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Start single-user server via JupyterHub on multiple systems")
   (description "Start single-user server via JupyterHub on multiple systems")
   (license #f)))

(define-public jupyterhub-outpost-0.9.4-dev.1
  (package
   (name "jupyterhub-outpost")
   (version "0.9.4-dev.1")
   (source (origin
            (method url-fetch)
            (uri "https://kaas.pages.jsc.fz-juelich.de/helm-charts/jupyterhub-outpost-0.9.4-dev.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Start single-user server via JupyterHub on multiple systems")
   (description "Start single-user server via JupyterHub on multiple systems")
   (license #f)))

(define-public jupyterhub-outpost-0.9.3
  (package
   (name "jupyterhub-outpost")
   (version "0.9.3")
   (source (origin
            (method url-fetch)
            (uri "https://kaas.pages.jsc.fz-juelich.de/helm-charts/jupyterhub-outpost-0.9.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Start single-user server via JupyterHub on multiple systems")
   (description "Start single-user server via JupyterHub on multiple systems")
   (license #f)))

(define-public jupyterhub-outpost-0.9.2
  (package
   (name "jupyterhub-outpost")
   (version "0.9.2")
   (source (origin
            (method url-fetch)
            (uri "https://kaas.pages.jsc.fz-juelich.de/helm-charts/jupyterhub-outpost-0.9.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Start single-user server via JupyterHub on multiple systems")
   (description "Start single-user server via JupyterHub on multiple systems")
   (license #f)))

(define-public jupyterhub-outpost-0.9.1
  (package
   (name "jupyterhub-outpost")
   (version "0.9.1")
   (source (origin
            (method url-fetch)
            (uri "https://kaas.pages.jsc.fz-juelich.de/helm-charts/jupyterhub-outpost-0.9.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Start single-user server via JupyterHub on multiple systems")
   (description "Start single-user server via JupyterHub on multiple systems")
   (license #f)))