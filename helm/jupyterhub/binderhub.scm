
(define-module (helm jupyterhub binderhub)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public binderhub-1.0.0-0.dev.git.3424.h2ef5e98
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3424.h2ef5e98")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3424.h2ef5e98.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3422.h35a34dd
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3422.h35a34dd")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3422.h35a34dd.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3419.heefb644
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3419.heefb644")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3419.heefb644.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3417.h7f8b6c3
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3417.h7f8b6c3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3417.h7f8b6c3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3415.hdaa5886
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3415.hdaa5886")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3415.hdaa5886.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3410.h92609de
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3410.h92609de")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3410.h92609de.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3398.h6b9d3a8
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3398.h6b9d3a8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3398.h6b9d3a8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3396.h693ba30
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3396.h693ba30")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3396.h693ba30.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3394.h160f80b
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3394.h160f80b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3394.h160f80b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3392.h86a1670
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3392.h86a1670")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3392.h86a1670.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3389.h0e117e0
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3389.h0e117e0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3389.h0e117e0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3373.h41b9d77
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3373.h41b9d77")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3373.h41b9d77.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3371.hb225e9f
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3371.hb225e9f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3371.hb225e9f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3363.he6329f4
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3363.he6329f4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3363.he6329f4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3361.h6ba80cf
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3361.h6ba80cf")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3361.h6ba80cf.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3359.h0ebb9d7
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3359.h0ebb9d7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3359.h0ebb9d7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3356.h5c8d324
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3356.h5c8d324")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3356.h5c8d324.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3351.hbaf39b3
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3351.hbaf39b3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3351.hbaf39b3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3350.he7995d6
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3350.he7995d6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3350.he7995d6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3345.h4ce9c0d
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3345.h4ce9c0d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3345.h4ce9c0d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3342.hd1665ba
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3342.hd1665ba")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3342.hd1665ba.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3335.hbf26d8c
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3335.hbf26d8c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3335.hbf26d8c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3332.h7baa4b8
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3332.h7baa4b8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3332.h7baa4b8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3329.h62b91a1
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3329.h62b91a1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3329.h62b91a1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3328.he889659
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3328.he889659")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3328.he889659.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3324.hd9d4f70
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3324.hd9d4f70")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3324.hd9d4f70.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3322.hb2e2ed6
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3322.hb2e2ed6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3322.hb2e2ed6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3318.hea82df3
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3318.hea82df3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3318.hea82df3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3316.haf81d7f
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3316.haf81d7f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3316.haf81d7f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3314.h9f6ea44
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3314.h9f6ea44")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3314.h9f6ea44.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3310.hf248c5e
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3310.hf248c5e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3310.hf248c5e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3308.h4a92ad9
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3308.h4a92ad9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3308.h4a92ad9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3298.hfe2bf1b
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3298.hfe2bf1b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3298.hfe2bf1b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3295.h411f3b6
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3295.h411f3b6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3295.h411f3b6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3290.h0dde0c0
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3290.h0dde0c0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3290.h0dde0c0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3240.hc385d62
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3240.hc385d62")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3240.hc385d62.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3237.hd3c40af
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3237.hd3c40af")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3237.hd3c40af.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3236.ha769bc1
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3236.ha769bc1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3236.ha769bc1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3234.h65704c0
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3234.h65704c0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3234.h65704c0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3230.hc9e711b
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3230.hc9e711b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3230.hc9e711b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3228.h30733fe
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3228.h30733fe")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3228.h30733fe.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3224.h1b71389
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3224.h1b71389")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3224.h1b71389.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3222.hdfaf652
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3222.hdfaf652")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3222.hdfaf652.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3199.h54522bc
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3199.h54522bc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3199.h54522bc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3197.h9d22a1c
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3197.h9d22a1c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3197.h9d22a1c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3193.hfdae02b
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3193.hfdae02b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3193.hfdae02b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3192.hd3078d3
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3192.hd3078d3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3192.hd3078d3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3189.h203e36a
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3189.h203e36a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3189.h203e36a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3182.h5312a70
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3182.h5312a70")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3182.h5312a70.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3177.h0ffed53
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3177.h0ffed53")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3177.h0ffed53.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3171.h88669a1
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3171.h88669a1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3171.h88669a1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3170.h84b1db9
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3170.h84b1db9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3170.h84b1db9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3167.hc46696b
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3167.hc46696b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3167.hc46696b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3163.h436d10a
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3163.h436d10a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3163.h436d10a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3161.hfcbd1e1
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3161.hfcbd1e1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3161.hfcbd1e1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3160.h99ce3ba
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3160.h99ce3ba")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3160.h99ce3ba.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3157.h1184b4d
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3157.h1184b4d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3157.h1184b4d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3155.h1e6b3e6
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3155.h1e6b3e6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3155.h1e6b3e6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3151.hcb6d328
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3151.hcb6d328")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3151.hcb6d328.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3145.h62334a7
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3145.h62334a7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3145.h62334a7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3143.h7097990
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3143.h7097990")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3143.h7097990.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3142.h343b716
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3142.h343b716")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3142.h343b716.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3139.hd122942
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3139.hd122942")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3139.hd122942.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3135.ha81862c
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3135.ha81862c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3135.ha81862c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3129.h4244d4c
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3129.h4244d4c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3129.h4244d4c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3128.h52ffd88
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3128.h52ffd88")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3128.h52ffd88.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3126.hb7be2bf
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3126.hb7be2bf")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3126.hb7be2bf.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3123.hd7c46a6
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3123.hd7c46a6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3123.hd7c46a6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3115.h29473bf
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3115.h29473bf")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3115.h29473bf.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3114.h7b6d423
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3114.h7b6d423")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3114.h7b6d423.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3112.h965e2ce
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3112.h965e2ce")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3112.h965e2ce.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3109.hf5f6776
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3109.hf5f6776")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3109.hf5f6776.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3105.hd91181a
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3105.hd91181a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3105.hd91181a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3098.h9e2e544
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3098.h9e2e544")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3098.h9e2e544.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3095.h47da66c
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3095.h47da66c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3095.h47da66c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3093.hdb4bb6b
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3093.hdb4bb6b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3093.hdb4bb6b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3082.hcd0eca0
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3082.hcd0eca0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3082.hcd0eca0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3080.h8f9a1dc
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3080.h8f9a1dc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3080.h8f9a1dc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3077.hedf4660
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3077.hedf4660")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3077.hedf4660.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3076.h41b831d
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3076.h41b831d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3076.h41b831d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3073.he533f92
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3073.he533f92")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3073.he533f92.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3069.h2631d73
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3069.h2631d73")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3069.h2631d73.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3065.he5612d4
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3065.he5612d4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3065.he5612d4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3063.h6659e2e
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3063.h6659e2e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3063.h6659e2e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3060.h817473f
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3060.h817473f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3060.h817473f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3059.h67352d7
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3059.h67352d7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3059.h67352d7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3057.h0a4304c
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3057.h0a4304c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3057.h0a4304c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3056.h8738813
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3056.h8738813")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3056.h8738813.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3052.hf71969f
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3052.hf71969f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3052.hf71969f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3049.hf45dc0b
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3049.hf45dc0b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3049.hf45dc0b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3047.h082dcce
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3047.h082dcce")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3047.h082dcce.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3040.h861e70f
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3040.h861e70f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3040.h861e70f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3035.h32213a4
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3035.h32213a4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3035.h32213a4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3031.h8cac969
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3031.h8cac969")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3031.h8cac969.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3025.h276be90
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3025.h276be90")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3025.h276be90.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3024.h9641ab8
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3024.h9641ab8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3024.h9641ab8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3020.h694f8cd
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3020.h694f8cd")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3020.h694f8cd.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3018.he00ec49
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3018.he00ec49")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3018.he00ec49.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3014.hc3a5b4c
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3014.hc3a5b4c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3014.hc3a5b4c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3009.h9046454
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3009.h9046454")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3009.h9046454.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3002.h5f189ce
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3002.h5f189ce")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3002.h5f189ce.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.3000.h7c75885
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.3000.h7c75885")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.3000.h7c75885.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2998.hb5d64dc
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2998.hb5d64dc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2998.hb5d64dc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2996.hcb5e2f9
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2996.hcb5e2f9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2996.hcb5e2f9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2992.he27f63f
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2992.he27f63f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2992.he27f63f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2979.hb6b96db
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2979.hb6b96db")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2979.hb6b96db.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2974.h651ef5b
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2974.h651ef5b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2974.h651ef5b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2970.h3126fb6
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2970.h3126fb6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2970.h3126fb6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2964.hf1de1e5
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2964.hf1de1e5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2964.hf1de1e5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2961.h51c4ae8
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2961.h51c4ae8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2961.h51c4ae8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2957.h3afb84c
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2957.h3afb84c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2957.h3afb84c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2952.hbf560a0
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2952.hbf560a0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2952.hbf560a0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2949.h1b040a6
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2949.h1b040a6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2949.h1b040a6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2944.hc0afa4b
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2944.hc0afa4b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2944.hc0afa4b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2937.h0f65f33
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2937.h0f65f33")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2937.h0f65f33.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2936.h2ba938b
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2936.h2ba938b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2936.h2ba938b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2930.h972fcb8
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2930.h972fcb8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2930.h972fcb8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2928.he21fb57
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2928.he21fb57")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2928.he21fb57.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2927.h3de27f2
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2927.h3de27f2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2927.h3de27f2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2921.hea10e45
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2921.hea10e45")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2921.hea10e45.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2920.hc64d689
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2920.hc64d689")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2920.hc64d689.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2879.hf579142
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2879.hf579142")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2879.hf579142.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2874.h2811d52
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2874.h2811d52")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2874.h2811d52.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2866.hed86f2e
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2866.hed86f2e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2866.hed86f2e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2863.hb7ad979
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2863.hb7ad979")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2863.hb7ad979.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2848.h559edf4
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2848.h559edf4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2848.h559edf4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2835.h5099ba7
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2835.h5099ba7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2835.h5099ba7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2830.h932bf40
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2830.h932bf40")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2830.h932bf40.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2825.haf97bce
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2825.haf97bce")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2825.haf97bce.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2816.h5db2f98
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2816.h5db2f98")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2816.h5db2f98.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2809.h97a8ce1
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2809.h97a8ce1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2809.h97a8ce1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-1.0.0-0.dev.git.2797.h951750e
  (package
   (name "binderhub")
   (version "1.0.0-0.dev.git.2797.h951750e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-1.0.0-0.dev.git.2797.h951750e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n988.h72e1852
  (package
   (name "binderhub")
   (version "0.2.0-n988.h72e1852")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n988.h72e1852.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n978.h2eb8b07
  (package
   (name "binderhub")
   (version "0.2.0-n978.h2eb8b07")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n978.h2eb8b07.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n966.h248f390
  (package
   (name "binderhub")
   (version "0.2.0-n966.h248f390")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n966.h248f390.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n961.h20697c6
  (package
   (name "binderhub")
   (version "0.2.0-n961.h20697c6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n961.h20697c6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n960.h47d7917
  (package
   (name "binderhub")
   (version "0.2.0-n960.h47d7917")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n960.h47d7917.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n956.h64a6ec4
  (package
   (name "binderhub")
   (version "0.2.0-n956.h64a6ec4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n956.h64a6ec4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n950.hc93c7dc
  (package
   (name "binderhub")
   (version "0.2.0-n950.hc93c7dc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n950.hc93c7dc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n939.h0e1813a
  (package
   (name "binderhub")
   (version "0.2.0-n939.h0e1813a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n939.h0e1813a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n923.hb925fd8
  (package
   (name "binderhub")
   (version "0.2.0-n923.hb925fd8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n923.hb925fd8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n919.he87e25a
  (package
   (name "binderhub")
   (version "0.2.0-n919.he87e25a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n919.he87e25a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n914.h51b7354
  (package
   (name "binderhub")
   (version "0.2.0-n914.h51b7354")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n914.h51b7354.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n905.h3d3e24e
  (package
   (name "binderhub")
   (version "0.2.0-n905.h3d3e24e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n905.h3d3e24e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n897.h9fefdc4
  (package
   (name "binderhub")
   (version "0.2.0-n897.h9fefdc4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n897.h9fefdc4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n889.hef354c0
  (package
   (name "binderhub")
   (version "0.2.0-n889.hef354c0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n889.hef354c0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n886.h4169712
  (package
   (name "binderhub")
   (version "0.2.0-n886.h4169712")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n886.h4169712.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n880.hd19a36a
  (package
   (name "binderhub")
   (version "0.2.0-n880.hd19a36a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n880.hd19a36a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n878.h8da69f2
  (package
   (name "binderhub")
   (version "0.2.0-n878.h8da69f2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n878.h8da69f2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n877.hb10a3d7
  (package
   (name "binderhub")
   (version "0.2.0-n877.hb10a3d7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n877.hb10a3d7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n874.h63c27b3
  (package
   (name "binderhub")
   (version "0.2.0-n874.h63c27b3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n874.h63c27b3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n870.h3d61c7a
  (package
   (name "binderhub")
   (version "0.2.0-n870.h3d61c7a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n870.h3d61c7a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n868.h48bc0f6
  (package
   (name "binderhub")
   (version "0.2.0-n868.h48bc0f6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n868.h48bc0f6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n866.h4e75de4
  (package
   (name "binderhub")
   (version "0.2.0-n866.h4e75de4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n866.h4e75de4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n864.hf7c9615
  (package
   (name "binderhub")
   (version "0.2.0-n864.hf7c9615")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n864.hf7c9615.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n862.h9fa2747
  (package
   (name "binderhub")
   (version "0.2.0-n862.h9fa2747")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n862.h9fa2747.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n861.h013c491
  (package
   (name "binderhub")
   (version "0.2.0-n861.h013c491")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n861.h013c491.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n852.h7c39292
  (package
   (name "binderhub")
   (version "0.2.0-n852.h7c39292")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n852.h7c39292.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n848.h39a9e38
  (package
   (name "binderhub")
   (version "0.2.0-n848.h39a9e38")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n848.h39a9e38.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n845.hcc57b24
  (package
   (name "binderhub")
   (version "0.2.0-n845.hcc57b24")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n845.hcc57b24.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n839.hf8a9d7a
  (package
   (name "binderhub")
   (version "0.2.0-n839.hf8a9d7a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n839.hf8a9d7a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n836.ha010f18
  (package
   (name "binderhub")
   (version "0.2.0-n836.ha010f18")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n836.ha010f18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n832.ha303070
  (package
   (name "binderhub")
   (version "0.2.0-n832.ha303070")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n832.ha303070.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n830.h674e79b
  (package
   (name "binderhub")
   (version "0.2.0-n830.h674e79b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n830.h674e79b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n826.h2567057
  (package
   (name "binderhub")
   (version "0.2.0-n826.h2567057")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n826.h2567057.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n823.h2f07e0f
  (package
   (name "binderhub")
   (version "0.2.0-n823.h2f07e0f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n823.h2f07e0f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n818.h9e1a870
  (package
   (name "binderhub")
   (version "0.2.0-n818.h9e1a870")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n818.h9e1a870.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n816.h6339b8e
  (package
   (name "binderhub")
   (version "0.2.0-n816.h6339b8e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n816.h6339b8e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n813.h1ee60f4
  (package
   (name "binderhub")
   (version "0.2.0-n813.h1ee60f4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n813.h1ee60f4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n812.h94183ea
  (package
   (name "binderhub")
   (version "0.2.0-n812.h94183ea")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n812.h94183ea.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n807.h2a3930b
  (package
   (name "binderhub")
   (version "0.2.0-n807.h2a3930b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n807.h2a3930b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n803.h2d03d99
  (package
   (name "binderhub")
   (version "0.2.0-n803.h2d03d99")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n803.h2d03d99.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n800.h92fc291
  (package
   (name "binderhub")
   (version "0.2.0-n800.h92fc291")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n800.h92fc291.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n797.h0ceb746
  (package
   (name "binderhub")
   (version "0.2.0-n797.h0ceb746")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n797.h0ceb746.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n791.hc539a31
  (package
   (name "binderhub")
   (version "0.2.0-n791.hc539a31")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n791.hc539a31.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n789.h3af0092
  (package
   (name "binderhub")
   (version "0.2.0-n789.h3af0092")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n789.h3af0092.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n781.ha8e3444
  (package
   (name "binderhub")
   (version "0.2.0-n781.ha8e3444")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n781.ha8e3444.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n779.hc08d5db
  (package
   (name "binderhub")
   (version "0.2.0-n779.hc08d5db")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n779.hc08d5db.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n774.hde3f15d
  (package
   (name "binderhub")
   (version "0.2.0-n774.hde3f15d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n774.hde3f15d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n773.h1375015
  (package
   (name "binderhub")
   (version "0.2.0-n773.h1375015")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n773.h1375015.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n771.he04a8c3
  (package
   (name "binderhub")
   (version "0.2.0-n771.he04a8c3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n771.he04a8c3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n768.h13978ab
  (package
   (name "binderhub")
   (version "0.2.0-n768.h13978ab")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n768.h13978ab.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n767.h908d7a9
  (package
   (name "binderhub")
   (version "0.2.0-n767.h908d7a9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n767.h908d7a9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n764.h0c6ccc9
  (package
   (name "binderhub")
   (version "0.2.0-n764.h0c6ccc9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n764.h0c6ccc9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n763.h69833c2
  (package
   (name "binderhub")
   (version "0.2.0-n763.h69833c2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n763.h69833c2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n760.h731b92f
  (package
   (name "binderhub")
   (version "0.2.0-n760.h731b92f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n760.h731b92f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n757.h37e5620
  (package
   (name "binderhub")
   (version "0.2.0-n757.h37e5620")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n757.h37e5620.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n749.h70f757f
  (package
   (name "binderhub")
   (version "0.2.0-n749.h70f757f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n749.h70f757f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n747.h370b9c0
  (package
   (name "binderhub")
   (version "0.2.0-n747.h370b9c0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n747.h370b9c0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n746.h051f302
  (package
   (name "binderhub")
   (version "0.2.0-n746.h051f302")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n746.h051f302.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n744.h26d7196
  (package
   (name "binderhub")
   (version "0.2.0-n744.h26d7196")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n744.h26d7196.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n739.h7f15a99
  (package
   (name "binderhub")
   (version "0.2.0-n739.h7f15a99")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n739.h7f15a99.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n735.h372efd3
  (package
   (name "binderhub")
   (version "0.2.0-n735.h372efd3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n735.h372efd3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n723.h43ee103
  (package
   (name "binderhub")
   (version "0.2.0-n723.h43ee103")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n723.h43ee103.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n719.h59c0e2d
  (package
   (name "binderhub")
   (version "0.2.0-n719.h59c0e2d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n719.h59c0e2d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n717.he11dccf
  (package
   (name "binderhub")
   (version "0.2.0-n717.he11dccf")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n717.he11dccf.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n716.h1f81316
  (package
   (name "binderhub")
   (version "0.2.0-n716.h1f81316")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n716.h1f81316.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n713.hda37339
  (package
   (name "binderhub")
   (version "0.2.0-n713.hda37339")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n713.hda37339.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n711.h034f58f
  (package
   (name "binderhub")
   (version "0.2.0-n711.h034f58f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n711.h034f58f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n704.h2b1842e
  (package
   (name "binderhub")
   (version "0.2.0-n704.h2b1842e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n704.h2b1842e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n692.hdf3beb4
  (package
   (name "binderhub")
   (version "0.2.0-n692.hdf3beb4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n692.hdf3beb4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n691.h849cfd4
  (package
   (name "binderhub")
   (version "0.2.0-n691.h849cfd4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n691.h849cfd4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n689.h8c8c0be
  (package
   (name "binderhub")
   (version "0.2.0-n689.h8c8c0be")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n689.h8c8c0be.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n685.ha5aff91
  (package
   (name "binderhub")
   (version "0.2.0-n685.ha5aff91")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n685.ha5aff91.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n683.h469f5e5
  (package
   (name "binderhub")
   (version "0.2.0-n683.h469f5e5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n683.h469f5e5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n676.h0cbee48
  (package
   (name "binderhub")
   (version "0.2.0-n676.h0cbee48")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n676.h0cbee48.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n674.h9b9895d
  (package
   (name "binderhub")
   (version "0.2.0-n674.h9b9895d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n674.h9b9895d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n673.hd733087
  (package
   (name "binderhub")
   (version "0.2.0-n673.hd733087")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n673.hd733087.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n671.hbe94c46
  (package
   (name "binderhub")
   (version "0.2.0-n671.hbe94c46")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n671.hbe94c46.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n668.hd8e7128
  (package
   (name "binderhub")
   (version "0.2.0-n668.hd8e7128")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n668.hd8e7128.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n665.h4f53b2e
  (package
   (name "binderhub")
   (version "0.2.0-n665.h4f53b2e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n665.h4f53b2e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n662.he137dbf
  (package
   (name "binderhub")
   (version "0.2.0-n662.he137dbf")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n662.he137dbf.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n661.h8269b12
  (package
   (name "binderhub")
   (version "0.2.0-n661.h8269b12")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n661.h8269b12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n658.h740b467
  (package
   (name "binderhub")
   (version "0.2.0-n658.h740b467")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n658.h740b467.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n655.h7cec065
  (package
   (name "binderhub")
   (version "0.2.0-n655.h7cec065")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n655.h7cec065.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n653.h195caac
  (package
   (name "binderhub")
   (version "0.2.0-n653.h195caac")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n653.h195caac.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n652.h034430a
  (package
   (name "binderhub")
   (version "0.2.0-n652.h034430a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n652.h034430a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n648.h8d1c0e7
  (package
   (name "binderhub")
   (version "0.2.0-n648.h8d1c0e7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n648.h8d1c0e7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n645.h06139f0
  (package
   (name "binderhub")
   (version "0.2.0-n645.h06139f0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n645.h06139f0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n641.h73c6ebd
  (package
   (name "binderhub")
   (version "0.2.0-n641.h73c6ebd")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n641.h73c6ebd.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n636.h8e3b5ac
  (package
   (name "binderhub")
   (version "0.2.0-n636.h8e3b5ac")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n636.h8e3b5ac.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n611.he777436
  (package
   (name "binderhub")
   (version "0.2.0-n611.he777436")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n611.he777436.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n609.h195837f
  (package
   (name "binderhub")
   (version "0.2.0-n609.h195837f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n609.h195837f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n605.h1d0ad75
  (package
   (name "binderhub")
   (version "0.2.0-n605.h1d0ad75")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n605.h1d0ad75.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n604.h9bc978a
  (package
   (name "binderhub")
   (version "0.2.0-n604.h9bc978a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n604.h9bc978a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n600.h36369a7
  (package
   (name "binderhub")
   (version "0.2.0-n600.h36369a7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n600.h36369a7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n598.h990047b
  (package
   (name "binderhub")
   (version "0.2.0-n598.h990047b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n598.h990047b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n597.h81451bf
  (package
   (name "binderhub")
   (version "0.2.0-n597.h81451bf")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n597.h81451bf.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n592.hc796c4d
  (package
   (name "binderhub")
   (version "0.2.0-n592.hc796c4d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n592.hc796c4d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n590.h9f942c5
  (package
   (name "binderhub")
   (version "0.2.0-n590.h9f942c5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n590.h9f942c5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n588.hec7671f
  (package
   (name "binderhub")
   (version "0.2.0-n588.hec7671f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n588.hec7671f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n586.h23477fb
  (package
   (name "binderhub")
   (version "0.2.0-n586.h23477fb")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n586.h23477fb.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n584.hf01c1e4
  (package
   (name "binderhub")
   (version "0.2.0-n584.hf01c1e4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n584.hf01c1e4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n577.h14cc6c7
  (package
   (name "binderhub")
   (version "0.2.0-n577.h14cc6c7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n577.h14cc6c7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n575.h2dc091f
  (package
   (name "binderhub")
   (version "0.2.0-n575.h2dc091f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n575.h2dc091f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n571.hc0efe13
  (package
   (name "binderhub")
   (version "0.2.0-n571.hc0efe13")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n571.hc0efe13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n563.h72d30fb
  (package
   (name "binderhub")
   (version "0.2.0-n563.h72d30fb")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n563.h72d30fb.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n562.h0b4462c
  (package
   (name "binderhub")
   (version "0.2.0-n562.h0b4462c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n562.h0b4462c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n557.h46ddaac
  (package
   (name "binderhub")
   (version "0.2.0-n557.h46ddaac")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n557.h46ddaac.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n555.heb692c5
  (package
   (name "binderhub")
   (version "0.2.0-n555.heb692c5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n555.heb692c5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n554.h4b41dc2
  (package
   (name "binderhub")
   (version "0.2.0-n554.h4b41dc2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n554.h4b41dc2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n536.h6034dc5
  (package
   (name "binderhub")
   (version "0.2.0-n536.h6034dc5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n536.h6034dc5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n535.h5c6afe8
  (package
   (name "binderhub")
   (version "0.2.0-n535.h5c6afe8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n535.h5c6afe8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n528.h3aa7cd9
  (package
   (name "binderhub")
   (version "0.2.0-n528.h3aa7cd9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n528.h3aa7cd9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n525.h7fc6e48
  (package
   (name "binderhub")
   (version "0.2.0-n525.h7fc6e48")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n525.h7fc6e48.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n523.h854be18
  (package
   (name "binderhub")
   (version "0.2.0-n523.h854be18")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n523.h854be18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n521.hd5efd72
  (package
   (name "binderhub")
   (version "0.2.0-n521.hd5efd72")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n521.hd5efd72.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n519.hf5c1a19
  (package
   (name "binderhub")
   (version "0.2.0-n519.hf5c1a19")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n519.hf5c1a19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n514.h9fb668d
  (package
   (name "binderhub")
   (version "0.2.0-n514.h9fb668d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n514.h9fb668d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n512.he17e39f
  (package
   (name "binderhub")
   (version "0.2.0-n512.he17e39f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n512.he17e39f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n510.h06dbe4c
  (package
   (name "binderhub")
   (version "0.2.0-n510.h06dbe4c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n510.h06dbe4c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n509.hc028fda
  (package
   (name "binderhub")
   (version "0.2.0-n509.hc028fda")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n509.hc028fda.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n506.hd631d84
  (package
   (name "binderhub")
   (version "0.2.0-n506.hd631d84")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n506.hd631d84.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n499.h81660eb
  (package
   (name "binderhub")
   (version "0.2.0-n499.h81660eb")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n499.h81660eb.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n496.h988aca0
  (package
   (name "binderhub")
   (version "0.2.0-n496.h988aca0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n496.h988aca0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n472.h32e06ee
  (package
   (name "binderhub")
   (version "0.2.0-n472.h32e06ee")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n472.h32e06ee.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n471.hf76e37e
  (package
   (name "binderhub")
   (version "0.2.0-n471.hf76e37e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n471.hf76e37e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n467.hbc700d4
  (package
   (name "binderhub")
   (version "0.2.0-n467.hbc700d4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n467.hbc700d4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n465.hb35ec4e
  (package
   (name "binderhub")
   (version "0.2.0-n465.hb35ec4e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n465.hb35ec4e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n461.h5651b33
  (package
   (name "binderhub")
   (version "0.2.0-n461.h5651b33")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n461.h5651b33.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n458.h4aee1fd
  (package
   (name "binderhub")
   (version "0.2.0-n458.h4aee1fd")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n458.h4aee1fd.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n454.h97fb8c3
  (package
   (name "binderhub")
   (version "0.2.0-n454.h97fb8c3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n454.h97fb8c3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n451.h6a2b398
  (package
   (name "binderhub")
   (version "0.2.0-n451.h6a2b398")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n451.h6a2b398.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n449.h8ab9f23
  (package
   (name "binderhub")
   (version "0.2.0-n449.h8ab9f23")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n449.h8ab9f23.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n444.h8c3bd51
  (package
   (name "binderhub")
   (version "0.2.0-n444.h8c3bd51")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n444.h8c3bd51.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n439.h2b921f1
  (package
   (name "binderhub")
   (version "0.2.0-n439.h2b921f1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n439.h2b921f1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n432.h687657b
  (package
   (name "binderhub")
   (version "0.2.0-n432.h687657b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n432.h687657b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n423.ha36348e
  (package
   (name "binderhub")
   (version "0.2.0-n423.ha36348e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n423.ha36348e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n412.h1f1f0c4
  (package
   (name "binderhub")
   (version "0.2.0-n412.h1f1f0c4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n412.h1f1f0c4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n409.hc20a2da
  (package
   (name "binderhub")
   (version "0.2.0-n409.hc20a2da")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n409.hc20a2da.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n388.h9cc5a54
  (package
   (name "binderhub")
   (version "0.2.0-n388.h9cc5a54")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n388.h9cc5a54.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n361.h6f57706
  (package
   (name "binderhub")
   (version "0.2.0-n361.h6f57706")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n361.h6f57706.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n359.h8a9d4ca
  (package
   (name "binderhub")
   (version "0.2.0-n359.h8a9d4ca")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n359.h8a9d4ca.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n357.hdfe0201
  (package
   (name "binderhub")
   (version "0.2.0-n357.hdfe0201")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n357.hdfe0201.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n348.h264289a
  (package
   (name "binderhub")
   (version "0.2.0-n348.h264289a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n348.h264289a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n301.hc5e7db2
  (package
   (name "binderhub")
   (version "0.2.0-n301.hc5e7db2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n301.hc5e7db2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n299.h343b871
  (package
   (name "binderhub")
   (version "0.2.0-n299.h343b871")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n299.h343b871.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n295.h9e6d009
  (package
   (name "binderhub")
   (version "0.2.0-n295.h9e6d009")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n295.h9e6d009.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n293.h7e04ad4
  (package
   (name "binderhub")
   (version "0.2.0-n293.h7e04ad4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n293.h7e04ad4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n287.h068b631
  (package
   (name "binderhub")
   (version "0.2.0-n287.h068b631")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n287.h068b631.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n277.h3187c31
  (package
   (name "binderhub")
   (version "0.2.0-n277.h3187c31")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n277.h3187c31.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n274.hc69f4c8
  (package
   (name "binderhub")
   (version "0.2.0-n274.hc69f4c8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n274.hc69f4c8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n269.hcb9bc69
  (package
   (name "binderhub")
   (version "0.2.0-n269.hcb9bc69")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n269.hcb9bc69.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n267.h1619d74
  (package
   (name "binderhub")
   (version "0.2.0-n267.h1619d74")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n267.h1619d74.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n265.h5625d55
  (package
   (name "binderhub")
   (version "0.2.0-n265.h5625d55")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n265.h5625d55.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n262.hbd81b48
  (package
   (name "binderhub")
   (version "0.2.0-n262.hbd81b48")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n262.hbd81b48.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n259.ha4908da
  (package
   (name "binderhub")
   (version "0.2.0-n259.ha4908da")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n259.ha4908da.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n257.h1c01fb1
  (package
   (name "binderhub")
   (version "0.2.0-n257.h1c01fb1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n257.h1c01fb1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n254.h7cedaa7
  (package
   (name "binderhub")
   (version "0.2.0-n254.h7cedaa7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n254.h7cedaa7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n251.h025ce54
  (package
   (name "binderhub")
   (version "0.2.0-n251.h025ce54")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n251.h025ce54.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n244.hd6d3e22
  (package
   (name "binderhub")
   (version "0.2.0-n244.hd6d3e22")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n244.hd6d3e22.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n242.h87e78af
  (package
   (name "binderhub")
   (version "0.2.0-n242.h87e78af")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n242.h87e78af.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n239.hddb603b
  (package
   (name "binderhub")
   (version "0.2.0-n239.hddb603b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n239.hddb603b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n232.hf08d55f
  (package
   (name "binderhub")
   (version "0.2.0-n232.hf08d55f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n232.hf08d55f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n228.ha00bbbb
  (package
   (name "binderhub")
   (version "0.2.0-n228.ha00bbbb")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n228.ha00bbbb.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n224.hf5cc56a
  (package
   (name "binderhub")
   (version "0.2.0-n224.hf5cc56a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n224.hf5cc56a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n221.h1875bcb
  (package
   (name "binderhub")
   (version "0.2.0-n221.h1875bcb")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n221.h1875bcb.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n219.hbc17443
  (package
   (name "binderhub")
   (version "0.2.0-n219.hbc17443")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n219.hbc17443.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n217.h35366ea
  (package
   (name "binderhub")
   (version "0.2.0-n217.h35366ea")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n217.h35366ea.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n215.h9447e17
  (package
   (name "binderhub")
   (version "0.2.0-n215.h9447e17")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n215.h9447e17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n213.hbb8f58f
  (package
   (name "binderhub")
   (version "0.2.0-n213.hbb8f58f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n213.hbb8f58f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n208.hb81d913
  (package
   (name "binderhub")
   (version "0.2.0-n208.hb81d913")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n208.hb81d913.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n206.h030b435
  (package
   (name "binderhub")
   (version "0.2.0-n206.h030b435")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n206.h030b435.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n202.hd01068c
  (package
   (name "binderhub")
   (version "0.2.0-n202.hd01068c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n202.hd01068c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n200.h305d0e8
  (package
   (name "binderhub")
   (version "0.2.0-n200.h305d0e8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n200.h305d0e8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n198.hcf2abd9
  (package
   (name "binderhub")
   (version "0.2.0-n198.hcf2abd9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n198.hcf2abd9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n193.hfc12027
  (package
   (name "binderhub")
   (version "0.2.0-n193.hfc12027")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n193.hfc12027.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n192.h900d1d6
  (package
   (name "binderhub")
   (version "0.2.0-n192.h900d1d6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n192.h900d1d6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n189.h5ece772
  (package
   (name "binderhub")
   (version "0.2.0-n189.h5ece772")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n189.h5ece772.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n187.hdb3aa0e
  (package
   (name "binderhub")
   (version "0.2.0-n187.hdb3aa0e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n187.hdb3aa0e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n186.h58a0b72
  (package
   (name "binderhub")
   (version "0.2.0-n186.h58a0b72")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n186.h58a0b72.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n181.h23ae206
  (package
   (name "binderhub")
   (version "0.2.0-n181.h23ae206")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n181.h23ae206.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n178.h9cff5e3
  (package
   (name "binderhub")
   (version "0.2.0-n178.h9cff5e3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n178.h9cff5e3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n175.he98f51d
  (package
   (name "binderhub")
   (version "0.2.0-n175.he98f51d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n175.he98f51d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n172.h9874c43
  (package
   (name "binderhub")
   (version "0.2.0-n172.h9874c43")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n172.h9874c43.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n169.h02ea279
  (package
   (name "binderhub")
   (version "0.2.0-n169.h02ea279")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n169.h02ea279.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n162.h003d856
  (package
   (name "binderhub")
   (version "0.2.0-n162.h003d856")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n162.h003d856.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n156.hec14d4a
  (package
   (name "binderhub")
   (version "0.2.0-n156.hec14d4a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n156.hec14d4a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n153.h7db895c
  (package
   (name "binderhub")
   (version "0.2.0-n153.h7db895c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n153.h7db895c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n152.hf01e3b2
  (package
   (name "binderhub")
   (version "0.2.0-n152.hf01e3b2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n152.hf01e3b2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n146.hfa59322
  (package
   (name "binderhub")
   (version "0.2.0-n146.hfa59322")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n146.hfa59322.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n144.h99e117b
  (package
   (name "binderhub")
   (version "0.2.0-n144.h99e117b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n144.h99e117b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n140.h18be10d
  (package
   (name "binderhub")
   (version "0.2.0-n140.h18be10d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n140.h18be10d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n139.ha1f9f09
  (package
   (name "binderhub")
   (version "0.2.0-n139.ha1f9f09")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n139.ha1f9f09.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n136.h146e5d2
  (package
   (name "binderhub")
   (version "0.2.0-n136.h146e5d2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n136.h146e5d2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n134.hb4819f9
  (package
   (name "binderhub")
   (version "0.2.0-n134.hb4819f9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n134.hb4819f9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n132.h1a8ce62
  (package
   (name "binderhub")
   (version "0.2.0-n132.h1a8ce62")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n132.h1a8ce62.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n130.hc693dc5
  (package
   (name "binderhub")
   (version "0.2.0-n130.hc693dc5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n130.hc693dc5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n128.h00b0576
  (package
   (name "binderhub")
   (version "0.2.0-n128.h00b0576")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n128.h00b0576.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n126.h5a1202f
  (package
   (name "binderhub")
   (version "0.2.0-n126.h5a1202f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n126.h5a1202f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n124.h7f83ebf
  (package
   (name "binderhub")
   (version "0.2.0-n124.h7f83ebf")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n124.h7f83ebf.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n121.h6d936d7
  (package
   (name "binderhub")
   (version "0.2.0-n121.h6d936d7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n121.h6d936d7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n120.h207ae70
  (package
   (name "binderhub")
   (version "0.2.0-n120.h207ae70")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n120.h207ae70.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n106.hd61fc9a
  (package
   (name "binderhub")
   (version "0.2.0-n106.hd61fc9a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n106.hd61fc9a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n104.h786c8e0
  (package
   (name "binderhub")
   (version "0.2.0-n104.h786c8e0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n104.h786c8e0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n102.hec2a2f3
  (package
   (name "binderhub")
   (version "0.2.0-n102.hec2a2f3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n102.hec2a2f3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n1011.hb49edf6
  (package
   (name "binderhub")
   (version "0.2.0-n1011.hb49edf6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n1011.hb49edf6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-n096.h4e521a1
  (package
   (name "binderhub")
   (version "0.2.0-n096.h4e521a1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n096.h4e521a1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n093.h7cfad48
  (package
   (name "binderhub")
   (version "0.2.0-n093.h7cfad48")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n093.h7cfad48.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n079.h351d336
  (package
   (name "binderhub")
   (version "0.2.0-n079.h351d336")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n079.h351d336.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n077.hef338bc
  (package
   (name "binderhub")
   (version "0.2.0-n077.hef338bc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n077.hef338bc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-n075.hb369d46
  (package
   (name "binderhub")
   (version "0.2.0-n075.hb369d46")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-n075.hb369d46.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-fe678dc
  (package
   (name "binderhub")
   (version "0.2.0-fe678dc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-fe678dc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-fe62cda
  (package
   (name "binderhub")
   (version "0.2.0-fe62cda")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-fe62cda.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-fcc4a4b
  (package
   (name "binderhub")
   (version "0.2.0-fcc4a4b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-fcc4a4b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-fbbc302
  (package
   (name "binderhub")
   (version "0.2.0-fbbc302")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-fbbc302.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-f98a1d0
  (package
   (name "binderhub")
   (version "0.2.0-f98a1d0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-f98a1d0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-f80738c
  (package
   (name "binderhub")
   (version "0.2.0-f80738c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-f80738c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-f746e50
  (package
   (name "binderhub")
   (version "0.2.0-f746e50")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-f746e50.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-f5cf428
  (package
   (name "binderhub")
   (version "0.2.0-f5cf428")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-f5cf428.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-f565958
  (package
   (name "binderhub")
   (version "0.2.0-f565958")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-f565958.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-eb1fc66
  (package
   (name "binderhub")
   (version "0.2.0-eb1fc66")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-eb1fc66.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-d8b8349
  (package
   (name "binderhub")
   (version "0.2.0-d8b8349")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-d8b8349.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-d50f045
  (package
   (name "binderhub")
   (version "0.2.0-d50f045")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-d50f045.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-d4b724a
  (package
   (name "binderhub")
   (version "0.2.0-d4b724a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-d4b724a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-d2e3b8b
  (package
   (name "binderhub")
   (version "0.2.0-d2e3b8b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-d2e3b8b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-c8ab0a3
  (package
   (name "binderhub")
   (version "0.2.0-c8ab0a3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-c8ab0a3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-c80b26c
  (package
   (name "binderhub")
   (version "0.2.0-c80b26c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-c80b26c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-c30114a
  (package
   (name "binderhub")
   (version "0.2.0-c30114a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-c30114a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-baa3808
  (package
   (name "binderhub")
   (version "0.2.0-baa3808")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-baa3808.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-b9ed7f5
  (package
   (name "binderhub")
   (version "0.2.0-b9ed7f5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-b9ed7f5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-b7f6047
  (package
   (name "binderhub")
   (version "0.2.0-b7f6047")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-b7f6047.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-b593363
  (package
   (name "binderhub")
   (version "0.2.0-b593363")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-b593363.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-b3c9128
  (package
   (name "binderhub")
   (version "0.2.0-b3c9128")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-b3c9128.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-b16f493
  (package
   (name "binderhub")
   (version "0.2.0-b16f493")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-b16f493.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-aef8b76
  (package
   (name "binderhub")
   (version "0.2.0-aef8b76")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-aef8b76.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-ad3881e
  (package
   (name "binderhub")
   (version "0.2.0-ad3881e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-ad3881e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-acc1724
  (package
   (name "binderhub")
   (version "0.2.0-acc1724")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-acc1724.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-a72d4d0
  (package
   (name "binderhub")
   (version "0.2.0-a72d4d0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-a72d4d0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-a5e19b3
  (package
   (name "binderhub")
   (version "0.2.0-a5e19b3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-a5e19b3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-a529251
  (package
   (name "binderhub")
   (version "0.2.0-a529251")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-a529251.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-a21e7f1
  (package
   (name "binderhub")
   (version "0.2.0-a21e7f1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-a21e7f1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-a2079a5
  (package
   (name "binderhub")
   (version "0.2.0-a2079a5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-a2079a5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-9fb6d3f
  (package
   (name "binderhub")
   (version "0.2.0-9fb6d3f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-9fb6d3f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-9f83710
  (package
   (name "binderhub")
   (version "0.2.0-9f83710")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-9f83710.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-9c3045e
  (package
   (name "binderhub")
   (version "0.2.0-9c3045e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-9c3045e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-99af80d
  (package
   (name "binderhub")
   (version "0.2.0-99af80d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-99af80d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-9994a92
  (package
   (name "binderhub")
   (version "0.2.0-9994a92")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-9994a92.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-95e023e
  (package
   (name "binderhub")
   (version "0.2.0-95e023e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-95e023e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-9469b37
  (package
   (name "binderhub")
   (version "0.2.0-9469b37")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-9469b37.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-93bb7e4
  (package
   (name "binderhub")
   (version "0.2.0-93bb7e4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-93bb7e4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-908c443
  (package
   (name "binderhub")
   (version "0.2.0-908c443")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-908c443.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-8f31091
  (package
   (name "binderhub")
   (version "0.2.0-8f31091")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-8f31091.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-8d3a41b
  (package
   (name "binderhub")
   (version "0.2.0-8d3a41b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-8d3a41b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-8d01547
  (package
   (name "binderhub")
   (version "0.2.0-8d01547")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-8d01547.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-899fed2
  (package
   (name "binderhub")
   (version "0.2.0-899fed2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-899fed2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-889380c
  (package
   (name "binderhub")
   (version "0.2.0-889380c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-889380c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-851025f
  (package
   (name "binderhub")
   (version "0.2.0-851025f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-851025f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-82fc209
  (package
   (name "binderhub")
   (version "0.2.0-82fc209")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-82fc209.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-822f7c9
  (package
   (name "binderhub")
   (version "0.2.0-822f7c9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-822f7c9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-817d3c9
  (package
   (name "binderhub")
   (version "0.2.0-817d3c9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-817d3c9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-80ac52c
  (package
   (name "binderhub")
   (version "0.2.0-80ac52c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-80ac52c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-7eef07e
  (package
   (name "binderhub")
   (version "0.2.0-7eef07e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-7eef07e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-7e8e9b7
  (package
   (name "binderhub")
   (version "0.2.0-7e8e9b7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-7e8e9b7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-7c3271e
  (package
   (name "binderhub")
   (version "0.2.0-7c3271e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-7c3271e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-7b2c4f8
  (package
   (name "binderhub")
   (version "0.2.0-7b2c4f8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-7b2c4f8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-798f8c6
  (package
   (name "binderhub")
   (version "0.2.0-798f8c6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-798f8c6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-6bfd93b
  (package
   (name "binderhub")
   (version "0.2.0-6bfd93b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-6bfd93b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-65e6ae4
  (package
   (name "binderhub")
   (version "0.2.0-65e6ae4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-65e6ae4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-658032c
  (package
   (name "binderhub")
   (version "0.2.0-658032c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-658032c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-6578e4d
  (package
   (name "binderhub")
   (version "0.2.0-6578e4d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-6578e4d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-654b31d
  (package
   (name "binderhub")
   (version "0.2.0-654b31d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-654b31d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-64bb3c7
  (package
   (name "binderhub")
   (version "0.2.0-64bb3c7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-64bb3c7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-612ade7
  (package
   (name "binderhub")
   (version "0.2.0-612ade7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-612ade7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-5ca42ec
  (package
   (name "binderhub")
   (version "0.2.0-5ca42ec")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-5ca42ec.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-5be4d08
  (package
   (name "binderhub")
   (version "0.2.0-5be4d08")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-5be4d08.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-5b3824d
  (package
   (name "binderhub")
   (version "0.2.0-5b3824d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-5b3824d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-58e8ae9
  (package
   (name "binderhub")
   (version "0.2.0-58e8ae9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-58e8ae9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-575fb2a
  (package
   (name "binderhub")
   (version "0.2.0-575fb2a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-575fb2a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-5536a0f
  (package
   (name "binderhub")
   (version "0.2.0-5536a0f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-5536a0f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-51c34ff
  (package
   (name "binderhub")
   (version "0.2.0-51c34ff")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-51c34ff.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-501f767
  (package
   (name "binderhub")
   (version "0.2.0-501f767")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-501f767.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-4db41e0
  (package
   (name "binderhub")
   (version "0.2.0-4db41e0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-4db41e0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-449bf67
  (package
   (name "binderhub")
   (version "0.2.0-449bf67")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-449bf67.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-4300f2c
  (package
   (name "binderhub")
   (version "0.2.0-4300f2c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-4300f2c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-409f200
  (package
   (name "binderhub")
   (version "0.2.0-409f200")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-409f200.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-3dd5511
  (package
   (name "binderhub")
   (version "0.2.0-3dd5511")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-3dd5511.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-3b53fce
  (package
   (name "binderhub")
   (version "0.2.0-3b53fce")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-3b53fce.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-35d4440
  (package
   (name "binderhub")
   (version "0.2.0-35d4440")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-35d4440.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-33a907e
  (package
   (name "binderhub")
   (version "0.2.0-33a907e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-33a907e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-2fe1a63
  (package
   (name "binderhub")
   (version "0.2.0-2fe1a63")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-2fe1a63.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-2e5086a
  (package
   (name "binderhub")
   (version "0.2.0-2e5086a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-2e5086a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-2b78880
  (package
   (name "binderhub")
   (version "0.2.0-2b78880")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-2b78880.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-2b368e4
  (package
   (name "binderhub")
   (version "0.2.0-2b368e4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-2b368e4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-29e7546
  (package
   (name "binderhub")
   (version "0.2.0-29e7546")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-29e7546.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-2935b4e
  (package
   (name "binderhub")
   (version "0.2.0-2935b4e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-2935b4e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-27f6538
  (package
   (name "binderhub")
   (version "0.2.0-27f6538")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-27f6538.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-276d090
  (package
   (name "binderhub")
   (version "0.2.0-276d090")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-276d090.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-275f286
  (package
   (name "binderhub")
   (version "0.2.0-275f286")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-275f286.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-1efa7b8
  (package
   (name "binderhub")
   (version "0.2.0-1efa7b8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-1efa7b8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-1eac3a0
  (package
   (name "binderhub")
   (version "0.2.0-1eac3a0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-1eac3a0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-1a76d5f
  (package
   (name "binderhub")
   (version "0.2.0-1a76d5f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-1a76d5f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-11f7e69
  (package
   (name "binderhub")
   (version "0.2.0-11f7e69")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-11f7e69.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-10ac4d8
  (package
   (name "binderhub")
   (version "0.2.0-10ac4d8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-10ac4d8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-10a3dee
  (package
   (name "binderhub")
   (version "0.2.0-10a3dee")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-10a3dee.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-0d300b8
  (package
   (name "binderhub")
   (version "0.2.0-0d300b8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-0d300b8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-07b71cd
  (package
   (name "binderhub")
   (version "0.2.0-07b71cd")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-07b71cd.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-4118033
  (package
   (name "binderhub")
   (version "0.2.0-4118033")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-4118033.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-4013220
  (package
   (name "binderhub")
   (version "0.2.0-4013220")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-4013220.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-1650132
  (package
   (name "binderhub")
   (version "0.2.0-1650132")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-1650132.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.2.0-0.dev.git.2789.h39980f0
  (package
   (name "binderhub")
   (version "0.2.0-0.dev.git.2789.h39980f0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-0.dev.git.2789.h39980f0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-0.dev.git.2782.h3ccfc44
  (package
   (name "binderhub")
   (version "0.2.0-0.dev.git.2782.h3ccfc44")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-0.dev.git.2782.h3ccfc44.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-0.dev.git.2778.h071e2ce
  (package
   (name "binderhub")
   (version "0.2.0-0.dev.git.2778.h071e2ce")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-0.dev.git.2778.h071e2ce.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-0.dev.git.2772.h904afb3
  (package
   (name "binderhub")
   (version "0.2.0-0.dev.git.2772.h904afb3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-0.dev.git.2772.h904afb3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-0.dev.git.2771.h969f2f6
  (package
   (name "binderhub")
   (version "0.2.0-0.dev.git.2771.h969f2f6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-0.dev.git.2771.h969f2f6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-0.dev.git.2763.h4324327
  (package
   (name "binderhub")
   (version "0.2.0-0.dev.git.2763.h4324327")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-0.dev.git.2763.h4324327.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-0.dev.git.2762.h3b55a37
  (package
   (name "binderhub")
   (version "0.2.0-0.dev.git.2762.h3b55a37")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-0.dev.git.2762.h3b55a37.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-0.dev.git.2754.h9bf1418
  (package
   (name "binderhub")
   (version "0.2.0-0.dev.git.2754.h9bf1418")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-0.dev.git.2754.h9bf1418.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.2.0-0.dev.git.2752.h3450e52
  (package
   (name "binderhub")
   (version "0.2.0-0.dev.git.2752.h3450e52")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.2.0-0.dev.git.2752.h3450e52.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://binderhub.readthedocs.io/en/latest/")
   (synopsis "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (description "BinderHub is like a JupyterHub that automatically builds environments for the users based on repo2docker. A BinderHub is by default not configured to authenticate users or provide storage for them.")
   (license #f)))

(define-public binderhub-0.1.0-fd778a3
  (package
   (name "binderhub")
   (version "0.1.0-fd778a3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-fd778a3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-fd0daf7
  (package
   (name "binderhub")
   (version "0.1.0-fd0daf7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-fd0daf7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-fc3b1a7
  (package
   (name "binderhub")
   (version "0.1.0-fc3b1a7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-fc3b1a7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-fc29c17
  (package
   (name "binderhub")
   (version "0.1.0-fc29c17")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-fc29c17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-fbf6e5a
  (package
   (name "binderhub")
   (version "0.1.0-fbf6e5a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-fbf6e5a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-fbd816c
  (package
   (name "binderhub")
   (version "0.1.0-fbd816c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-fbd816c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-faa453c
  (package
   (name "binderhub")
   (version "0.1.0-faa453c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-faa453c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f87ac35
  (package
   (name "binderhub")
   (version "0.1.0-f87ac35")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f87ac35.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f810e0e
  (package
   (name "binderhub")
   (version "0.1.0-f810e0e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f810e0e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f71de2c
  (package
   (name "binderhub")
   (version "0.1.0-f71de2c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f71de2c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f707e41
  (package
   (name "binderhub")
   (version "0.1.0-f707e41")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f707e41.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f6dbf3a
  (package
   (name "binderhub")
   (version "0.1.0-f6dbf3a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f6dbf3a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f5f76e6
  (package
   (name "binderhub")
   (version "0.1.0-f5f76e6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f5f76e6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f46f161
  (package
   (name "binderhub")
   (version "0.1.0-f46f161")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f46f161.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f42be0f
  (package
   (name "binderhub")
   (version "0.1.0-f42be0f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f42be0f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f33fea6
  (package
   (name "binderhub")
   (version "0.1.0-f33fea6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f33fea6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f303f01
  (package
   (name "binderhub")
   (version "0.1.0-f303f01")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f303f01.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-f110d6b
  (package
   (name "binderhub")
   (version "0.1.0-f110d6b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-f110d6b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-ee9373b
  (package
   (name "binderhub")
   (version "0.1.0-ee9373b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-ee9373b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-ee79f00
  (package
   (name "binderhub")
   (version "0.1.0-ee79f00")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-ee79f00.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-ed8217d
  (package
   (name "binderhub")
   (version "0.1.0-ed8217d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-ed8217d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e94b48b
  (package
   (name "binderhub")
   (version "0.1.0-e94b48b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e94b48b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e94a2a4
  (package
   (name "binderhub")
   (version "0.1.0-e94a2a4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e94a2a4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e80b083
  (package
   (name "binderhub")
   (version "0.1.0-e80b083")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e80b083.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e75cc14
  (package
   (name "binderhub")
   (version "0.1.0-e75cc14")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e75cc14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e65121b
  (package
   (name "binderhub")
   (version "0.1.0-e65121b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e65121b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e64bc21
  (package
   (name "binderhub")
   (version "0.1.0-e64bc21")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e64bc21.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e56e50a
  (package
   (name "binderhub")
   (version "0.1.0-e56e50a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e56e50a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e54c217
  (package
   (name "binderhub")
   (version "0.1.0-e54c217")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e54c217.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e539e64
  (package
   (name "binderhub")
   (version "0.1.0-e539e64")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e539e64.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e2cffe5
  (package
   (name "binderhub")
   (version "0.1.0-e2cffe5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e2cffe5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-e113dbb
  (package
   (name "binderhub")
   (version "0.1.0-e113dbb")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-e113dbb.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-dec8682
  (package
   (name "binderhub")
   (version "0.1.0-dec8682")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-dec8682.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-dd2de38
  (package
   (name "binderhub")
   (version "0.1.0-dd2de38")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-dd2de38.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-da6b462
  (package
   (name "binderhub")
   (version "0.1.0-da6b462")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-da6b462.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-d9d7613
  (package
   (name "binderhub")
   (version "0.1.0-d9d7613")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-d9d7613.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-d95cd14
  (package
   (name "binderhub")
   (version "0.1.0-d95cd14")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-d95cd14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-d780bd6
  (package
   (name "binderhub")
   (version "0.1.0-d780bd6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-d780bd6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-d434a0d
  (package
   (name "binderhub")
   (version "0.1.0-d434a0d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-d434a0d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-d2a8262
  (package
   (name "binderhub")
   (version "0.1.0-d2a8262")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-d2a8262.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-d02a031
  (package
   (name "binderhub")
   (version "0.1.0-d02a031")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-d02a031.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-ce79685
  (package
   (name "binderhub")
   (version "0.1.0-ce79685")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-ce79685.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-cd4f6de
  (package
   (name "binderhub")
   (version "0.1.0-cd4f6de")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-cd4f6de.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-cc509fa
  (package
   (name "binderhub")
   (version "0.1.0-cc509fa")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-cc509fa.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-caada72
  (package
   (name "binderhub")
   (version "0.1.0-caada72")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-caada72.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-c79a4fc
  (package
   (name "binderhub")
   (version "0.1.0-c79a4fc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-c79a4fc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-c6670a4
  (package
   (name "binderhub")
   (version "0.1.0-c6670a4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-c6670a4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-c5bf0e2
  (package
   (name "binderhub")
   (version "0.1.0-c5bf0e2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-c5bf0e2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-c3df45c
  (package
   (name "binderhub")
   (version "0.1.0-c3df45c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-c3df45c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-c2fc607
  (package
   (name "binderhub")
   (version "0.1.0-c2fc607")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-c2fc607.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-c146785
  (package
   (name "binderhub")
   (version "0.1.0-c146785")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-c146785.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-c0a1dae
  (package
   (name "binderhub")
   (version "0.1.0-c0a1dae")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-c0a1dae.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-be00d27
  (package
   (name "binderhub")
   (version "0.1.0-be00d27")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-be00d27.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-bda3323
  (package
   (name "binderhub")
   (version "0.1.0-bda3323")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-bda3323.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-bb4029a
  (package
   (name "binderhub")
   (version "0.1.0-bb4029a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-bb4029a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-b7e6711
  (package
   (name "binderhub")
   (version "0.1.0-b7e6711")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-b7e6711.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-b502c95
  (package
   (name "binderhub")
   (version "0.1.0-b502c95")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-b502c95.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-b5000ad
  (package
   (name "binderhub")
   (version "0.1.0-b5000ad")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-b5000ad.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-b0ac9a8
  (package
   (name "binderhub")
   (version "0.1.0-b0ac9a8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-b0ac9a8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-af0d09e
  (package
   (name "binderhub")
   (version "0.1.0-af0d09e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-af0d09e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-ae4195f
  (package
   (name "binderhub")
   (version "0.1.0-ae4195f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-ae4195f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-ad582a5
  (package
   (name "binderhub")
   (version "0.1.0-ad582a5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-ad582a5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-abe395e
  (package
   (name "binderhub")
   (version "0.1.0-abe395e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-abe395e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a97aec7
  (package
   (name "binderhub")
   (version "0.1.0-a97aec7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a97aec7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a9608f6
  (package
   (name "binderhub")
   (version "0.1.0-a9608f6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a9608f6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a8559e8
  (package
   (name "binderhub")
   (version "0.1.0-a8559e8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a8559e8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a6b64f6
  (package
   (name "binderhub")
   (version "0.1.0-a6b64f6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a6b64f6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a66ae0d
  (package
   (name "binderhub")
   (version "0.1.0-a66ae0d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a66ae0d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a6235f8
  (package
   (name "binderhub")
   (version "0.1.0-a6235f8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a6235f8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a6144d1
  (package
   (name "binderhub")
   (version "0.1.0-a6144d1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a6144d1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a5d1851
  (package
   (name "binderhub")
   (version "0.1.0-a5d1851")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a5d1851.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a413de2
  (package
   (name "binderhub")
   (version "0.1.0-a413de2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a413de2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a36cd83
  (package
   (name "binderhub")
   (version "0.1.0-a36cd83")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a36cd83.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-a072a12
  (package
   (name "binderhub")
   (version "0.1.0-a072a12")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-a072a12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-9f0f622
  (package
   (name "binderhub")
   (version "0.1.0-9f0f622")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-9f0f622.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-9e95e99
  (package
   (name "binderhub")
   (version "0.1.0-9e95e99")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-9e95e99.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-9e509fa
  (package
   (name "binderhub")
   (version "0.1.0-9e509fa")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-9e509fa.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-9b56017
  (package
   (name "binderhub")
   (version "0.1.0-9b56017")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-9b56017.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-9abd8bf
  (package
   (name "binderhub")
   (version "0.1.0-9abd8bf")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-9abd8bf.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-98ec94e
  (package
   (name "binderhub")
   (version "0.1.0-98ec94e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-98ec94e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-96b907f
  (package
   (name "binderhub")
   (version "0.1.0-96b907f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-96b907f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-9623b55
  (package
   (name "binderhub")
   (version "0.1.0-9623b55")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-9623b55.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-94f5f3e
  (package
   (name "binderhub")
   (version "0.1.0-94f5f3e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-94f5f3e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-9494d48
  (package
   (name "binderhub")
   (version "0.1.0-9494d48")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-9494d48.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-94116a1
  (package
   (name "binderhub")
   (version "0.1.0-94116a1")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-94116a1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-8f7b71c
  (package
   (name "binderhub")
   (version "0.1.0-8f7b71c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-8f7b71c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-8ebaf89
  (package
   (name "binderhub")
   (version "0.1.0-8ebaf89")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-8ebaf89.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-8e9376c
  (package
   (name "binderhub")
   (version "0.1.0-8e9376c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-8e9376c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-8dd7595
  (package
   (name "binderhub")
   (version "0.1.0-8dd7595")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-8dd7595.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-8dc11b8
  (package
   (name "binderhub")
   (version "0.1.0-8dc11b8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-8dc11b8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-8d171f0
  (package
   (name "binderhub")
   (version "0.1.0-8d171f0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-8d171f0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-88fd96d
  (package
   (name "binderhub")
   (version "0.1.0-88fd96d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-88fd96d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-88ebd43
  (package
   (name "binderhub")
   (version "0.1.0-88ebd43")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-88ebd43.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-88d62ca
  (package
   (name "binderhub")
   (version "0.1.0-88d62ca")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-88d62ca.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-87e3a43
  (package
   (name "binderhub")
   (version "0.1.0-87e3a43")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-87e3a43.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-87025d4
  (package
   (name "binderhub")
   (version "0.1.0-87025d4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-87025d4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-85ac189
  (package
   (name "binderhub")
   (version "0.1.0-85ac189")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-85ac189.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-85a0372
  (package
   (name "binderhub")
   (version "0.1.0-85a0372")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-85a0372.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-856e3e6
  (package
   (name "binderhub")
   (version "0.1.0-856e3e6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-856e3e6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-84b394c
  (package
   (name "binderhub")
   (version "0.1.0-84b394c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-84b394c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-81f9e33
  (package
   (name "binderhub")
   (version "0.1.0-81f9e33")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-81f9e33.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-811406b
  (package
   (name "binderhub")
   (version "0.1.0-811406b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-811406b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-8047c6e
  (package
   (name "binderhub")
   (version "0.1.0-8047c6e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-8047c6e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-7d008a3
  (package
   (name "binderhub")
   (version "0.1.0-7d008a3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-7d008a3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-7cc02b0
  (package
   (name "binderhub")
   (version "0.1.0-7cc02b0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-7cc02b0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-7ca33f9
  (package
   (name "binderhub")
   (version "0.1.0-7ca33f9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-7ca33f9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-7a2b134
  (package
   (name "binderhub")
   (version "0.1.0-7a2b134")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-7a2b134.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-7a22080
  (package
   (name "binderhub")
   (version "0.1.0-7a22080")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-7a22080.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-79a9de8
  (package
   (name "binderhub")
   (version "0.1.0-79a9de8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-79a9de8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-795801e
  (package
   (name "binderhub")
   (version "0.1.0-795801e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-795801e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-789e30a
  (package
   (name "binderhub")
   (version "0.1.0-789e30a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-789e30a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-78972e6
  (package
   (name "binderhub")
   (version "0.1.0-78972e6")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-78972e6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-75c0c6e
  (package
   (name "binderhub")
   (version "0.1.0-75c0c6e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-75c0c6e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-755c498
  (package
   (name "binderhub")
   (version "0.1.0-755c498")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-755c498.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-74ec877
  (package
   (name "binderhub")
   (version "0.1.0-74ec877")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-74ec877.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-74d98ed
  (package
   (name "binderhub")
   (version "0.1.0-74d98ed")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-74d98ed.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-748c2f4
  (package
   (name "binderhub")
   (version "0.1.0-748c2f4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-748c2f4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-73b001e
  (package
   (name "binderhub")
   (version "0.1.0-73b001e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-73b001e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-730f4a0
  (package
   (name "binderhub")
   (version "0.1.0-730f4a0")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-730f4a0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-72c2c36
  (package
   (name "binderhub")
   (version "0.1.0-72c2c36")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-72c2c36.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-71e690d
  (package
   (name "binderhub")
   (version "0.1.0-71e690d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-71e690d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-7132d45
  (package
   (name "binderhub")
   (version "0.1.0-7132d45")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-7132d45.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-710c4cb
  (package
   (name "binderhub")
   (version "0.1.0-710c4cb")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-710c4cb.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-70d53fe
  (package
   (name "binderhub")
   (version "0.1.0-70d53fe")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-70d53fe.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-6f57b2a
  (package
   (name "binderhub")
   (version "0.1.0-6f57b2a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-6f57b2a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-6e91396
  (package
   (name "binderhub")
   (version "0.1.0-6e91396")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-6e91396.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-6e8f9dc
  (package
   (name "binderhub")
   (version "0.1.0-6e8f9dc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-6e8f9dc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-6a7826f
  (package
   (name "binderhub")
   (version "0.1.0-6a7826f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-6a7826f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-691c2bc
  (package
   (name "binderhub")
   (version "0.1.0-691c2bc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-691c2bc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-6910d38
  (package
   (name "binderhub")
   (version "0.1.0-6910d38")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-6910d38.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-6837cc7
  (package
   (name "binderhub")
   (version "0.1.0-6837cc7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-6837cc7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-65e767a
  (package
   (name "binderhub")
   (version "0.1.0-65e767a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-65e767a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-6333d53
  (package
   (name "binderhub")
   (version "0.1.0-6333d53")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-6333d53.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-62e9e19
  (package
   (name "binderhub")
   (version "0.1.0-62e9e19")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-62e9e19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-62bb376
  (package
   (name "binderhub")
   (version "0.1.0-62bb376")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-62bb376.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-628f741
  (package
   (name "binderhub")
   (version "0.1.0-628f741")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-628f741.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-5f82a56
  (package
   (name "binderhub")
   (version "0.1.0-5f82a56")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-5f82a56.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-5f52e41
  (package
   (name "binderhub")
   (version "0.1.0-5f52e41")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-5f52e41.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-5d72e9f
  (package
   (name "binderhub")
   (version "0.1.0-5d72e9f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-5d72e9f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-5cdfc38
  (package
   (name "binderhub")
   (version "0.1.0-5cdfc38")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-5cdfc38.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-5b7685a
  (package
   (name "binderhub")
   (version "0.1.0-5b7685a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-5b7685a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-587e8a9
  (package
   (name "binderhub")
   (version "0.1.0-587e8a9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-587e8a9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-5877a09
  (package
   (name "binderhub")
   (version "0.1.0-5877a09")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-5877a09.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-587536f
  (package
   (name "binderhub")
   (version "0.1.0-587536f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-587536f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-576e5fa
  (package
   (name "binderhub")
   (version "0.1.0-576e5fa")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-576e5fa.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-545ceee
  (package
   (name "binderhub")
   (version "0.1.0-545ceee")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-545ceee.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-534681b
  (package
   (name "binderhub")
   (version "0.1.0-534681b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-534681b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-52d9ac3
  (package
   (name "binderhub")
   (version "0.1.0-52d9ac3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-52d9ac3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-517ab7c
  (package
   (name "binderhub")
   (version "0.1.0-517ab7c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-517ab7c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-513ba4e
  (package
   (name "binderhub")
   (version "0.1.0-513ba4e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-513ba4e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-512fbac
  (package
   (name "binderhub")
   (version "0.1.0-512fbac")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-512fbac.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-511cd5e
  (package
   (name "binderhub")
   (version "0.1.0-511cd5e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-511cd5e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-5085efc
  (package
   (name "binderhub")
   (version "0.1.0-5085efc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-5085efc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-502ffb8
  (package
   (name "binderhub")
   (version "0.1.0-502ffb8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-502ffb8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-4fef405
  (package
   (name "binderhub")
   (version "0.1.0-4fef405")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-4fef405.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-4b03e3a
  (package
   (name "binderhub")
   (version "0.1.0-4b03e3a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-4b03e3a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-47ed8cd
  (package
   (name "binderhub")
   (version "0.1.0-47ed8cd")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-47ed8cd.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-47830c5
  (package
   (name "binderhub")
   (version "0.1.0-47830c5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-47830c5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-470642d
  (package
   (name "binderhub")
   (version "0.1.0-470642d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-470642d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-469fe5b
  (package
   (name "binderhub")
   (version "0.1.0-469fe5b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-469fe5b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-4670b66
  (package
   (name "binderhub")
   (version "0.1.0-4670b66")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-4670b66.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-464c4f5
  (package
   (name "binderhub")
   (version "0.1.0-464c4f5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-464c4f5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-4648c74
  (package
   (name "binderhub")
   (version "0.1.0-4648c74")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-4648c74.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-4635c23
  (package
   (name "binderhub")
   (version "0.1.0-4635c23")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-4635c23.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-45942f3
  (package
   (name "binderhub")
   (version "0.1.0-45942f3")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-45942f3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-4461af2
  (package
   (name "binderhub")
   (version "0.1.0-4461af2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-4461af2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-441f5ea
  (package
   (name "binderhub")
   (version "0.1.0-441f5ea")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-441f5ea.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-410bd57
  (package
   (name "binderhub")
   (version "0.1.0-410bd57")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-410bd57.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-4075ab8
  (package
   (name "binderhub")
   (version "0.1.0-4075ab8")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-4075ab8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-401c3f9
  (package
   (name "binderhub")
   (version "0.1.0-401c3f9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-401c3f9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-3fbbd73
  (package
   (name "binderhub")
   (version "0.1.0-3fbbd73")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-3fbbd73.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-3f81760
  (package
   (name "binderhub")
   (version "0.1.0-3f81760")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-3f81760.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-3e511fd
  (package
   (name "binderhub")
   (version "0.1.0-3e511fd")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-3e511fd.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-3df2b75
  (package
   (name "binderhub")
   (version "0.1.0-3df2b75")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-3df2b75.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-3c21fde
  (package
   (name "binderhub")
   (version "0.1.0-3c21fde")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-3c21fde.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-3b1009b
  (package
   (name "binderhub")
   (version "0.1.0-3b1009b")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-3b1009b.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-3a1ab89
  (package
   (name "binderhub")
   (version "0.1.0-3a1ab89")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-3a1ab89.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-397eb59
  (package
   (name "binderhub")
   (version "0.1.0-397eb59")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-397eb59.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-38ba9cb
  (package
   (name "binderhub")
   (version "0.1.0-38ba9cb")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-38ba9cb.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-35c53ce
  (package
   (name "binderhub")
   (version "0.1.0-35c53ce")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-35c53ce.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-331ab96
  (package
   (name "binderhub")
   (version "0.1.0-331ab96")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-331ab96.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-316895a
  (package
   (name "binderhub")
   (version "0.1.0-316895a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-316895a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-2f56b77
  (package
   (name "binderhub")
   (version "0.1.0-2f56b77")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-2f56b77.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-2ea22d9
  (package
   (name "binderhub")
   (version "0.1.0-2ea22d9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-2ea22d9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-2dfa9b5
  (package
   (name "binderhub")
   (version "0.1.0-2dfa9b5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-2dfa9b5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-2c2b48f
  (package
   (name "binderhub")
   (version "0.1.0-2c2b48f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-2c2b48f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-2a7b0aa
  (package
   (name "binderhub")
   (version "0.1.0-2a7b0aa")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-2a7b0aa.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-2986e56
  (package
   (name "binderhub")
   (version "0.1.0-2986e56")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-2986e56.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-297d09d
  (package
   (name "binderhub")
   (version "0.1.0-297d09d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-297d09d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-2801aaf
  (package
   (name "binderhub")
   (version "0.1.0-2801aaf")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-2801aaf.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-268b527
  (package
   (name "binderhub")
   (version "0.1.0-268b527")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-268b527.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-24ad99a
  (package
   (name "binderhub")
   (version "0.1.0-24ad99a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-24ad99a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-230bbde
  (package
   (name "binderhub")
   (version "0.1.0-230bbde")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-230bbde.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-22fcbc5
  (package
   (name "binderhub")
   (version "0.1.0-22fcbc5")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-22fcbc5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-2299f73
  (package
   (name "binderhub")
   (version "0.1.0-2299f73")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-2299f73.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-1e3dd8e
  (package
   (name "binderhub")
   (version "0.1.0-1e3dd8e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-1e3dd8e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-1cc96ee
  (package
   (name "binderhub")
   (version "0.1.0-1cc96ee")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-1cc96ee.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-1c25da7
  (package
   (name "binderhub")
   (version "0.1.0-1c25da7")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-1c25da7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-1be5b6a
  (package
   (name "binderhub")
   (version "0.1.0-1be5b6a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-1be5b6a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-1bc8b4c
  (package
   (name "binderhub")
   (version "0.1.0-1bc8b4c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-1bc8b4c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-19d22bb
  (package
   (name "binderhub")
   (version "0.1.0-19d22bb")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-19d22bb.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-18afb2c
  (package
   (name "binderhub")
   (version "0.1.0-18afb2c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-18afb2c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-183addc
  (package
   (name "binderhub")
   (version "0.1.0-183addc")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-183addc.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-174093c
  (package
   (name "binderhub")
   (version "0.1.0-174093c")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-174093c.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-165bdc4
  (package
   (name "binderhub")
   (version "0.1.0-165bdc4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-165bdc4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-13b65e2
  (package
   (name "binderhub")
   (version "0.1.0-13b65e2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-13b65e2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-127dd5e
  (package
   (name "binderhub")
   (version "0.1.0-127dd5e")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-127dd5e.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-10e99d9
  (package
   (name "binderhub")
   (version "0.1.0-10e99d9")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-10e99d9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-108c668
  (package
   (name "binderhub")
   (version "0.1.0-108c668")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-108c668.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-100220a
  (package
   (name "binderhub")
   (version "0.1.0-100220a")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-100220a.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-0deae84
  (package
   (name "binderhub")
   (version "0.1.0-0deae84")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-0deae84.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-0d4acbd
  (package
   (name "binderhub")
   (version "0.1.0-0d4acbd")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-0d4acbd.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-0bb083d
  (package
   (name "binderhub")
   (version "0.1.0-0bb083d")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-0bb083d.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-0b6c4f4
  (package
   (name "binderhub")
   (version "0.1.0-0b6c4f4")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-0b6c4f4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-0b19c22
  (package
   (name "binderhub")
   (version "0.1.0-0b19c22")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-0b19c22.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-09c10eb
  (package
   (name "binderhub")
   (version "0.1.0-09c10eb")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-09c10eb.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-044c8c2
  (package
   (name "binderhub")
   (version "0.1.0-044c8c2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-044c8c2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-04022d2
  (package
   (name "binderhub")
   (version "0.1.0-04022d2")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-04022d2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-0144a36
  (package
   (name "binderhub")
   (version "0.1.0-0144a36")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-0144a36.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-010de6f
  (package
   (name "binderhub")
   (version "0.1.0-010de6f")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-010de6f.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-9692255
  (package
   (name "binderhub")
   (version "0.1.0-9692255")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-9692255.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-7473366
  (package
   (name "binderhub")
   (version "0.1.0-7473366")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-7473366.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-5737187
  (package
   (name "binderhub")
   (version "0.1.0-5737187")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-5737187.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-5166091
  (package
   (name "binderhub")
   (version "0.1.0-5166091")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-5166091.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))

(define-public binderhub-0.1.0-1975451
  (package
   (name "binderhub")
   (version "0.1.0-1975451")
   (source (origin
            (method url-fetch)
            (uri "https://jupyterhub.github.io/helm-chart/binderhub-0.1.0-1975451.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart to install Binder")
   (description "A helm chart to install Binder")
   (license #f)))