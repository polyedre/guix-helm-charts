
(define-module (helm k8s-image-availability-exporter k8s-image-availability-exporter)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public k8s-image-availability-exporter-0.13.0
  (package
   (name "k8s-image-availability-exporter")
   (version "0.13.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/deckhouse/k8s-image-availability-exporter/releases/download/k8s-image-availability-exporter-0.13.0/k8s-image-availability-exporter-0.13.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Application for monitoring the cluster workloads image presence in a container registry.")
   (description "Application for monitoring the cluster workloads image presence in a container registry.")
   (license #f)))

(define-public k8s-image-availability-exporter-0.12.0
  (package
   (name "k8s-image-availability-exporter")
   (version "0.12.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/deckhouse/k8s-image-availability-exporter/releases/download/k8s-image-availability-exporter-0.12.0/k8s-image-availability-exporter-0.12.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Application for monitoring the cluster workloads image presence in a container registry.")
   (description "Application for monitoring the cluster workloads image presence in a container registry.")
   (license #f)))

(define-public k8s-image-availability-exporter-0.11.0
  (package
   (name "k8s-image-availability-exporter")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/deckhouse/k8s-image-availability-exporter/releases/download/k8s-image-availability-exporter-0.11.0/k8s-image-availability-exporter-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Application for monitoring the cluster workloads image presence in a container registry.")
   (description "Application for monitoring the cluster workloads image presence in a container registry.")
   (license #f)))

(define-public k8s-image-availability-exporter-0.10.1
  (package
   (name "k8s-image-availability-exporter")
   (version "0.10.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/deckhouse/k8s-image-availability-exporter/releases/download/k8s-image-availability-exporter-0.10.1/k8s-image-availability-exporter-0.10.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Application for monitoring the cluster workloads image presence in a container registry.")
   (description "Application for monitoring the cluster workloads image presence in a container registry.")
   (license #f)))

(define-public k8s-image-availability-exporter-0.10.0
  (package
   (name "k8s-image-availability-exporter")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/deckhouse/k8s-image-availability-exporter/releases/download/k8s-image-availability-exporter-0.10.0/k8s-image-availability-exporter-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Application for monitoring the cluster workloads image presence in a container registry.")
   (description "Application for monitoring the cluster workloads image presence in a container registry.")
   (license #f)))

(define-public k8s-image-availability-exporter-0.9.2
  (package
   (name "k8s-image-availability-exporter")
   (version "0.9.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/deckhouse/k8s-image-availability-exporter/releases/download/k8s-image-availability-exporter-0.9.2/k8s-image-availability-exporter-0.9.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Application for monitoring the cluster workloads image presence in a container registry.")
   (description "Application for monitoring the cluster workloads image presence in a container registry.")
   (license #f)))

(define-public k8s-image-availability-exporter-0.9.1
  (package
   (name "k8s-image-availability-exporter")
   (version "0.9.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/deckhouse/k8s-image-availability-exporter/releases/download/k8s-image-availability-exporter-0.9.1/k8s-image-availability-exporter-0.9.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Application for monitoring the cluster workloads image presence in a container registry.")
   (description "Application for monitoring the cluster workloads image presence in a container registry.")
   (license #f)))

(define-public k8s-image-availability-exporter-0.9.0
  (package
   (name "k8s-image-availability-exporter")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/deckhouse/k8s-image-availability-exporter/releases/download/k8s-image-availability-exporter-0.9.0/k8s-image-availability-exporter-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Application for monitoring the cluster workloads image presence in a container registry.")
   (description "Application for monitoring the cluster workloads image presence in a container registry.")
   (license #f)))