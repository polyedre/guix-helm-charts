
(define-module (helm kangal kangal)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public kangal-2.3.0
  (package
   (name "kangal")
   (version "2.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.3.0/kangal-2.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.2.6
  (package
   (name "kangal")
   (version "2.2.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.2.6/kangal-2.2.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.2.5
  (package
   (name "kangal")
   (version "2.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.2.5/kangal-2.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.2.4
  (package
   (name "kangal")
   (version "2.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.2.4/kangal-2.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.2.3
  (package
   (name "kangal")
   (version "2.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.2.3/kangal-2.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.2.2
  (package
   (name "kangal")
   (version "2.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.2.2/kangal-2.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.2.1
  (package
   (name "kangal")
   (version "2.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.2.1/kangal-2.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.2.0
  (package
   (name "kangal")
   (version "2.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.2.0/kangal-2.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.1.0
  (package
   (name "kangal")
   (version "2.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.1.0/kangal-2.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.10
  (package
   (name "kangal")
   (version "2.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.10/kangal-2.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.9
  (package
   (name "kangal")
   (version "2.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.9/kangal-2.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.8
  (package
   (name "kangal")
   (version "2.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.8/kangal-2.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.7
  (package
   (name "kangal")
   (version "2.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.7/kangal-2.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.6
  (package
   (name "kangal")
   (version "2.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.6/kangal-2.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.5
  (package
   (name "kangal")
   (version "2.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.5/kangal-2.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.4
  (package
   (name "kangal")
   (version "2.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.4/kangal-2.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.3
  (package
   (name "kangal")
   (version "2.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.3/kangal-2.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.2
  (package
   (name "kangal")
   (version "2.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.2/kangal-2.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.1
  (package
   (name "kangal")
   (version "2.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.1/kangal-2.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-2.0.0
  (package
   (name "kangal")
   (version "2.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-2.0.0/kangal-2.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.10
  (package
   (name "kangal")
   (version "1.4.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.10/kangal-1.4.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.9
  (package
   (name "kangal")
   (version "1.4.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.9/kangal-1.4.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.8
  (package
   (name "kangal")
   (version "1.4.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.8/kangal-1.4.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.7
  (package
   (name "kangal")
   (version "1.4.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.7/kangal-1.4.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.6
  (package
   (name "kangal")
   (version "1.4.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.6/kangal-1.4.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.5
  (package
   (name "kangal")
   (version "1.4.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.5/kangal-1.4.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.4
  (package
   (name "kangal")
   (version "1.4.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.4/kangal-1.4.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.3
  (package
   (name "kangal")
   (version "1.4.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.3/kangal-1.4.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.2
  (package
   (name "kangal")
   (version "1.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.2/kangal-1.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.1
  (package
   (name "kangal")
   (version "1.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.1/kangal-1.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.4.0
  (package
   (name "kangal")
   (version "1.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.4.0/kangal-1.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.3.0
  (package
   (name "kangal")
   (version "1.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.3.0/kangal-1.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.2.2
  (package
   (name "kangal")
   (version "1.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.2.2/kangal-1.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.2.1
  (package
   (name "kangal")
   (version "1.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.2.1/kangal-1.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.2.0
  (package
   (name "kangal")
   (version "1.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.2.0/kangal-1.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.1.0
  (package
   (name "kangal")
   (version "1.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.1.0/kangal-1.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.0.5
  (package
   (name "kangal")
   (version "1.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.0.5/kangal-1.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.0.4
  (package
   (name "kangal")
   (version "1.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.0.4/kangal-1.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.0.3
  (package
   (name "kangal")
   (version "1.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.0.3/kangal-1.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.0.2
  (package
   (name "kangal")
   (version "1.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.0.2/kangal-1.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))

(define-public kangal-1.0.1
  (package
   (name "kangal")
   (version "1.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/hellofresh/kangal/releases/download/kangal-1.0.1/kangal-1.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/hellofresh/kangal")
   (synopsis "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (description "Run JMeter performance tests in Kubernetes cluster with Kangal")
   (license #f)))