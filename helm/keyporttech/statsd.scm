
(define-module (helm keyporttech statsd)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public statsd-0.1.19
  (package
   (name "statsd")
   (version "0.1.19")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.19/statsd-0.1.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.18
  (package
   (name "statsd")
   (version "0.1.18")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.18/statsd-0.1.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.16
  (package
   (name "statsd")
   (version "0.1.16")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.16/statsd-0.1.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.14
  (package
   (name "statsd")
   (version "0.1.14")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.14/statsd-0.1.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.13
  (package
   (name "statsd")
   (version "0.1.13")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.13/statsd-0.1.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.12
  (package
   (name "statsd")
   (version "0.1.12")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.12/statsd-0.1.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.11
  (package
   (name "statsd")
   (version "0.1.11")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.11/statsd-0.1.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.10
  (package
   (name "statsd")
   (version "0.1.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.10/statsd-0.1.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.9
  (package
   (name "statsd")
   (version "0.1.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.9/statsd-0.1.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.8
  (package
   (name "statsd")
   (version "0.1.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.8/statsd-0.1.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.7
  (package
   (name "statsd")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.7/statsd-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.6
  (package
   (name "statsd")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.6/statsd-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.5
  (package
   (name "statsd")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.5/statsd-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.4
  (package
   (name "statsd")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.4/statsd-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.3
  (package
   (name "statsd")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.3/statsd-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.2
  (package
   (name "statsd")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.2/statsd-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))

(define-public statsd-0.1.0
  (package
   (name "statsd")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/keyporttech/helm-charts/releases/download/statsd-0.1.0/statsd-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/keyporttech/helm-statsd")
   (synopsis "a simple chart to deploy statsd to a cluster")
   (description "a simple chart to deploy statsd to a cluster")
   (license #f)))