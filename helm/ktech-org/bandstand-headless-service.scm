
(define-module (helm ktech-org bandstand-headless-service)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public bandstand-headless-service-3.1.0
  (package
   (name "bandstand-headless-service")
   (version "3.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-3.1.0/bandstand-headless-service-3.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-3.0.3
  (package
   (name "bandstand-headless-service")
   (version "3.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-3.0.3/bandstand-headless-service-3.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-3.0.2
  (package
   (name "bandstand-headless-service")
   (version "3.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-3.0.2/bandstand-headless-service-3.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-3.0.1
  (package
   (name "bandstand-headless-service")
   (version "3.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-3.0.1/bandstand-headless-service-3.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-3.0.0
  (package
   (name "bandstand-headless-service")
   (version "3.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-3.0.0/bandstand-headless-service-3.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-2.5.0
  (package
   (name "bandstand-headless-service")
   (version "2.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-2.5.0/bandstand-headless-service-2.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-2.4.0
  (package
   (name "bandstand-headless-service")
   (version "2.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-2.4.0/bandstand-headless-service-2.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-2.3.2
  (package
   (name "bandstand-headless-service")
   (version "2.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-2.3.2/bandstand-headless-service-2.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-2.3.1
  (package
   (name "bandstand-headless-service")
   (version "2.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-2.3.1/bandstand-headless-service-2.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-2.3.0
  (package
   (name "bandstand-headless-service")
   (version "2.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-2.3.0/bandstand-headless-service-2.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-2.2.0
  (package
   (name "bandstand-headless-service")
   (version "2.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-2.2.0/bandstand-headless-service-2.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-2.1.1
  (package
   (name "bandstand-headless-service")
   (version "2.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-2.1.1/bandstand-headless-service-2.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-2.1.0
  (package
   (name "bandstand-headless-service")
   (version "2.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-2.1.0/bandstand-headless-service-2.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-2.0.0
  (package
   (name "bandstand-headless-service")
   (version "2.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-2.0.0/bandstand-headless-service-2.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))

(define-public bandstand-headless-service-1.0.0
  (package
   (name "bandstand-headless-service")
   (version "1.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/ktech-org/bandstand-charts/releases/download/bandstand-headless-service-1.0.0/bandstand-headless-service-1.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Chart for a standalone (non-web) service")
   (description "Chart for a standalone (non-web) service")
   (license #f)))