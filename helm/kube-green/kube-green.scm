
(define-module (helm kube-green kube-green)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public kube-green-0.0.13
  (package
   (name "kube-green")
   (version "0.0.13")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))

(define-public kube-green-0.0.12
  (package
   (name "kube-green")
   (version "0.0.12")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))

(define-public kube-green-0.0.11
  (package
   (name "kube-green")
   (version "0.0.11")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))

(define-public kube-green-0.0.10
  (package
   (name "kube-green")
   (version "0.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))

(define-public kube-green-0.0.9
  (package
   (name "kube-green")
   (version "0.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))

(define-public kube-green-0.0.8
  (package
   (name "kube-green")
   (version "0.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))

(define-public kube-green-0.0.7
  (package
   (name "kube-green")
   (version "0.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))

(define-public kube-green-0.0.6
  (package
   (name "kube-green")
   (version "0.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))

(define-public kube-green-0.0.4
  (package
   (name "kube-green")
   (version "0.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))

(define-public kube-green-0.0.3
  (package
   (name "kube-green")
   (version "0.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))

(define-public kube-green-0.0.1
  (package
   (name "kube-green")
   (version "0.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://kube-green.additi.fr/kube-green-0.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://gitlab.com/additi/external/kube-green")
   (synopsis "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (description "kube-green is a simple k8s addon that automatically shuts down (some of) your resources when you don't need them.")
   (license #f)))