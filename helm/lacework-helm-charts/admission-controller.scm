
(define-module (helm lacework-helm-charts admission-controller)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public admission-controller-0.23.2
  (package
   (name "admission-controller")
   (version "0.23.2")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.23.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.23.1
  (package
   (name "admission-controller")
   (version "0.23.1")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.23.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.23.0
  (package
   (name "admission-controller")
   (version "0.23.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.23.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.21.1
  (package
   (name "admission-controller")
   (version "0.21.1")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.21.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.21.0
  (package
   (name "admission-controller")
   (version "0.21.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.21.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.20.1
  (package
   (name "admission-controller")
   (version "0.20.1")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.20.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.20.0
  (package
   (name "admission-controller")
   (version "0.20.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.20.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.19.0
  (package
   (name "admission-controller")
   (version "0.19.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.19.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.18.1
  (package
   (name "admission-controller")
   (version "0.18.1")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.18.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.18.0
  (package
   (name "admission-controller")
   (version "0.18.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.18.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.17.0
  (package
   (name "admission-controller")
   (version "0.17.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.17.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.16.0
  (package
   (name "admission-controller")
   (version "0.16.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.16.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.15.1
  (package
   (name "admission-controller")
   (version "0.15.1")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.15.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.15.0
  (package
   (name "admission-controller")
   (version "0.15.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.15.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.14.2
  (package
   (name "admission-controller")
   (version "0.14.2")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.14.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.14.1
  (package
   (name "admission-controller")
   (version "0.14.1")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.14.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.14.0
  (package
   (name "admission-controller")
   (version "0.14.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.14.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.13.0
  (package
   (name "admission-controller")
   (version "0.13.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.13.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.12.1
  (package
   (name "admission-controller")
   (version "0.12.1")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.12.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.12.0
  (package
   (name "admission-controller")
   (version "0.12.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.12.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.11.4
  (package
   (name "admission-controller")
   (version "0.11.4")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.11.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.11.3
  (package
   (name "admission-controller")
   (version "0.11.3")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.11.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.11.2
  (package
   (name "admission-controller")
   (version "0.11.2")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.11.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.11.1
  (package
   (name "admission-controller")
   (version "0.11.1")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.11.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.11.0
  (package
   (name "admission-controller")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.14
  (package
   (name "admission-controller")
   (version "0.1.14")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.13
  (package
   (name "admission-controller")
   (version "0.1.13")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.12
  (package
   (name "admission-controller")
   (version "0.1.12")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.11
  (package
   (name "admission-controller")
   (version "0.1.11")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.10
  (package
   (name "admission-controller")
   (version "0.1.10")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.9
  (package
   (name "admission-controller")
   (version "0.1.9")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.8
  (package
   (name "admission-controller")
   (version "0.1.8")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.7
  (package
   (name "admission-controller")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.6
  (package
   (name "admission-controller")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.5
  (package
   (name "admission-controller")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.4
  (package
   (name "admission-controller")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.3
  (package
   (name "admission-controller")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.2
  (package
   (name "admission-controller")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.1
  (package
   (name "admission-controller")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))

(define-public admission-controller-0.1.0
  (package
   (name "admission-controller")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://lacework.github.io/helm-charts//admission-controller-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lacework admission controller using Lacework proxy scanner")
   (description "Lacework admission controller using Lacework proxy scanner")
   (license #f)))