
(define-module (helm livekit-server livekit-recorder)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public livekit-recorder-0.3.13
  (package
   (name "livekit-recorder")
   (version "0.3.13")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.3.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.3.12
  (package
   (name "livekit-recorder")
   (version "0.3.12")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.3.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.3.10
  (package
   (name "livekit-recorder")
   (version "0.3.10")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.3.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.3.7
  (package
   (name "livekit-recorder")
   (version "0.3.7")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.3.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.3.5
  (package
   (name "livekit-recorder")
   (version "0.3.5")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.3.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.3.4
  (package
   (name "livekit-recorder")
   (version "0.3.4")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.3.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.3.3
  (package
   (name "livekit-recorder")
   (version "0.3.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.3.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.3.1
  (package
   (name "livekit-recorder")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.3.0
  (package
   (name "livekit-recorder")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.2.3
  (package
   (name "livekit-recorder")
   (version "0.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.2.2
  (package
   (name "livekit-recorder")
   (version "0.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.2.1
  (package
   (name "livekit-recorder")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.2.0
  (package
   (name "livekit-recorder")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.1.7
  (package
   (name "livekit-recorder")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))

(define-public livekit-recorder-0.1.6
  (package
   (name "livekit-recorder")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-recorder-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit recorder is used by LiveKit server to record rooms.")
   (description "LiveKit recorder is used by LiveKit server to record rooms.")
   (license #f)))