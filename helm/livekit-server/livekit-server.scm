
(define-module (helm livekit-server livekit-server)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public livekit-server-1.5.3
  (package
   (name "livekit-server")
   (version "1.5.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.5.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Real-time infrastructure for developers. LiveKit is the open source stack for streaming audio, video, and data.")
   (description "Real-time infrastructure for developers. LiveKit is the open source stack for streaming audio, video, and data.")
   (license #f)))

(define-public livekit-server-1.5.2
  (package
   (name "livekit-server")
   (version "1.5.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.5.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.5.1
  (package
   (name "livekit-server")
   (version "1.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.5.0
  (package
   (name "livekit-server")
   (version "1.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.4.5
  (package
   (name "livekit-server")
   (version "1.4.5")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.4.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.4.4
  (package
   (name "livekit-server")
   (version "1.4.4")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.4.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.4.3
  (package
   (name "livekit-server")
   (version "1.4.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.4.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.4.2
  (package
   (name "livekit-server")
   (version "1.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.4.1
  (package
   (name "livekit-server")
   (version "1.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.4.0
  (package
   (name "livekit-server")
   (version "1.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.3.5
  (package
   (name "livekit-server")
   (version "1.3.5")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.3.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.3.4
  (package
   (name "livekit-server")
   (version "1.3.4")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.3.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.3.3
  (package
   (name "livekit-server")
   (version "1.3.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.3.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.3.2
  (package
   (name "livekit-server")
   (version "1.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.3.1
  (package
   (name "livekit-server")
   (version "1.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.3.0
  (package
   (name "livekit-server")
   (version "1.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.2.5
  (package
   (name "livekit-server")
   (version "1.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.2.3
  (package
   (name "livekit-server")
   (version "1.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.2.1
  (package
   (name "livekit-server")
   (version "1.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.2.0
  (package
   (name "livekit-server")
   (version "1.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.1.3
  (package
   (name "livekit-server")
   (version "1.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.1.2
  (package
   (name "livekit-server")
   (version "1.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.1.0
  (package
   (name "livekit-server")
   (version "1.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-1.0.2
  (package
   (name "livekit-server")
   (version "1.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-1.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-0.15.7
  (package
   (name "livekit-server")
   (version "0.15.7")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.15.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-0.15.5
  (package
   (name "livekit-server")
   (version "0.15.5")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.15.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-0.15.4
  (package
   (name "livekit-server")
   (version "0.15.4")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.15.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-0.15.3
  (package
   (name "livekit-server")
   (version "0.15.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.15.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-0.15.2
  (package
   (name "livekit-server")
   (version "0.15.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.15.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-0.15.1
  (package
   (name "livekit-server")
   (version "0.15.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.15.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (description "Open source WebRTC infrastructure. Host your own real-time video/audio experiences.")
   (license #f)))

(define-public livekit-server-0.14.2
  (package
   (name "livekit-server")
   (version "0.14.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.14.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server.")
   (description "LiveKit is a distributed WebRTC server.")
   (license #f)))

(define-public livekit-server-0.14.0
  (package
   (name "livekit-server")
   (version "0.14.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.14.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server.")
   (description "LiveKit is a distributed WebRTC server.")
   (license #f)))

(define-public livekit-server-0.13.5
  (package
   (name "livekit-server")
   (version "0.13.5")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.13.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server.")
   (description "LiveKit is a distributed WebRTC server.")
   (license #f)))

(define-public livekit-server-0.13.4
  (package
   (name "livekit-server")
   (version "0.13.4")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.13.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server.")
   (description "LiveKit is a distributed WebRTC server.")
   (license #f)))

(define-public livekit-server-0.13.3
  (package
   (name "livekit-server")
   (version "0.13.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.13.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server.")
   (description "LiveKit is a distributed WebRTC server.")
   (license #f)))

(define-public livekit-server-0.13.2
  (package
   (name "livekit-server")
   (version "0.13.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.13.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server.")
   (description "LiveKit is a distributed WebRTC server.")
   (license #f)))

(define-public livekit-server-0.13.1
  (package
   (name "livekit-server")
   (version "0.13.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.13.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server.")
   (description "LiveKit is a distributed WebRTC server.")
   (license #f)))

(define-public livekit-server-0.13.0
  (package
   (name "livekit-server")
   (version "0.13.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.13.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server.")
   (description "LiveKit is a distributed WebRTC server.")
   (license #f)))

(define-public livekit-server-0.12.5
  (package
   (name "livekit-server")
   (version "0.12.5")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.12.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.12.3
  (package
   (name "livekit-server")
   (version "0.12.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.12.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.12.2
  (package
   (name "livekit-server")
   (version "0.12.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.12.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.12.1
  (package
   (name "livekit-server")
   (version "0.12.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.12.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.11.5
  (package
   (name "livekit-server")
   (version "0.11.5")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.11.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.11.4
  (package
   (name "livekit-server")
   (version "0.11.4")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.11.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.11.1
  (package
   (name "livekit-server")
   (version "0.11.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.11.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.11.0
  (package
   (name "livekit-server")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.10.6
  (package
   (name "livekit-server")
   (version "0.10.6")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.10.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.10.4
  (package
   (name "livekit-server")
   (version "0.10.4")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.10.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.10.3
  (package
   (name "livekit-server")
   (version "0.10.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.10.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.10.2
  (package
   (name "livekit-server")
   (version "0.10.2")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.10.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.10.1
  (package
   (name "livekit-server")
   (version "0.10.1")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.10.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.10.0
  (package
   (name "livekit-server")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.9.14
  (package
   (name "livekit-server")
   (version "0.9.14")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.9.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.9.13
  (package
   (name "livekit-server")
   (version "0.9.13")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.9.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.9.12
  (package
   (name "livekit-server")
   (version "0.9.12")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.9.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.9.11
  (package
   (name "livekit-server")
   (version "0.9.11")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/livekit-server-0.9.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.9.10
  (package
   (name "livekit-server")
   (version "0.9.10")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/s3://livekit-helm/livekit-server-0.9.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.9.9
  (package
   (name "livekit-server")
   (version "0.9.9")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/s3://livekit-helm/livekit-server-0.9.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.9.8
  (package
   (name "livekit-server")
   (version "0.9.8")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/s3://livekit-helm/livekit-server-0.9.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.9.7
  (package
   (name "livekit-server")
   (version "0.9.7")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/s3://livekit-helm/livekit-server-0.9.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.9.3
  (package
   (name "livekit-server")
   (version "0.9.3")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/s3://livekit-helm/livekit-server-0.9.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))

(define-public livekit-server-0.9.0
  (package
   (name "livekit-server")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://helm.livekit.io/s3://livekit-helm/livekit-server-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (description "LiveKit is a distributed WebRTC server. This chart runs livekit-server in distributed mode.")
   (license #f)))