
(define-module (helm lsst-sqre sasquatch)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public sasquatch-0.1.13
  (package
   (name "sasquatch")
   (version "0.1.13")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.13/sasquatch-0.1.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.12
  (package
   (name "sasquatch")
   (version "0.1.12")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.12/sasquatch-0.1.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.11
  (package
   (name "sasquatch")
   (version "0.1.11")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.11/sasquatch-0.1.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.10
  (package
   (name "sasquatch")
   (version "0.1.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.10/sasquatch-0.1.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.9
  (package
   (name "sasquatch")
   (version "0.1.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.9/sasquatch-0.1.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.8
  (package
   (name "sasquatch")
   (version "0.1.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.8/sasquatch-0.1.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.7
  (package
   (name "sasquatch")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.7/sasquatch-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.6
  (package
   (name "sasquatch")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.6/sasquatch-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.5
  (package
   (name "sasquatch")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.5/sasquatch-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.4
  (package
   (name "sasquatch")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.4/sasquatch-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.3
  (package
   (name "sasquatch")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.3/sasquatch-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.2
  (package
   (name "sasquatch")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.2/sasquatch-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))

(define-public sasquatch-0.1.0
  (package
   (name "sasquatch")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/lsst-sqre/charts/releases/download/sasquatch-0.1.0/sasquatch-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart to deploy Sasquatch components on Kubernetes")
   (description "A Helm chart to deploy Sasquatch components on Kubernetes")
   (license #f)))