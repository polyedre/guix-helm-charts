
(define-module (helm mitre-siphon mitre-siphon)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public mitre-siphon-0.2.9
  (package
   (name "mitre-siphon")
   (version "0.2.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.2.9/mitre-siphon-0.2.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))

(define-public mitre-siphon-0.2.8
  (package
   (name "mitre-siphon")
   (version "0.2.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.2.8/mitre-siphon-0.2.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))

(define-public mitre-siphon-0.2.7
  (package
   (name "mitre-siphon")
   (version "0.2.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.2.7/mitre-siphon-0.2.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))

(define-public mitre-siphon-0.2.6
  (package
   (name "mitre-siphon")
   (version "0.2.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.2.6/mitre-siphon-0.2.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))

(define-public mitre-siphon-0.2.5
  (package
   (name "mitre-siphon")
   (version "0.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.2.5/mitre-siphon-0.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))

(define-public mitre-siphon-0.2.4
  (package
   (name "mitre-siphon")
   (version "0.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.2.4/mitre-siphon-0.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))

(define-public mitre-siphon-0.2.3
  (package
   (name "mitre-siphon")
   (version "0.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.2.3/mitre-siphon-0.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))

(define-public mitre-siphon-0.2.2
  (package
   (name "mitre-siphon")
   (version "0.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.2.2/mitre-siphon-0.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))

(define-public mitre-siphon-0.2.1
  (package
   (name "mitre-siphon")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.2.1/mitre-siphon-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))

(define-public mitre-siphon-0.2.0
  (package
   (name "mitre-siphon")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.2.0/mitre-siphon-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))

(define-public mitre-siphon-0.1.0
  (package
   (name "mitre-siphon")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/curium-rocks/mitre-siphon/releases/download/mitre-siphon-0.1.0/mitre-siphon-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A full text search of the mitre CVE database")
   (description "A full text search of the mitre CVE database")
   (license #f)))