
(define-module (helm mya litestream)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public litestream-22.4.5
  (package
   (name "litestream")
   (version "22.4.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/litestream-22.4.5/litestream-22.4.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary init and sidecar containers. ")
   (description "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary init and sidecar containers. ")
   (license #f)))

(define-public litestream-22.4.4
  (package
   (name "litestream")
   (version "22.4.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/litestream-22.4.4/litestream-22.4.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary init and sidecar containers. ")
   (description "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary init and sidecar containers. ")
   (license #f)))

(define-public litestream-22.4.3
  (package
   (name "litestream")
   (version "22.4.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/litestream-22.4.3/litestream-22.4.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary init and sidecar containers. ")
   (description "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary init and sidecar containers. ")
   (license #f)))

(define-public litestream-22.4.2
  (package
   (name "litestream")
   (version "22.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/litestream-22.4.2/litestream-22.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary init and sidecar containers. ")
   (description "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary init and sidecar containers. ")
   (license #f)))

(define-public litestream-22.4.1
  (package
   (name "litestream")
   (version "22.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/litestream-22.4.1/litestream-22.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary init and sidecar containers. ")
   (description "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary init and sidecar containers. ")
   (license #f)))

(define-public litestream-22.4.0
  (package
   (name "litestream")
   (version "22.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/litestream-22.4.0/litestream-22.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required  configuration and provides template functions that render the necessary init and sidecar containers. ")
   (description "Easily add litestream to any sqlite backed application as a sidecar. This chart optionally deploys the required  configuration and provides template functions that render the necessary init and sidecar containers. ")
   (license #f)))