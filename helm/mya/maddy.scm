
(define-module (helm mya maddy)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public maddy-22.4.12
  (package
   (name "maddy")
   (version "22.4.12")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.12/maddy-22.4.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.11
  (package
   (name "maddy")
   (version "22.4.11")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.11/maddy-22.4.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.10
  (package
   (name "maddy")
   (version "22.4.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.10/maddy-22.4.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.9
  (package
   (name "maddy")
   (version "22.4.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.9/maddy-22.4.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.8
  (package
   (name "maddy")
   (version "22.4.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.8/maddy-22.4.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.7
  (package
   (name "maddy")
   (version "22.4.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.7/maddy-22.4.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.6
  (package
   (name "maddy")
   (version "22.4.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.6/maddy-22.4.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.4
  (package
   (name "maddy")
   (version "22.4.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.4/maddy-22.4.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.3
  (package
   (name "maddy")
   (version "22.4.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.3/maddy-22.4.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.2
  (package
   (name "maddy")
   (version "22.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.2/maddy-22.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.1
  (package
   (name "maddy")
   (version "22.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.1/maddy-22.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))

(define-public maddy-22.4.0
  (package
   (name "maddy")
   (version "22.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/maddy-22.4.0/maddy-22.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (description "Easily deploy and configure a maddy mail server. This chart handles a fair bit of setup, however, additional work needs to be done to properly configure all the DNS records. For a complete guide on setting up DNS for maddy, see the user guide: https://maddy.email/tutorials/setting-up/ ")
   (license #f)))