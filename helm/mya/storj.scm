
(define-module (helm mya storj)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public storj-22.4.5
  (package
   (name "storj")
   (version "22.4.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/storj-22.4.5/storj-22.4.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar containers. ")
   (description "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar containers. ")
   (license #f)))

(define-public storj-22.4.4
  (package
   (name "storj")
   (version "22.4.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/storj-22.4.4/storj-22.4.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar containers. ")
   (description "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar containers. ")
   (license #f)))

(define-public storj-22.4.3
  (package
   (name "storj")
   (version "22.4.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/storj-22.4.3/storj-22.4.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar containers. ")
   (description "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar containers. ")
   (license #f)))

(define-public storj-22.4.2
  (package
   (name "storj")
   (version "22.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/storj-22.4.2/storj-22.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar containers. ")
   (description "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar containers. ")
   (license #f)))

(define-public storj-22.4.1
  (package
   (name "storj")
   (version "22.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/storj-22.4.1/storj-22.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar containers. ")
   (description "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar containers. ")
   (license #f)))

(define-public storj-22.4.0
  (package
   (name "storj")
   (version "22.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/storj-22.4.0/storj-22.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar components. ")
   (description "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar components. ")
   (license #f)))

(define-public storj-22.3.3
  (package
   (name "storj")
   (version "22.3.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/storj-22.3.3/storj-22.3.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar components. ")
   (description "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar components. ")
   (license #f)))

(define-public storj-22.3.2
  (package
   (name "storj")
   (version "22.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/storj-22.3.2/storj-22.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar components. ")
   (description "Easily add a Storj S3 gateway to any Deployment, DaemonSet, or StatefulSet using a sidecar. This chart optionally deploys the required configuration and provides template functions that render the necessary sidecar components. ")
   (license #f)))

(define-public storj-22.3.0
  (package
   (name "storj")
   (version "22.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/mjpitz/mjpitz/releases/download/storj-22.3.0/storj-22.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart that supports deploying a storj gateway as a sidecar.")
   (description "A Helm chart that supports deploying a storj gateway as a sidecar.")
   (license #f)))