
(define-module (helm netsoc shhd)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public shhd-0.1.7
  (package
   (name "shhd")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/netsoc/charts/releases/download/shhd-0.1.7/shhd-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "SSH server with virtual shell for access to Netsoc services")
   (description "SSH server with virtual shell for access to Netsoc services")
   (license #f)))

(define-public shhd-0.1.6
  (package
   (name "shhd")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/netsoc/charts/releases/download/shhd-0.1.6/shhd-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "SSH server with virtual shell for access to Netsoc services")
   (description "SSH server with virtual shell for access to Netsoc services")
   (license #f)))

(define-public shhd-0.1.5
  (package
   (name "shhd")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/netsoc/charts/releases/download/shhd-0.1.5/shhd-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "SSH server with virtual shell for access to Netsoc services")
   (description "SSH server with virtual shell for access to Netsoc services")
   (license #f)))

(define-public shhd-0.1.4
  (package
   (name "shhd")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/netsoc/charts/releases/download/shhd-0.1.4/shhd-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "SSH server with virtual shell for access to Netsoc services")
   (description "SSH server with virtual shell for access to Netsoc services")
   (license #f)))

(define-public shhd-0.1.3
  (package
   (name "shhd")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/netsoc/charts/releases/download/shhd-0.1.3/shhd-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "SSH server with virtual shell for access to Netsoc services")
   (description "SSH server with virtual shell for access to Netsoc services")
   (license #f)))

(define-public shhd-0.1.2
  (package
   (name "shhd")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/netsoc/charts/releases/download/shhd-0.1.2/shhd-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "SSH server with virtual shell for access to Netsoc services")
   (description "SSH server with virtual shell for access to Netsoc services")
   (license #f)))

(define-public shhd-0.1.1
  (package
   (name "shhd")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/netsoc/charts/releases/download/shhd-0.1.1/shhd-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "SSH server with virtual shell for access to Netsoc services")
   (description "SSH server with virtual shell for access to Netsoc services")
   (license #f)))

(define-public shhd-0.1.0
  (package
   (name "shhd")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/netsoc/charts/releases/download/shhd-0.1.0/shhd-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "SSH server with virtual shell for access to Netsoc services")
   (description "SSH server with virtual shell for access to Netsoc services")
   (license #f)))