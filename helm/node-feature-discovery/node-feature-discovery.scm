
(define-module (helm node-feature-discovery node-feature-discovery)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public node-feature-discovery-0.15.3
  (package
   (name "node-feature-discovery")
   (version "0.15.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.15.3/node-feature-discovery-chart-0.15.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.15.2
  (package
   (name "node-feature-discovery")
   (version "0.15.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.15.2/node-feature-discovery-chart-0.15.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.15.1
  (package
   (name "node-feature-discovery")
   (version "0.15.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.15.1/node-feature-discovery-chart-0.15.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.15.0
  (package
   (name "node-feature-discovery")
   (version "0.15.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.15.0/node-feature-discovery-chart-0.15.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.14.5
  (package
   (name "node-feature-discovery")
   (version "0.14.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.14.5/node-feature-discovery-chart-0.14.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.14.4
  (package
   (name "node-feature-discovery")
   (version "0.14.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.14.4/node-feature-discovery-chart-0.14.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.14.3
  (package
   (name "node-feature-discovery")
   (version "0.14.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.14.3/node-feature-discovery-chart-0.14.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.14.2
  (package
   (name "node-feature-discovery")
   (version "0.14.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.14.2/node-feature-discovery-chart-0.14.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.14.1
  (package
   (name "node-feature-discovery")
   (version "0.14.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.14.1/node-feature-discovery-chart-0.14.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.14.0
  (package
   (name "node-feature-discovery")
   (version "0.14.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.14.0/node-feature-discovery-chart-0.14.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.13.6
  (package
   (name "node-feature-discovery")
   (version "0.13.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.13.6/node-feature-discovery-chart-0.13.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.13.5
  (package
   (name "node-feature-discovery")
   (version "0.13.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.13.5/node-feature-discovery-chart-0.13.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.13.4
  (package
   (name "node-feature-discovery")
   (version "0.13.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.13.4/node-feature-discovery-chart-0.13.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.13.3
  (package
   (name "node-feature-discovery")
   (version "0.13.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.13.3/node-feature-discovery-chart-0.13.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.13.2
  (package
   (name "node-feature-discovery")
   (version "0.13.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.13.2/node-feature-discovery-chart-0.13.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.13.1
  (package
   (name "node-feature-discovery")
   (version "0.13.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.13.1/node-feature-discovery-chart-0.13.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.13.0
  (package
   (name "node-feature-discovery")
   (version "0.13.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.13.0/node-feature-discovery-chart-0.13.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.12.5
  (package
   (name "node-feature-discovery")
   (version "0.12.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.12.5/node-feature-discovery-chart-0.12.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.12.4
  (package
   (name "node-feature-discovery")
   (version "0.12.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.12.4/node-feature-discovery-chart-0.12.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.12.3
  (package
   (name "node-feature-discovery")
   (version "0.12.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.12.3/node-feature-discovery-chart-0.12.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.12.2
  (package
   (name "node-feature-discovery")
   (version "0.12.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.12.2/node-feature-discovery-chart-0.12.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.12.1
  (package
   (name "node-feature-discovery")
   (version "0.12.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.12.1/node-feature-discovery-chart-0.12.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.12.0
  (package
   (name "node-feature-discovery")
   (version "0.12.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.12.0/node-feature-discovery-chart-0.12.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.11.3
  (package
   (name "node-feature-discovery")
   (version "0.11.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.11.3/node-feature-discovery-chart-0.11.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.11.2
  (package
   (name "node-feature-discovery")
   (version "0.11.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.11.2/node-feature-discovery-chart-0.11.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.11.1
  (package
   (name "node-feature-discovery")
   (version "0.11.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.11.1/node-feature-discovery-chart-0.11.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.11.0
  (package
   (name "node-feature-discovery")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.11.0/node-feature-discovery-chart-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.10.1
  (package
   (name "node-feature-discovery")
   (version "0.10.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.10.1/node-feature-discovery-chart-0.10.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.10.0
  (package
   (name "node-feature-discovery")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.10.0/node-feature-discovery-chart-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.9.0
  (package
   (name "node-feature-discovery")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.9.0/node-feature-discovery-chart-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.8.2
  (package
   (name "node-feature-discovery")
   (version "0.8.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.8.2/node-feature-discovery-chart-0.8.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.8.1
  (package
   (name "node-feature-discovery")
   (version "0.8.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.8.1/node-feature-discovery-chart-0.8.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))

(define-public node-feature-discovery-0.8.0
  (package
   (name "node-feature-discovery")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/kubernetes-sigs/node-feature-discovery/releases/download/v0.8.0/node-feature-discovery-chart-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/kubernetes-sigs/node-feature-discovery")
   (synopsis "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (description "Detects hardware features available on each node in a Kubernetes cluster, and advertises those features using node labels. ")
   (license #f)))