
(define-module (helm opencord att-workflow-driver)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public att-workflow-driver-2.0.1
  (package
   (name "att-workflow-driver")
   (version "2.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/att-workflow-driver-2.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "att-workflow-driver" service")
   (description "A Helm chart for XOS's "att-workflow-driver" service")
   (license #f)))

(define-public att-workflow-driver-2.0.0
  (package
   (name "att-workflow-driver")
   (version "2.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/att-workflow-driver-2.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "att-workflow-driver" service")
   (description "A Helm chart for XOS's "att-workflow-driver" service")
   (license #f)))

(define-public att-workflow-driver-1.1.2
  (package
   (name "att-workflow-driver")
   (version "1.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/att-workflow-driver-1.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "att-workflow-driver" service")
   (description "A Helm chart for XOS's "att-workflow-driver" service")
   (license #f)))

(define-public att-workflow-driver-1.1.0
  (package
   (name "att-workflow-driver")
   (version "1.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/att-workflow-driver-1.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "att-workflow-driver" service")
   (description "A Helm chart for XOS's "att-workflow-driver" service")
   (license #f)))

(define-public att-workflow-driver-1.0.20
  (package
   (name "att-workflow-driver")
   (version "1.0.20")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/att-workflow-driver-1.0.20.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "att-workflow-driver" service")
   (description "A Helm chart for XOS's "att-workflow-driver" service")
   (license #f)))

(define-public att-workflow-driver-1.0.19
  (package
   (name "att-workflow-driver")
   (version "1.0.19")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/att-workflow-driver-1.0.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "att-workflow-driver" service")
   (description "A Helm chart for XOS's "att-workflow-driver" service")
   (license #f)))

(define-public att-workflow-driver-1.0.17
  (package
   (name "att-workflow-driver")
   (version "1.0.17")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/att-workflow-driver-1.0.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "att-workflow-driver" service")
   (description "A Helm chart for XOS's "att-workflow-driver" service")
   (license #f)))

(define-public att-workflow-driver-1.0.16
  (package
   (name "att-workflow-driver")
   (version "1.0.16")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/att-workflow-driver-1.0.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "att-workflow-driver" service")
   (description "A Helm chart for XOS's "att-workflow-driver" service")
   (license #f)))

(define-public att-workflow-driver-1.0.14
  (package
   (name "att-workflow-driver")
   (version "1.0.14")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/att-workflow-driver-1.0.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "att-workflow-driver" service")
   (description "A Helm chart for XOS's "att-workflow-driver" service")
   (license #f)))

(define-public att-workflow-driver-1.0.12
  (package
   (name "att-workflow-driver")
   (version "1.0.12")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/att-workflow-driver-1.0.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "att-workflow-driver" service")
   (description "A Helm chart for XOS's "att-workflow-driver" service")
   (license #f)))