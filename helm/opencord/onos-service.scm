
(define-module (helm opencord onos-service)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public onos-service-3.0.1
  (package
   (name "onos-service")
   (version "3.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/onos-service-3.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (description "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (license #f)))

(define-public onos-service-3.0.0
  (package
   (name "onos-service")
   (version "3.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/onos-service-3.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (description "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (license #f)))

(define-public onos-service-2.1.4
  (package
   (name "onos-service")
   (version "2.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/onos-service-2.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (description "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (license #f)))

(define-public onos-service-2.1.3
  (package
   (name "onos-service")
   (version "2.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/onos-service-2.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (description "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (license #f)))

(define-public onos-service-2.1.2
  (package
   (name "onos-service")
   (version "2.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/onos-service-2.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (description "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (license #f)))

(define-public onos-service-2.1.0
  (package
   (name "onos-service")
   (version "2.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/onos-service-2.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (description "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (license #f)))

(define-public onos-service-2.0.13
  (package
   (name "onos-service")
   (version "2.0.13")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/onos-service-2.0.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (description "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (license #f)))

(define-public onos-service-2.0.12
  (package
   (name "onos-service")
   (version "2.0.12")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/onos-service-2.0.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (description "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (license #f)))

(define-public onos-service-2.0.7
  (package
   (name "onos-service")
   (version "2.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/onos-service-2.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (description "A Helm chart for XOS's "onos-service" service, which configures ONOS")
   (license #f)))