
(define-module (helm opencord progran)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public progran-2.0.10
  (package
   (name "progran")
   (version "2.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/progran-2.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "progran" service")
   (description "A Helm chart for XOS's "progran" service")
   (license #f)))

(define-public progran-2.0.9
  (package
   (name "progran")
   (version "2.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/progran-2.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "progran" service")
   (description "A Helm chart for XOS's "progran" service")
   (license #f)))

(define-public progran-2.0.8
  (package
   (name "progran")
   (version "2.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/progran-2.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "progran" service")
   (description "A Helm chart for XOS's "progran" service")
   (license #f)))

(define-public progran-2.0.7
  (package
   (name "progran")
   (version "2.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/progran-2.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "progran" service")
   (description "A Helm chart for XOS's "progran" service")
   (license #f)))

(define-public progran-2.0.6
  (package
   (name "progran")
   (version "2.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/progran-2.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "progran" service")
   (description "A Helm chart for XOS's "progran" service")
   (license #f)))

(define-public progran-2.0.5
  (package
   (name "progran")
   (version "2.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/progran-2.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "progran" service")
   (description "A Helm chart for XOS's "progran" service")
   (license #f)))

(define-public progran-2.0.4
  (package
   (name "progran")
   (version "2.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/progran-2.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "progran" service")
   (description "A Helm chart for XOS's "progran" service")
   (license #f)))

(define-public progran-2.0.3
  (package
   (name "progran")
   (version "2.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://charts.opencord.org/progran-2.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for XOS's "progran" service")
   (description "A Helm chart for XOS's "progran" service")
   (license #f)))