
(define-module (helm openfaas pro-builder)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public pro-builder-0.4.13
  (package
   (name "pro-builder")
   (version "0.4.13")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.12
  (package
   (name "pro-builder")
   (version "0.4.12")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.11
  (package
   (name "pro-builder")
   (version "0.4.11")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.10
  (package
   (name "pro-builder")
   (version "0.4.10")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.9
  (package
   (name "pro-builder")
   (version "0.4.9")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.8
  (package
   (name "pro-builder")
   (version "0.4.8")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.7
  (package
   (name "pro-builder")
   (version "0.4.7")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.6
  (package
   (name "pro-builder")
   (version "0.4.6")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.5
  (package
   (name "pro-builder")
   (version "0.4.5")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.4
  (package
   (name "pro-builder")
   (version "0.4.4")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.3
  (package
   (name "pro-builder")
   (version "0.4.3")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.2
  (package
   (name "pro-builder")
   (version "0.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.1
  (package
   (name "pro-builder")
   (version "0.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.4.0
  (package
   (name "pro-builder")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.3.9
  (package
   (name "pro-builder")
   (version "0.3.9")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.3.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.3.8
  (package
   (name "pro-builder")
   (version "0.3.8")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.3.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.3.7
  (package
   (name "pro-builder")
   (version "0.3.7")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.3.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.3.6
  (package
   (name "pro-builder")
   (version "0.3.6")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.3.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.3.5
  (package
   (name "pro-builder")
   (version "0.3.5")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.3.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.3.3
  (package
   (name "pro-builder")
   (version "0.3.3")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.3.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.3.2
  (package
   (name "pro-builder")
   (version "0.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.3.1
  (package
   (name "pro-builder")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))

(define-public pro-builder-0.3.0
  (package
   (name "pro-builder")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://openfaas.github.io/faas-netes/pro-builder-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://www.openfaas.com")
   (synopsis "Build OpenFaaS functions via a REST API")
   (description "Build OpenFaaS functions via a REST API")
   (license #f)))