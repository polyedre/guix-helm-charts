
(define-module (helm openshift-bootstraps generic-cluster-config)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public generic-cluster-config-1.0.21
  (package
   (name "generic-cluster-config")
   (version "1.0.21")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.21/generic-cluster-config-1.0.21.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/generic-cluster-config")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.20
  (package
   (name "generic-cluster-config")
   (version "1.0.20")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.20/generic-cluster-config-1.0.20.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/generic-cluster-config")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.19
  (package
   (name "generic-cluster-config")
   (version "1.0.19")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.19/generic-cluster-config-1.0.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/generic-cluster-config")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.18
  (package
   (name "generic-cluster-config")
   (version "1.0.18")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.18/generic-cluster-config-1.0.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/generic-cluster-config")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.17
  (package
   (name "generic-cluster-config")
   (version "1.0.17")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.17/generic-cluster-config-1.0.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/generic-cluster-config")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.16
  (package
   (name "generic-cluster-config")
   (version "1.0.16")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.16/generic-cluster-config-1.0.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/generic-cluster-config")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.15
  (package
   (name "generic-cluster-config")
   (version "1.0.15")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.15/generic-cluster-config-1.0.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/generic-cluster-config")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.14
  (package
   (name "generic-cluster-config")
   (version "1.0.14")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.14/generic-cluster-config-1.0.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/generic-cluster-config")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.13
  (package
   (name "generic-cluster-config")
   (version "1.0.13")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.13/generic-cluster-config-1.0.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/generic-cluster-config")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.12
  (package
   (name "generic-cluster-config")
   (version "1.0.12")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.12/generic-cluster-config-1.0.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/generic-cluster-config")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.11
  (package
   (name "generic-cluster-config")
   (version "1.0.11")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.11/generic-cluster-config-1.0.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.10
  (package
   (name "generic-cluster-config")
   (version "1.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.10/generic-cluster-config-1.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.9
  (package
   (name "generic-cluster-config")
   (version "1.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.9/generic-cluster-config-1.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.8
  (package
   (name "generic-cluster-config")
   (version "1.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.8/generic-cluster-config-1.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.7
  (package
   (name "generic-cluster-config")
   (version "1.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.7/generic-cluster-config-1.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.6
  (package
   (name "generic-cluster-config")
   (version "1.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.6/generic-cluster-config-1.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.5
  (package
   (name "generic-cluster-config")
   (version "1.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.5/generic-cluster-config-1.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.4
  (package
   (name "generic-cluster-config")
   (version "1.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.4/generic-cluster-config-1.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.3
  (package
   (name "generic-cluster-config")
   (version "1.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.3/generic-cluster-config-1.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.2
  (package
   (name "generic-cluster-config")
   (version "1.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.2/generic-cluster-config-1.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.1
  (package
   (name "generic-cluster-config")
   (version "1.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.1/generic-cluster-config-1.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))

(define-public generic-cluster-config-1.0.0
  (package
   (name "generic-cluster-config")
   (version "1.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/tjungbauer/helm-charts/releases/download/generic-cluster-config-1.0.0/generic-cluster-config-1.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/tjungbauer/helm-charts/tree/main/charts/helper-status-checker")
   (synopsis "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (description "This Chart is responsible to deploy generic clusterconfiguration, for example etcd encryption, oauth etc.")
   (license #f)))