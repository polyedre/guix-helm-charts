
(define-module (helm osc database)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public database-0.12.0
  (package
   (name "database")
   (version "0.12.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.12.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.11.0
  (package
   (name "database")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.10.1
  (package
   (name "database")
   (version "0.10.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.10.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.10.0
  (package
   (name "database")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.9.0
  (package
   (name "database")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.8.0
  (package
   (name "database")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.7.0
  (package
   (name "database")
   (version "0.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.6.0
  (package
   (name "database")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.5.0
  (package
   (name "database")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.4.0
  (package
   (name "database")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.3.1
  (package
   (name "database")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.3.0
  (package
   (name "database")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.2.0
  (package
   (name "database")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))

(define-public database-0.1.0
  (package
   (name "database")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/database-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC database service Helm Chart")
   (description "OSC database service Helm Chart")
   (license #f)))