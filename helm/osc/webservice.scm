
(define-module (helm osc webservice)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public webservice-0.33.0
  (package
   (name "webservice")
   (version "0.33.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.33.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.32.3
  (package
   (name "webservice")
   (version "0.32.3")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.32.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.32.2
  (package
   (name "webservice")
   (version "0.32.2")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.32.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.32.1
  (package
   (name "webservice")
   (version "0.32.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.32.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.32.0
  (package
   (name "webservice")
   (version "0.32.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.32.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.31.1
  (package
   (name "webservice")
   (version "0.31.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.31.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.31.0
  (package
   (name "webservice")
   (version "0.31.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.31.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.30.0
  (package
   (name "webservice")
   (version "0.30.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.30.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.29.2
  (package
   (name "webservice")
   (version "0.29.2")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.29.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.29.1
  (package
   (name "webservice")
   (version "0.29.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.29.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.29.0
  (package
   (name "webservice")
   (version "0.29.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.29.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.28.2
  (package
   (name "webservice")
   (version "0.28.2")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.28.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.28.1
  (package
   (name "webservice")
   (version "0.28.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.28.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.28.0
  (package
   (name "webservice")
   (version "0.28.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.28.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.27.0
  (package
   (name "webservice")
   (version "0.27.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.27.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.26.0
  (package
   (name "webservice")
   (version "0.26.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.26.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.25.1
  (package
   (name "webservice")
   (version "0.25.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.25.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.25.0
  (package
   (name "webservice")
   (version "0.25.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.25.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.24.0
  (package
   (name "webservice")
   (version "0.24.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.24.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.23.0
  (package
   (name "webservice")
   (version "0.23.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.23.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.22.0
  (package
   (name "webservice")
   (version "0.22.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.22.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.21.0
  (package
   (name "webservice")
   (version "0.21.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.21.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.20.0
  (package
   (name "webservice")
   (version "0.20.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.20.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.19.1
  (package
   (name "webservice")
   (version "0.19.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.19.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.19.0
  (package
   (name "webservice")
   (version "0.19.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.19.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.18.2
  (package
   (name "webservice")
   (version "0.18.2")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.18.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.18.1
  (package
   (name "webservice")
   (version "0.18.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.18.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.18.0
  (package
   (name "webservice")
   (version "0.18.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.18.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.17.0
  (package
   (name "webservice")
   (version "0.17.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.17.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.16.0
  (package
   (name "webservice")
   (version "0.16.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.16.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.15.0
  (package
   (name "webservice")
   (version "0.15.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.15.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.14.0
  (package
   (name "webservice")
   (version "0.14.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.14.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.13.0
  (package
   (name "webservice")
   (version "0.13.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.13.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.12.0
  (package
   (name "webservice")
   (version "0.12.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.12.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.11.1
  (package
   (name "webservice")
   (version "0.11.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.11.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.11.0
  (package
   (name "webservice")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.10.1
  (package
   (name "webservice")
   (version "0.10.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.10.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.10.0
  (package
   (name "webservice")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.9.1
  (package
   (name "webservice")
   (version "0.9.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.9.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.9.0
  (package
   (name "webservice")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.8.1
  (package
   (name "webservice")
   (version "0.8.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.8.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.8.0
  (package
   (name "webservice")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.7.0
  (package
   (name "webservice")
   (version "0.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.6.0
  (package
   (name "webservice")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.5.1
  (package
   (name "webservice")
   (version "0.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.5.0
  (package
   (name "webservice")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.4.1
  (package
   (name "webservice")
   (version "0.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.4.0
  (package
   (name "webservice")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.3.0
  (package
   (name "webservice")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.2.0
  (package
   (name "webservice")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))

(define-public webservice-0.1.0
  (package
   (name "webservice")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://OSC.github.io/osc-helm-charts/webservice-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "OSC webservice bootstrap Helm Chart")
   (description "OSC webservice bootstrap Helm Chart")
   (license #f)))