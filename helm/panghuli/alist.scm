
(define-module (helm panghuli alist)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public alist-0.1.5
  (package
   (name "alist")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/phenix3443/charts/releases/download/alist-0.1.5/alist-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://alist.nn.ci/")
   (synopsis "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (description "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (license #f)))

(define-public alist-0.1.4
  (package
   (name "alist")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/phenix3443/charts/releases/download/alist-0.1.4/alist-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://alist.nn.ci/")
   (synopsis "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (description "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (license #f)))

(define-public alist-0.1.3
  (package
   (name "alist")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/phenix3443/charts/releases/download/alist-0.1.3/alist-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://alist.nn.ci/")
   (synopsis "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (description "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (license #f)))

(define-public alist-0.1.2
  (package
   (name "alist")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/phenix3443/charts/releases/download/alist-0.1.2/alist-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://alist.nn.ci/")
   (synopsis "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (description "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (license #f)))

(define-public alist-0.1.1
  (package
   (name "alist")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/phenix3443/charts/releases/download/alist-0.1.1/alist-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://alist.nn.ci/")
   (synopsis "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (description "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (license #f)))

(define-public alist-0.1.0
  (package
   (name "alist")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/phenix3443/charts/releases/download/alist-0.1.0/alist-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://alist.nn.ci/")
   (synopsis "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (description "A file list program that supports multiple storage, powered by Gin and Solidjs.")
   (license #f)))