
(define-module (helm philippwaller external-service)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public external-service-1.0.8
  (package
   (name "external-service")
   (version "1.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/philippwaller/helm-charts/releases/download/external-service-1.0.8/external-service-1.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (description "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (license #f)))

(define-public external-service-1.0.7
  (package
   (name "external-service")
   (version "1.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/philippwaller/helm-charts/releases/download/external-service-1.0.7/external-service-1.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (description "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (license #f)))

(define-public external-service-1.0.6
  (package
   (name "external-service")
   (version "1.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/philippwaller/helm-charts/releases/download/external-service-1.0.6/external-service-1.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (description "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (license #f)))

(define-public external-service-1.0.5
  (package
   (name "external-service")
   (version "1.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/philippwaller/helm-charts/releases/download/external-service-1.0.5/external-service-1.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (description "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (license #f)))

(define-public external-service-1.0.4
  (package
   (name "external-service")
   (version "1.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/philippwaller/helm-charts/releases/download/external-service-1.0.4/external-service-1.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (description "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (license #f)))

(define-public external-service-1.0.3
  (package
   (name "external-service")
   (version "1.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/philippwaller/helm-charts/releases/download/external-service-1.0.3/external-service-1.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (description "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (license #f)))

(define-public external-service-1.0.2
  (package
   (name "external-service")
   (version "1.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/philippwaller/helm-charts/releases/download/external-service-1.0.2/external-service-1.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (description "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (license #f)))

(define-public external-service-1.0.1
  (package
   (name "external-service")
   (version "1.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/philippwaller/helm-charts/releases/download/external-service-1.0.1/external-service-1.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (description "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (license #f)))

(define-public external-service-1.0.0
  (package
   (name "external-service")
   (version "1.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/philippwaller/helm-charts/releases/download/external-service-1.0.0/external-service-1.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (description "This Helm Chart allows services that operate outside the Kubernetes cluster to be accessible via the ingress controller.")
   (license #f)))