
(define-module (helm pomerium pomerium)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public pomerium-34.0.1
  (package
   (name "pomerium")
   (version "34.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-34.0.1/pomerium-34.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-34.0.0
  (package
   (name "pomerium")
   (version "34.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-34.0.0/pomerium-34.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-33.0.2
  (package
   (name "pomerium")
   (version "33.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-33.0.2/pomerium-33.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-33.0.1
  (package
   (name "pomerium")
   (version "33.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-33.0.1/pomerium-33.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-33.0.0
  (package
   (name "pomerium")
   (version "33.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-33.0.0/pomerium-33.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-32.1.0
  (package
   (name "pomerium")
   (version "32.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-32.1.0/pomerium-32.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-32.0.6
  (package
   (name "pomerium")
   (version "32.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-32.0.6/pomerium-32.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-32.0.5
  (package
   (name "pomerium")
   (version "32.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-32.0.5/pomerium-32.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-32.0.4
  (package
   (name "pomerium")
   (version "32.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-32.0.4/pomerium-32.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-32.0.3
  (package
   (name "pomerium")
   (version "32.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-32.0.3/pomerium-32.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-32.0.2
  (package
   (name "pomerium")
   (version "32.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-32.0.2/pomerium-32.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-32.0.1
  (package
   (name "pomerium")
   (version "32.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-32.0.1/pomerium-32.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-32.0.0
  (package
   (name "pomerium")
   (version "32.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-32.0.0/pomerium-32.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.2.6
  (package
   (name "pomerium")
   (version "31.2.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.2.6/pomerium-31.2.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.2.5
  (package
   (name "pomerium")
   (version "31.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.2.5/pomerium-31.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.2.4
  (package
   (name "pomerium")
   (version "31.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.2.4/pomerium-31.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.2.3
  (package
   (name "pomerium")
   (version "31.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.2.3/pomerium-31.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.2.2
  (package
   (name "pomerium")
   (version "31.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.2.2/pomerium-31.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.2.1
  (package
   (name "pomerium")
   (version "31.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.2.1/pomerium-31.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.2.0
  (package
   (name "pomerium")
   (version "31.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.2.0/pomerium-31.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.1.8
  (package
   (name "pomerium")
   (version "31.1.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.1.8/pomerium-31.1.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.1.7
  (package
   (name "pomerium")
   (version "31.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.1.7/pomerium-31.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.1.6
  (package
   (name "pomerium")
   (version "31.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.1.6/pomerium-31.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.1.5
  (package
   (name "pomerium")
   (version "31.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.1.5/pomerium-31.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.1.4
  (package
   (name "pomerium")
   (version "31.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.1.4/pomerium-31.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.1.3
  (package
   (name "pomerium")
   (version "31.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.1.3/pomerium-31.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.1.2
  (package
   (name "pomerium")
   (version "31.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.1.2/pomerium-31.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.1.1
  (package
   (name "pomerium")
   (version "31.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.1.1/pomerium-31.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.1.0
  (package
   (name "pomerium")
   (version "31.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.1.0/pomerium-31.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.0.3
  (package
   (name "pomerium")
   (version "31.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.0.3/pomerium-31.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.0.2
  (package
   (name "pomerium")
   (version "31.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.0.2/pomerium-31.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.0.1
  (package
   (name "pomerium")
   (version "31.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.0.1/pomerium-31.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-31.0.0
  (package
   (name "pomerium")
   (version "31.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-31.0.0/pomerium-31.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-30.1.2
  (package
   (name "pomerium")
   (version "30.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-30.1.2/pomerium-30.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-30.1.1
  (package
   (name "pomerium")
   (version "30.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-30.1.1/pomerium-30.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-30.1.0
  (package
   (name "pomerium")
   (version "30.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-30.1.0/pomerium-30.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-30.0.2
  (package
   (name "pomerium")
   (version "30.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-30.0.2/pomerium-30.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-30.0.1
  (package
   (name "pomerium")
   (version "30.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-30.0.1/pomerium-30.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-30.0.0
  (package
   (name "pomerium")
   (version "30.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-30.0.0/pomerium-30.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-29.0.2
  (package
   (name "pomerium")
   (version "29.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-29.0.2/pomerium-29.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-29.0.1
  (package
   (name "pomerium")
   (version "29.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-29.0.1/pomerium-29.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-29.0.0
  (package
   (name "pomerium")
   (version "29.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-29.0.0/pomerium-29.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-28.0.2
  (package
   (name "pomerium")
   (version "28.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-28.0.2/pomerium-28.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-28.0.1
  (package
   (name "pomerium")
   (version "28.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-28.0.1/pomerium-28.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-28.0.0
  (package
   (name "pomerium")
   (version "28.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-28.0.0/pomerium-28.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-27.0.2
  (package
   (name "pomerium")
   (version "27.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-27.0.2/pomerium-27.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-27.0.1
  (package
   (name "pomerium")
   (version "27.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-27.0.1/pomerium-27.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-27.0.0
  (package
   (name "pomerium")
   (version "27.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-27.0.0/pomerium-27.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-26.0.1
  (package
   (name "pomerium")
   (version "26.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-26.0.1/pomerium-26.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-26.0.0
  (package
   (name "pomerium")
   (version "26.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-26.0.0/pomerium-26.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-25.0.6
  (package
   (name "pomerium")
   (version "25.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-25.0.6/pomerium-25.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-25.0.5
  (package
   (name "pomerium")
   (version "25.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-25.0.5/pomerium-25.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-25.0.4
  (package
   (name "pomerium")
   (version "25.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-25.0.4/pomerium-25.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-25.0.3
  (package
   (name "pomerium")
   (version "25.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-25.0.3/pomerium-25.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-25.0.2
  (package
   (name "pomerium")
   (version "25.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-25.0.2/pomerium-25.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-25.0.1
  (package
   (name "pomerium")
   (version "25.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-25.0.1/pomerium-25.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-25.0.0
  (package
   (name "pomerium")
   (version "25.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-25.0.0/pomerium-25.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-24.0.0
  (package
   (name "pomerium")
   (version "24.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-24.0.0/pomerium-24.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.com/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-23.2.0
  (package
   (name "pomerium")
   (version "23.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-23.2.0/pomerium-23.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-23.1.0
  (package
   (name "pomerium")
   (version "23.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-23.1.0/pomerium-23.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-23.0.0
  (package
   (name "pomerium")
   (version "23.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-23.0.0/pomerium-23.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-22.2.0
  (package
   (name "pomerium")
   (version "22.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-22.2.0/pomerium-22.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-22.1.0
  (package
   (name "pomerium")
   (version "22.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-22.1.0/pomerium-22.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-22.0.0
  (package
   (name "pomerium")
   (version "22.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-22.0.0/pomerium-22.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-21.1.3
  (package
   (name "pomerium")
   (version "21.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-21.1.3/pomerium-21.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-21.1.2
  (package
   (name "pomerium")
   (version "21.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-21.1.2/pomerium-21.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-21.1.1
  (package
   (name "pomerium")
   (version "21.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-21.1.1/pomerium-21.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-21.0.1
  (package
   (name "pomerium")
   (version "21.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-21.0.1/pomerium-21.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-21.0.0
  (package
   (name "pomerium")
   (version "21.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-21.0.0/pomerium-21.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-20.1.0
  (package
   (name "pomerium")
   (version "20.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-20.1.0/pomerium-20.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-20.0.0
  (package
   (name "pomerium")
   (version "20.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-20.0.0/pomerium-20.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-19.1.1
  (package
   (name "pomerium")
   (version "19.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-19.1.1/pomerium-19.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-19.1.0
  (package
   (name "pomerium")
   (version "19.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-19.1.0/pomerium-19.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-19.0.0
  (package
   (name "pomerium")
   (version "19.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-19.0.0/pomerium-19.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-18.0.0
  (package
   (name "pomerium")
   (version "18.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-18.0.0/pomerium-18.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-17.0.0
  (package
   (name "pomerium")
   (version "17.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-17.0.0/pomerium-17.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-16.0.4
  (package
   (name "pomerium")
   (version "16.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-16.0.4/pomerium-16.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-16.0.3
  (package
   (name "pomerium")
   (version "16.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-16.0.3/pomerium-16.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-16.0.2
  (package
   (name "pomerium")
   (version "16.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-16.0.2/pomerium-16.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-16.0.1
  (package
   (name "pomerium")
   (version "16.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-16.0.1/pomerium-16.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-16.0.0
  (package
   (name "pomerium")
   (version "16.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-16.0.0/pomerium-16.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-15.1.0
  (package
   (name "pomerium")
   (version "15.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-15.1.0/pomerium-15.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-15.0.0
  (package
   (name "pomerium")
   (version "15.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-15.0.0/pomerium-15.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-14.0.0
  (package
   (name "pomerium")
   (version "14.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-14.0.0/pomerium-14.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-13.2.3
  (package
   (name "pomerium")
   (version "13.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-13.2.3/pomerium-13.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-13.1.3
  (package
   (name "pomerium")
   (version "13.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-13.1.3/pomerium-13.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-13.1.2
  (package
   (name "pomerium")
   (version "13.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-13.1.2/pomerium-13.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-13.1.1
  (package
   (name "pomerium")
   (version "13.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-13.1.1/pomerium-13.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-13.1.0
  (package
   (name "pomerium")
   (version "13.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-13.1.0/pomerium-13.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-13.0.2
  (package
   (name "pomerium")
   (version "13.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-13.0.2/pomerium-13.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-13.0.1
  (package
   (name "pomerium")
   (version "13.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-13.0.1/pomerium-13.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-13.0.0
  (package
   (name "pomerium")
   (version "13.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-13.0.0/pomerium-13.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.5.0
  (package
   (name "pomerium")
   (version "12.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.5.0/pomerium-12.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.4.2
  (package
   (name "pomerium")
   (version "12.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.4.2/pomerium-12.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.4.1
  (package
   (name "pomerium")
   (version "12.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.4.1/pomerium-12.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.4.0
  (package
   (name "pomerium")
   (version "12.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.4.0/pomerium-12.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.3.0
  (package
   (name "pomerium")
   (version "12.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.3.0/pomerium-12.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.2.5
  (package
   (name "pomerium")
   (version "12.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.2.5/pomerium-12.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.2.4
  (package
   (name "pomerium")
   (version "12.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.2.4/pomerium-12.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.2.3
  (package
   (name "pomerium")
   (version "12.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.2.3/pomerium-12.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.2.2
  (package
   (name "pomerium")
   (version "12.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.2.2/pomerium-12.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.2.1
  (package
   (name "pomerium")
   (version "12.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.2.1/pomerium-12.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.2.0
  (package
   (name "pomerium")
   (version "12.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.2.0/pomerium-12.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.1.1
  (package
   (name "pomerium")
   (version "12.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.1.1/pomerium-12.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.1.0
  (package
   (name "pomerium")
   (version "12.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.1.0/pomerium-12.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-12.0.0
  (package
   (name "pomerium")
   (version "12.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-12.0.0/pomerium-12.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-11.2.3
  (package
   (name "pomerium")
   (version "11.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-11.2.3/pomerium-11.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-11.2.2
  (package
   (name "pomerium")
   (version "11.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-11.2.2/pomerium-11.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-11.2.1
  (package
   (name "pomerium")
   (version "11.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-11.2.1/pomerium-11.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-11.2.0
  (package
   (name "pomerium")
   (version "11.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-11.2.0/pomerium-11.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-11.1.0
  (package
   (name "pomerium")
   (version "11.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-11.1.0/pomerium-11.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-11.0.0
  (package
   (name "pomerium")
   (version "11.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-11.0.0/pomerium-11.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-10.2.0
  (package
   (name "pomerium")
   (version "10.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-10.2.0/pomerium-10.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-10.1.4
  (package
   (name "pomerium")
   (version "10.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-10.1.4/pomerium-10.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-10.1.3
  (package
   (name "pomerium")
   (version "10.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-10.1.3/pomerium-10.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-10.1.2
  (package
   (name "pomerium")
   (version "10.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-10.1.2/pomerium-10.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-10.0.2
  (package
   (name "pomerium")
   (version "10.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-10.0.2/pomerium-10.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-10.0.1
  (package
   (name "pomerium")
   (version "10.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-10.0.1/pomerium-10.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-10.0.0
  (package
   (name "pomerium")
   (version "10.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-10.0.0/pomerium-10.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-9.4.0
  (package
   (name "pomerium")
   (version "9.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-9.4.0/pomerium-9.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-9.3.0
  (package
   (name "pomerium")
   (version "9.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-9.3.0/pomerium-9.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-9.2.0
  (package
   (name "pomerium")
   (version "9.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-9.2.0/pomerium-9.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-9.1.0
  (package
   (name "pomerium")
   (version "9.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-9.1.0/pomerium-9.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-9.0.0
  (package
   (name "pomerium")
   (version "9.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-9.0.0/pomerium-9.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.6.0
  (package
   (name "pomerium")
   (version "8.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.6.0/pomerium-8.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.5.5
  (package
   (name "pomerium")
   (version "8.5.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.5.5/pomerium-8.5.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.5.4
  (package
   (name "pomerium")
   (version "8.5.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.5.4/pomerium-8.5.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.5.3
  (package
   (name "pomerium")
   (version "8.5.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.5.3/pomerium-8.5.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.5.2
  (package
   (name "pomerium")
   (version "8.5.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.5.2/pomerium-8.5.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.5.1
  (package
   (name "pomerium")
   (version "8.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.5.1/pomerium-8.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.5.0
  (package
   (name "pomerium")
   (version "8.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.5.0/pomerium-8.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.4.0
  (package
   (name "pomerium")
   (version "8.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.4.0/pomerium-8.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.3.0
  (package
   (name "pomerium")
   (version "8.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.3.0/pomerium-8.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.2.0
  (package
   (name "pomerium")
   (version "8.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.2.0/pomerium-8.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.1.0
  (package
   (name "pomerium")
   (version "8.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.1.0/pomerium-8.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.0.1
  (package
   (name "pomerium")
   (version "8.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.0.1/pomerium-8.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-8.0.0
  (package
   (name "pomerium")
   (version "8.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-8.0.0/pomerium-8.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-7.0.1
  (package
   (name "pomerium")
   (version "7.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-7.0.1/pomerium-7.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-7.0.0
  (package
   (name "pomerium")
   (version "7.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-7.0.0/pomerium-7.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-6.0.4
  (package
   (name "pomerium")
   (version "6.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-6.0.4/pomerium-6.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-6.0.3
  (package
   (name "pomerium")
   (version "6.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-6.0.3/pomerium-6.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-6.0.2
  (package
   (name "pomerium")
   (version "6.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-6.0.2/pomerium-6.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-6.0.1
  (package
   (name "pomerium")
   (version "6.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-6.0.1/pomerium-6.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-6.0.0
  (package
   (name "pomerium")
   (version "6.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-6.0.0/pomerium-6.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-5.0.6
  (package
   (name "pomerium")
   (version "5.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-5.0.6/pomerium-5.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-5.0.5
  (package
   (name "pomerium")
   (version "5.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-5.0.5/pomerium-5.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-5.0.4
  (package
   (name "pomerium")
   (version "5.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-5.0.4/pomerium-5.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-5.0.3
  (package
   (name "pomerium")
   (version "5.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-5.0.3/pomerium-5.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-5.0.2
  (package
   (name "pomerium")
   (version "5.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-5.0.2/pomerium-5.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-5.0.1
  (package
   (name "pomerium")
   (version "5.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-5.0.1/pomerium-5.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-5.0.0
  (package
   (name "pomerium")
   (version "5.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-5.0.0/pomerium-5.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-4.2.2
  (package
   (name "pomerium")
   (version "4.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-4.2.2/pomerium-4.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-4.2.1
  (package
   (name "pomerium")
   (version "4.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-4.2.1/pomerium-4.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-4.2.0
  (package
   (name "pomerium")
   (version "4.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-4.2.0/pomerium-4.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-4.1.4
  (package
   (name "pomerium")
   (version "4.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-4.1.4/pomerium-4.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-4.1.3
  (package
   (name "pomerium")
   (version "4.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-4.1.3/pomerium-4.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-4.1.2
  (package
   (name "pomerium")
   (version "4.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-4.1.2/pomerium-4.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-4.1.1
  (package
   (name "pomerium")
   (version "4.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-4.1.1/pomerium-4.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-4.1.0
  (package
   (name "pomerium")
   (version "4.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-4.1.0/pomerium-4.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-4.0.1
  (package
   (name "pomerium")
   (version "4.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-4.0.1/pomerium-4.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))

(define-public pomerium-4.0.0
  (package
   (name "pomerium")
   (version "4.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/pomerium/pomerium-helm/releases/download/pomerium-4.0.0/pomerium-4.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "http://www.pomerium.io/")
   (synopsis "Pomerium is an identity-aware access proxy.")
   (description "Pomerium is an identity-aware access proxy.")
   (license #f)))