
(define-module (helm psa-restricted-patcher psa-restricted-patcher)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public psa-restricted-patcher-0.10.1
  (package
   (name "psa-restricted-patcher")
   (version "0.10.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bryopsida/psa-restricted-patcher/releases/download/psa-restricted-patcher-0.10.1/psa-restricted-patcher-0.10.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Automatically patches pods on creation to conform to the pod security restricted profile")
   (description "Automatically patches pods on creation to conform to the pod security restricted profile")
   (license #f)))

(define-public psa-restricted-patcher-0.10.0
  (package
   (name "psa-restricted-patcher")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bryopsida/psa-restricted-patcher/releases/download/psa-restricted-patcher-0.10.0/psa-restricted-patcher-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Automatically patches pods on creation to conform to the pod security restricted profile")
   (description "Automatically patches pods on creation to conform to the pod security restricted profile")
   (license #f)))

(define-public psa-restricted-patcher-0.9.0
  (package
   (name "psa-restricted-patcher")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bryopsida/psa-restricted-patcher/releases/download/psa-restricted-patcher-0.9.0/psa-restricted-patcher-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Automatically patches pods on creation to conform to the pod security restricted profile")
   (description "Automatically patches pods on creation to conform to the pod security restricted profile")
   (license #f)))

(define-public psa-restricted-patcher-0.8.0
  (package
   (name "psa-restricted-patcher")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bryopsida/psa-restricted-patcher/releases/download/psa-restricted-patcher-0.8.0/psa-restricted-patcher-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Automatically patches pods on creation to conform to the pod security restricted profile")
   (description "Automatically patches pods on creation to conform to the pod security restricted profile")
   (license #f)))

(define-public psa-restricted-patcher-0.7.0
  (package
   (name "psa-restricted-patcher")
   (version "0.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bryopsida/psa-restricted-patcher/releases/download/psa-restricted-patcher-0.7.0/psa-restricted-patcher-0.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Automatically patches pods on creation to conform to the pod security restricted profile")
   (description "Automatically patches pods on creation to conform to the pod security restricted profile")
   (license #f)))

(define-public psa-restricted-patcher-0.6.0
  (package
   (name "psa-restricted-patcher")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bryopsida/psa-restricted-patcher/releases/download/psa-restricted-patcher-0.6.0/psa-restricted-patcher-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Automatically patches pods on creation to conform to the pod security restricted profile")
   (description "Automatically patches pods on creation to conform to the pod security restricted profile")
   (license #f)))

(define-public psa-restricted-patcher-0.5.0
  (package
   (name "psa-restricted-patcher")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bryopsida/psa-restricted-patcher/releases/download/psa-restricted-patcher-0.5.0/psa-restricted-patcher-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Automatically patches pods on creation to conform to the pod security restricted profile")
   (description "Automatically patches pods on creation to conform to the pod security restricted profile")
   (license #f)))

(define-public psa-restricted-patcher-0.4.0
  (package
   (name "psa-restricted-patcher")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bryopsida/psa-restricted-patcher/releases/download/psa-restricted-patcher-0.4.0/psa-restricted-patcher-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Automatically patches pods on creation to conform to the pod security restricted profile")
   (description "Automatically patches pods on creation to conform to the pod security restricted profile")
   (license #f)))

(define-public psa-restricted-patcher-0.3.0
  (package
   (name "psa-restricted-patcher")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/bryopsida/psa-restricted-patcher/releases/download/psa-restricted-patcher-0.3.0/psa-restricted-patcher-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Automatically patches pods on creation to conform to the pod security restricted profile")
   (description "Automatically patches pods on creation to conform to the pod security restricted profile")
   (license #f)))