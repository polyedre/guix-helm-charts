
(define-module (helm registry-creds registry-creds)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public registry-creds-0.2.3
  (package
   (name "registry-creds")
   (version "0.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/moonape1226/registry-creds/releases/download/registry-creds-0.2.3/registry-creds-0.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (description "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (license #f)))

(define-public registry-creds-0.2.2
  (package
   (name "registry-creds")
   (version "0.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/moonape1226/registry-creds/releases/download/registry-creds-0.2.2/registry-creds-0.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (description "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (license #f)))

(define-public registry-creds-0.2.1
  (package
   (name "registry-creds")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/moonape1226/registry-creds/releases/download/registry-creds-0.2.1/registry-creds-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (description "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (license #f)))

(define-public registry-creds-0.2.0
  (package
   (name "registry-creds")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/moonape1226/registry-creds/releases/download/registry-creds-0.2.0/registry-creds-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (description "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (license #f)))

(define-public registry-creds-0.1.5
  (package
   (name "registry-creds")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/moonape1226/registry-creds/releases/download/registry-creds-0.1.5/registry-creds-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (description "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (license #f)))

(define-public registry-creds-0.1.4
  (package
   (name "registry-creds")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/moonape1226/registry-creds/releases/download/registry-creds-0.1.4/registry-creds-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (description "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (license #f)))

(define-public registry-creds-0.1.3
  (package
   (name "registry-creds")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/moonape1226/registry-creds/releases/download/registry-creds-0.1.3/registry-creds-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (description "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (license #f)))

(define-public registry-creds-0.1.2
  (package
   (name "registry-creds")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/moonape1226/registry-creds/releases/download/registry-creds-0.1.2/registry-creds-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (description "A Helm chart for creating image pull secret for dockerhub (https://github.com/alexellis/registry-creds)")
   (license #f)))