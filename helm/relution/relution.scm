
(define-module (helm relution relution)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public relution-2.2.6-1
  (package
   (name "relution")
   (version "2.2.6-1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.2.6-1/relution-2.2.6-1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.2.5-3
  (package
   (name "relution")
   (version "2.2.5-3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.2.5-3/relution-2.2.5-3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.2.4
  (package
   (name "relution")
   (version "2.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.2.4/relution-2.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.2.3
  (package
   (name "relution")
   (version "2.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.2.3/relution-2.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.2.2
  (package
   (name "relution")
   (version "2.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.2.2/relution-2.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.2.1
  (package
   (name "relution")
   (version "2.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.2.1/relution-2.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.2.0
  (package
   (name "relution")
   (version "2.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.2.0/relution-2.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.1.5
  (package
   (name "relution")
   (version "2.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.1.5/relution-2.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.1.4
  (package
   (name "relution")
   (version "2.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.1.4/relution-2.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.1.3
  (package
   (name "relution")
   (version "2.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.1.3/relution-2.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.1.2
  (package
   (name "relution")
   (version "2.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.1.2/relution-2.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.1.1
  (package
   (name "relution")
   (version "2.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.1.1/relution-2.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.1.0
  (package
   (name "relution")
   (version "2.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.1.0/relution-2.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-2.0.0
  (package
   (name "relution")
   (version "2.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-2.0.0/relution-2.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-1.3.1
  (package
   (name "relution")
   (version "1.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-1.3.1/relution-1.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-1.3.0
  (package
   (name "relution")
   (version "1.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-1.3.0/relution-1.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-1.2.1
  (package
   (name "relution")
   (version "1.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-1.2.1/relution-1.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-1.2.0
  (package
   (name "relution")
   (version "1.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-1.2.0/relution-1.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-1.1.1
  (package
   (name "relution")
   (version "1.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-1.1.1/relution-1.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-1.1.0
  (package
   (name "relution")
   (version "1.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-1.1.0/relution-1.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-1.0.0
  (package
   (name "relution")
   (version "1.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-1.0.0/relution-1.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.11
  (package
   (name "relution")
   (version "0.0.11")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.11/relution-0.0.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.10
  (package
   (name "relution")
   (version "0.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.10/relution-0.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.9
  (package
   (name "relution")
   (version "0.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.9/relution-0.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.8
  (package
   (name "relution")
   (version "0.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.8/relution-0.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.7
  (package
   (name "relution")
   (version "0.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.7/relution-0.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.6
  (package
   (name "relution")
   (version "0.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.6/relution-0.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.5
  (package
   (name "relution")
   (version "0.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.5/relution-0.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.4
  (package
   (name "relution")
   (version "0.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.4/relution-0.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.3
  (package
   (name "relution")
   (version "0.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.3/relution-0.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.2
  (package
   (name "relution")
   (version "0.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.2/relution-0.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Install Relution in your Kubernetes cluster using Helm.")
   (description "Install Relution in your Kubernetes cluster using Helm.")
   (license #f)))

(define-public relution-0.0.1
  (package
   (name "relution")
   (version "0.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/relution-io/relution-kubernetes/releases/download/relution-0.0.1/relution-0.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Helm chart for Kubernetes")
   (description "A Helm chart for Kubernetes")
   (license #f)))