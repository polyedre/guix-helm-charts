
(define-module (helm reposilite reposilite)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public reposilite-1.3.6
  (package
   (name "reposilite")
   (version "1.3.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.3.6/reposilite-1.3.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.3.5
  (package
   (name "reposilite")
   (version "1.3.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.3.5/reposilite-1.3.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.3.4
  (package
   (name "reposilite")
   (version "1.3.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.3.4/reposilite-1.3.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.3.3
  (package
   (name "reposilite")
   (version "1.3.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.3.3/reposilite-1.3.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.3.2
  (package
   (name "reposilite")
   (version "1.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.3.2/reposilite-1.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.3.1
  (package
   (name "reposilite")
   (version "1.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.3.1/reposilite-1.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.3.0
  (package
   (name "reposilite")
   (version "1.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.3.0/reposilite-1.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.10
  (package
   (name "reposilite")
   (version "1.2.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.10/reposilite-1.2.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.9
  (package
   (name "reposilite")
   (version "1.2.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.9/reposilite-1.2.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.8
  (package
   (name "reposilite")
   (version "1.2.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.8/reposilite-1.2.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.7
  (package
   (name "reposilite")
   (version "1.2.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.7/reposilite-1.2.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.6
  (package
   (name "reposilite")
   (version "1.2.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.6/reposilite-1.2.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.5
  (package
   (name "reposilite")
   (version "1.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.5/reposilite-1.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.4
  (package
   (name "reposilite")
   (version "1.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.4/reposilite-1.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.3
  (package
   (name "reposilite")
   (version "1.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.3/reposilite-1.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.2
  (package
   (name "reposilite")
   (version "1.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.2/reposilite-1.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.1
  (package
   (name "reposilite")
   (version "1.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.1/reposilite-1.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.2.0
  (package
   (name "reposilite")
   (version "1.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.2.0/reposilite-1.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.1.2
  (package
   (name "reposilite")
   (version "1.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.1.2/reposilite-1.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.1.1
  (package
   (name "reposilite")
   (version "1.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.1.1/reposilite-1.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.1.0
  (package
   (name "reposilite")
   (version "1.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.1.0/reposilite-1.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.10
  (package
   (name "reposilite")
   (version "1.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.10/reposilite-1.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.9
  (package
   (name "reposilite")
   (version "1.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.9/reposilite-1.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.8
  (package
   (name "reposilite")
   (version "1.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.8/reposilite-1.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.7
  (package
   (name "reposilite")
   (version "1.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.7/reposilite-1.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.6
  (package
   (name "reposilite")
   (version "1.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.6/reposilite-1.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.5
  (package
   (name "reposilite")
   (version "1.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.5/reposilite-1.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.4
  (package
   (name "reposilite")
   (version "1.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.4/reposilite-1.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.3
  (package
   (name "reposilite")
   (version "1.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.3/reposilite-1.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.2
  (package
   (name "reposilite")
   (version "1.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.2/reposilite-1.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.1
  (package
   (name "reposilite")
   (version "1.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.1/reposilite-1.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))

(define-public reposilite-1.0.0
  (package
   (name "reposilite")
   (version "1.0.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/reposilite-playground/reposilite-helm/releases/download/reposilite-1.0.0/reposilite-1.0.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (description "Lightweight and easy-to-use repository management software dedicated for the Maven based artifacts in the JVM ecosystem")
   (license #f)))