
(define-module (helm restinthemiddle restinthemiddle)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public restinthemiddle-0.11.0
  (package
   (name "restinthemiddle")
   (version "0.11.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/restinthemiddle/helm/releases/download/restinthemiddle-0.11.0/restinthemiddle-0.11.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy the HTTP logging proxy with ease")
   (description "Deploy the HTTP logging proxy with ease")
   (license #f)))

(define-public restinthemiddle-0.10.0
  (package
   (name "restinthemiddle")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/restinthemiddle/helm/releases/download/restinthemiddle-0.10.0/restinthemiddle-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy the HTTP logging proxy with ease")
   (description "Deploy the HTTP logging proxy with ease")
   (license #f)))

(define-public restinthemiddle-0.9.1
  (package
   (name "restinthemiddle")
   (version "0.9.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/restinthemiddle/helm/releases/download/restinthemiddle-0.9.1/restinthemiddle-0.9.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy the HTTP logging proxy with ease")
   (description "Deploy the HTTP logging proxy with ease")
   (license #f)))

(define-public restinthemiddle-0.9.0
  (package
   (name "restinthemiddle")
   (version "0.9.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/restinthemiddle/helm/releases/download/restinthemiddle-0.9.0/restinthemiddle-0.9.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy the HTTP logging proxy with ease")
   (description "Deploy the HTTP logging proxy with ease")
   (license #f)))

(define-public restinthemiddle-0.8.0
  (package
   (name "restinthemiddle")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/restinthemiddle/helm/releases/download/restinthemiddle-0.8.0/restinthemiddle-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy the HTTP logging proxy with ease")
   (description "Deploy the HTTP logging proxy with ease")
   (license #f)))

(define-public restinthemiddle-0.7.2
  (package
   (name "restinthemiddle")
   (version "0.7.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/restinthemiddle/helm/releases/download/restinthemiddle-0.7.2/restinthemiddle-0.7.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy the HTTP logging proxy with ease")
   (description "Deploy the HTTP logging proxy with ease")
   (license #f)))

(define-public restinthemiddle-0.7.1
  (package
   (name "restinthemiddle")
   (version "0.7.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/restinthemiddle/helm/releases/download/restinthemiddle-0.7.1/restinthemiddle-0.7.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy the HTTP logging proxy with ease")
   (description "Deploy the HTTP logging proxy with ease")
   (license #f)))

(define-public restinthemiddle-0.7.0
  (package
   (name "restinthemiddle")
   (version "0.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/restinthemiddle/helm/releases/download/restinthemiddle-0.7.0/restinthemiddle-0.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy the HTTP logging proxy with ease")
   (description "Deploy the HTTP logging proxy with ease")
   (license #f)))

(define-public restinthemiddle-0.6.0
  (package
   (name "restinthemiddle")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/restinthemiddle/helm/releases/download/restinthemiddle-0.6.0/restinthemiddle-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy the HTTP logging proxy with ease")
   (description "Deploy the HTTP logging proxy with ease")
   (license #f)))

(define-public restinthemiddle-0.5.0
  (package
   (name "restinthemiddle")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/restinthemiddle/helm/releases/download/restinthemiddle-0.5.0/restinthemiddle-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Deploy the HTTP logging proxy with ease")
   (description "Deploy the HTTP logging proxy with ease")
   (license #f)))