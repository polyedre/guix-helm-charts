
(define-module (helm rimusz hostpath-provisioner)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public hostpath-provisioner-0.2.13
  (package
   (name "hostpath-provisioner")
   (version "0.2.13")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.12
  (package
   (name "hostpath-provisioner")
   (version "0.2.12")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.11
  (package
   (name "hostpath-provisioner")
   (version "0.2.11")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.10
  (package
   (name "hostpath-provisioner")
   (version "0.2.10")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.9
  (package
   (name "hostpath-provisioner")
   (version "0.2.9")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.8
  (package
   (name "hostpath-provisioner")
   (version "0.2.8")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.7
  (package
   (name "hostpath-provisioner")
   (version "0.2.7")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.6
  (package
   (name "hostpath-provisioner")
   (version "0.2.6")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.5
  (package
   (name "hostpath-provisioner")
   (version "0.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.4
  (package
   (name "hostpath-provisioner")
   (version "0.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.3
  (package
   (name "hostpath-provisioner")
   (version "0.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.2
  (package
   (name "hostpath-provisioner")
   (version "0.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))

(define-public hostpath-provisioner-0.2.0
  (package
   (name "hostpath-provisioner")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.rimusz.net/hostpath-provisioner-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/rimusz/charts/tree/master/stable/hostpath-provisioner")
   (synopsis "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (description "hostpath-provisioner is an automatic provisioner creating Persistent Volumes from the hostpath.")
   (license #f)))