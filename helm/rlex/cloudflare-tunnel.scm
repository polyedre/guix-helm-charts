
(define-module (helm rlex cloudflare-tunnel)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public cloudflare-tunnel-0.8.0
  (package
   (name "cloudflare-tunnel")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.8.0/cloudflare-tunnel-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.7.0
  (package
   (name "cloudflare-tunnel")
   (version "0.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.7.0/cloudflare-tunnel-0.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.6.0
  (package
   (name "cloudflare-tunnel")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.6.0/cloudflare-tunnel-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.5.0
  (package
   (name "cloudflare-tunnel")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.5.0/cloudflare-tunnel-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.4.0
  (package
   (name "cloudflare-tunnel")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.4.0/cloudflare-tunnel-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.2.1
  (package
   (name "cloudflare-tunnel")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.2.1/cloudflare-tunnel-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.2.0
  (package
   (name "cloudflare-tunnel")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.2.0/cloudflare-tunnel-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.12
  (package
   (name "cloudflare-tunnel")
   (version "0.1.12")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.12/cloudflare-tunnel-0.1.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.11
  (package
   (name "cloudflare-tunnel")
   (version "0.1.11")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.11/cloudflare-tunnel-0.1.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.9
  (package
   (name "cloudflare-tunnel")
   (version "0.1.9")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.9/cloudflare-tunnel-0.1.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.7
  (package
   (name "cloudflare-tunnel")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.7/cloudflare-tunnel-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.6
  (package
   (name "cloudflare-tunnel")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.6/cloudflare-tunnel-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.5
  (package
   (name "cloudflare-tunnel")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.5/cloudflare-tunnel-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.4
  (package
   (name "cloudflare-tunnel")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.4/cloudflare-tunnel-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.3
  (package
   (name "cloudflare-tunnel")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.3/cloudflare-tunnel-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.2
  (package
   (name "cloudflare-tunnel")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.2/cloudflare-tunnel-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.1
  (package
   (name "cloudflare-tunnel")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.1/cloudflare-tunnel-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))

(define-public cloudflare-tunnel-0.1.0
  (package
   (name "cloudflare-tunnel")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/cloudflare-tunnel-0.1.0/cloudflare-tunnel-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (description "Manage and use cloudflare tunnels (also known as argo tunnels) on k8s cluster")
   (license #f)))