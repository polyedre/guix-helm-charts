
(define-module (helm rlex coroot-node-agent)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public coroot-node-agent-0.7.1
  (package
   (name "coroot-node-agent")
   (version "0.7.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.7.1/coroot-node-agent-0.7.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))

(define-public coroot-node-agent-0.7.0
  (package
   (name "coroot-node-agent")
   (version "0.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.7.0/coroot-node-agent-0.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))

(define-public coroot-node-agent-0.6.0
  (package
   (name "coroot-node-agent")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.6.0/coroot-node-agent-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))

(define-public coroot-node-agent-0.5.2
  (package
   (name "coroot-node-agent")
   (version "0.5.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.5.2/coroot-node-agent-0.5.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))

(define-public coroot-node-agent-0.4.0
  (package
   (name "coroot-node-agent")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.4.0/coroot-node-agent-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))

(define-public coroot-node-agent-0.3.1
  (package
   (name "coroot-node-agent")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.3.1/coroot-node-agent-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))

(define-public coroot-node-agent-0.3.0
  (package
   (name "coroot-node-agent")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.3.0/coroot-node-agent-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))

(define-public coroot-node-agent-0.2.0
  (package
   (name "coroot-node-agent")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.2.0/coroot-node-agent-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))

(define-public coroot-node-agent-0.1.2
  (package
   (name "coroot-node-agent")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.1.2/coroot-node-agent-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))

(define-public coroot-node-agent-0.1.1
  (package
   (name "coroot-node-agent")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.1.1/coroot-node-agent-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))

(define-public coroot-node-agent-0.1.0
  (package
   (name "coroot-node-agent")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rlex/helm-charts/releases/download/coroot-node-agent-0.1.0/coroot-node-agent-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (description "A Prometheus exporter based on eBPF that gathers comprehensive container metrics")
   (license #f)))