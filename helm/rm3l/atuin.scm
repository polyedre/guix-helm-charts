
(define-module (helm rm3l atuin)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public atuin-0.8.0
  (package
   (name "atuin")
   (version "0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rm3l/helm-charts/releases/download/atuin-0.8.0/atuin-0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (description "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (license #f)))

(define-public atuin-0.7.0
  (package
   (name "atuin")
   (version "0.7.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rm3l/helm-charts/releases/download/atuin-0.7.0/atuin-0.7.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (description "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (license #f)))

(define-public atuin-0.6.0
  (package
   (name "atuin")
   (version "0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rm3l/helm-charts/releases/download/atuin-0.6.0/atuin-0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (description "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (license #f)))

(define-public atuin-0.5.0
  (package
   (name "atuin")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rm3l/helm-charts/releases/download/atuin-0.5.0/atuin-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (description "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (license #f)))

(define-public atuin-0.4.0
  (package
   (name "atuin")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rm3l/helm-charts/releases/download/atuin-0.4.0/atuin-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (description "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (license #f)))

(define-public atuin-0.3.0
  (package
   (name "atuin")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rm3l/helm-charts/releases/download/atuin-0.3.0/atuin-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (description "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (license #f)))

(define-public atuin-0.2.0
  (package
   (name "atuin")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rm3l/helm-charts/releases/download/atuin-0.2.0/atuin-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (description "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (license #f)))

(define-public atuin-0.1.1
  (package
   (name "atuin")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rm3l/helm-charts/releases/download/atuin-0.1.1/atuin-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (description "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (license #f)))

(define-public atuin-0.1.0
  (package
   (name "atuin")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/rm3l/helm-charts/releases/download/atuin-0.1.0/atuin-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (description "Unofficial Chart for Atuin, the magical shell history. The server provides fully encrypted synchronization of the shell history across machines. https://github.com/ellie/atuin ")
   (license #f)))