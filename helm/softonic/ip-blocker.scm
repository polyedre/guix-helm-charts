
(define-module (helm softonic ip-blocker)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public ip-blocker-0.6.2
  (package
   (name "ip-blocker")
   (version "0.6.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.6.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.6.1
  (package
   (name "ip-blocker")
   (version "0.6.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.6.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.5.1
  (package
   (name "ip-blocker")
   (version "0.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.5.0
  (package
   (name "ip-blocker")
   (version "0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.4.5
  (package
   (name "ip-blocker")
   (version "0.4.5")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.4.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.4.4
  (package
   (name "ip-blocker")
   (version "0.4.4")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.4.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.4.1
  (package
   (name "ip-blocker")
   (version "0.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.3.2
  (package
   (name "ip-blocker")
   (version "0.3.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.3.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.3.1
  (package
   (name "ip-blocker")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.3.0
  (package
   (name "ip-blocker")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.2.1
  (package
   (name "ip-blocker")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.2.0
  (package
   (name "ip-blocker")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))

(define-public ip-blocker-0.0.1
  (package
   (name "ip-blocker")
   (version "0.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.softonic.io/charts/ip-blocker-0.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (description "This chart deploys a daemon in k8s for blocking IPs from unwanted bots")
   (license #f)))