
(define-module (helm stakater exposecontroller)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public exposecontroller-1.0.19
  (package
   (name "exposecontroller")
   (version "1.0.19")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater-charts/exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.18
  (package
   (name "exposecontroller")
   (version "1.0.18")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater-charts/exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.17
  (package
   (name "exposecontroller")
   (version "1.0.17")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.16
  (package
   (name "exposecontroller")
   (version "1.0.16")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.15
  (package
   (name "exposecontroller")
   (version "1.0.15")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.14
  (package
   (name "exposecontroller")
   (version "1.0.14")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.13
  (package
   (name "exposecontroller")
   (version "1.0.13")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.12
  (package
   (name "exposecontroller")
   (version "1.0.12")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.11
  (package
   (name "exposecontroller")
   (version "1.0.11")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.10
  (package
   (name "exposecontroller")
   (version "1.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.9
  (package
   (name "exposecontroller")
   (version "1.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.8
  (package
   (name "exposecontroller")
   (version "1.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.7
  (package
   (name "exposecontroller")
   (version "1.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.6
  (package
   (name "exposecontroller")
   (version "1.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))

(define-public exposecontroller-1.0.5
  (package
   (name "exposecontroller")
   (version "1.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/exposecontroller-1.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/chart-exposecontroller")
   (synopsis "exposecontroller chart that runs on kubernetes")
   (description "exposecontroller chart that runs on kubernetes")
   (license #f)))