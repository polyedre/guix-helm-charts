
(define-module (helm stakater konfigurator)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public konfigurator-0.1.32
  (package
   (name "konfigurator")
   (version "0.1.32")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.32.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.31
  (package
   (name "konfigurator")
   (version "0.1.31")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.31.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.30
  (package
   (name "konfigurator")
   (version "0.1.30")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.30.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.29
  (package
   (name "konfigurator")
   (version "0.1.29")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.29.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.28
  (package
   (name "konfigurator")
   (version "0.1.28")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.28.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.27
  (package
   (name "konfigurator")
   (version "0.1.27")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.27.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.26
  (package
   (name "konfigurator")
   (version "0.1.26")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.26.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.25
  (package
   (name "konfigurator")
   (version "0.1.25")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.25.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.24
  (package
   (name "konfigurator")
   (version "0.1.24")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.24.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.23
  (package
   (name "konfigurator")
   (version "0.1.23")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.23.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.22
  (package
   (name "konfigurator")
   (version "0.1.22")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.22.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.21
  (package
   (name "konfigurator")
   (version "0.1.21")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.21.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.20
  (package
   (name "konfigurator")
   (version "0.1.20")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.20.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.19
  (package
   (name "konfigurator")
   (version "0.1.19")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.18
  (package
   (name "konfigurator")
   (version "0.1.18")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.17
  (package
   (name "konfigurator")
   (version "0.1.17")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.16
  (package
   (name "konfigurator")
   (version "0.1.16")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.15
  (package
   (name "konfigurator")
   (version "0.1.15")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.14
  (package
   (name "konfigurator")
   (version "0.1.14")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.13
  (package
   (name "konfigurator")
   (version "0.1.13")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.1.12
  (package
   (name "konfigurator")
   (version "0.1.12")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.1.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-v0.0.26
  (package
   (name "konfigurator")
   (version "v0.0.26")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-v0.0.26.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-v0.0.25
  (package
   (name "konfigurator")
   (version "v0.0.25")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-v0.0.25.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-v0.0.24
  (package
   (name "konfigurator")
   (version "v0.0.24")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-v0.0.24.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-v0.0.23
  (package
   (name "konfigurator")
   (version "v0.0.23")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-v0.0.23.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-v0.0.22
  (package
   (name "konfigurator")
   (version "v0.0.22")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-v0.0.22.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-v0.0.21
  (package
   (name "konfigurator")
   (version "v0.0.21")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-v0.0.21.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-v0.0.20
  (package
   (name "konfigurator")
   (version "v0.0.20")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-v0.0.20.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-v0.0.19
  (package
   (name "konfigurator")
   (version "v0.0.19")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-v0.0.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-v0.0.18
  (package
   (name "konfigurator")
   (version "v0.0.18")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-v0.0.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.17
  (package
   (name "konfigurator")
   (version "0.0.17")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.16
  (package
   (name "konfigurator")
   (version "0.0.16")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.15
  (package
   (name "konfigurator")
   (version "0.0.15")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.11
  (package
   (name "konfigurator")
   (version "0.0.11")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.10
  (package
   (name "konfigurator")
   (version "0.0.10")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.9
  (package
   (name "konfigurator")
   (version "0.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.7
  (package
   (name "konfigurator")
   (version "0.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.6
  (package
   (name "konfigurator")
   (version "0.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.5
  (package
   (name "konfigurator")
   (version "0.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.4
  (package
   (name "konfigurator")
   (version "0.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.3
  (package
   (name "konfigurator")
   (version "0.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.2
  (package
   (name "konfigurator")
   (version "0.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))

(define-public konfigurator-0.0.1
  (package
   (name "konfigurator")
   (version "0.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/konfigurator-0.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/Konfigurator")
   (synopsis "Konfigurator chart that runs on kubernetes")
   (description "Konfigurator chart that runs on kubernetes")
   (license #f)))