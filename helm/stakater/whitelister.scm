
(define-module (helm stakater whitelister)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public whitelister-v0.0.16
  (package
   (name "whitelister")
   (version "v0.0.16")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-v0.0.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-v0.0.14
  (package
   (name "whitelister")
   (version "v0.0.14")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-v0.0.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-v0.0.13
  (package
   (name "whitelister")
   (version "v0.0.13")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-v0.0.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-0.0.9
  (package
   (name "whitelister")
   (version "0.0.9")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-0.0.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-0.0.8
  (package
   (name "whitelister")
   (version "0.0.8")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-0.0.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-0.0.7
  (package
   (name "whitelister")
   (version "0.0.7")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-0.0.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-0.0.6
  (package
   (name "whitelister")
   (version "0.0.6")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-0.0.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-0.0.5
  (package
   (name "whitelister")
   (version "0.0.5")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-0.0.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-0.0.4
  (package
   (name "whitelister")
   (version "0.0.4")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-0.0.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-0.0.3
  (package
   (name "whitelister")
   (version "0.0.3")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-0.0.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-0.0.2
  (package
   (name "whitelister")
   (version "0.0.2")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-0.0.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))

(define-public whitelister-0.0.1
  (package
   (name "whitelister")
   (version "0.0.1")
   (source (origin
            (method url-fetch)
            (uri "https://stakater.github.io/stakater-charts/whitelister-0.0.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/stakater/whitelister")
   (synopsis "whitelister chart that runs on kubernetes")
   (description "whitelister chart that runs on kubernetes")
   (license #f)))