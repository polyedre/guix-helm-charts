
(define-module (helm statcan fdi-dotstatsuite)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public fdi-dotstatsuite-0.2.21
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.21")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/StatCan/charts/releases/download/fdi-dotstatsuite-0.2.21/fdi-dotstatsuite-0.2.21.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.20
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.20")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/StatCan/charts/releases/download/fdi-dotstatsuite-0.2.20/fdi-dotstatsuite-0.2.20.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.19
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.19")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/StatCan/charts/releases/download/fdi-dotstatsuite-0.2.19/fdi-dotstatsuite-0.2.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.18
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.18")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/StatCan/charts/releases/download/fdi-dotstatsuite-0.2.18/fdi-dotstatsuite-0.2.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.17
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.17")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/StatCan/charts/releases/download/fdi-dotstatsuite-0.2.17/fdi-dotstatsuite-0.2.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.16
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.16")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/StatCan/charts/releases/download/fdi-dotstatsuite-0.2.16/fdi-dotstatsuite-0.2.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.15
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.15")
   (source (origin
            (method url-fetch)
            (uri "https://statcan.github.io/charts/fdi-dotstatsuite-0.2.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.14
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.14")
   (source (origin
            (method url-fetch)
            (uri "https://statcan.github.io/charts/fdi-dotstatsuite-0.2.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.13
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.13")
   (source (origin
            (method url-fetch)
            (uri "https://statcan.github.io/charts/fdi-dotstatsuite-0.2.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.12
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.12")
   (source (origin
            (method url-fetch)
            (uri "https://statcan.github.io/charts/fdi-dotstatsuite-0.2.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.11
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.11")
   (source (origin
            (method url-fetch)
            (uri "https://statcan.github.io/charts/fdi-dotstatsuite-0.2.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite kube core services")
   (description "fdi-dotstatsuite kube core services")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.10
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.10")
   (source (origin
            (method url-fetch)
            (uri "https://statcan.github.io/charts/fdi-dotstatsuite-0.2.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite")
   (description "fdi-dotstatsuite")
   (license #f)))

(define-public fdi-dotstatsuite-0.2.9
  (package
   (name "fdi-dotstatsuite")
   (version "0.2.9")
   (source (origin
            (method url-fetch)
            (uri "https://statcan.github.io/charts/fdi-dotstatsuite-0.2.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://statcan.gc.ca")
   (synopsis "fdi-dotstatsuite")
   (description "fdi-dotstatsuite")
   (license #f)))