
(define-module (helm ternero ternero)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public ternero-0.1.6
  (package
   (name "ternero")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gusraggio/ternero/releases/download/ternero-0.1.6/ternero-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A simple dashboard fo k3s/rke clusters")
   (description "A simple dashboard fo k3s/rke clusters")
   (license #f)))

(define-public ternero-0.1.5
  (package
   (name "ternero")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gusraggio/ternero/releases/download/ternero-0.1.5/ternero-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A simple dashboard fo k3s/rke clusters")
   (description "A simple dashboard fo k3s/rke clusters")
   (license #f)))

(define-public ternero-0.1.4
  (package
   (name "ternero")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gusraggio/ternero/releases/download/ternero-0.1.4/ternero-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A simple dashboard fo k3s/rke clusters")
   (description "A simple dashboard fo k3s/rke clusters")
   (license #f)))

(define-public ternero-0.1.3
  (package
   (name "ternero")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gusraggio/ternero/releases/download/ternero-0.1.3/ternero-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A simple dashboard fo k3s/rke clusters")
   (description "A simple dashboard fo k3s/rke clusters")
   (license #f)))

(define-public ternero-0.1.2
  (package
   (name "ternero")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gusraggio/ternero/releases/download/ternero-0.1.2/ternero-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A simple dashboard fo k3s/rke clusters")
   (description "A simple dashboard fo k3s/rke clusters")
   (license #f)))

(define-public ternero-0.1.1
  (package
   (name "ternero")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gusraggio/ternero/releases/download/ternero-0.1.1/ternero-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A simple dashboard fo k3s/rke clusters")
   (description "A simple dashboard fo k3s/rke clusters")
   (license #f)))

(define-public ternero-0.1.0
  (package
   (name "ternero")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/gusraggio/ternero/releases/download/ternero-0.1.0/ternero-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A simple dashboard fo k3s/rke clusters")
   (description "A simple dashboard fo k3s/rke clusters")
   (license #f)))