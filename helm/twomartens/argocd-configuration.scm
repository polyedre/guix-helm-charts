
(define-module (helm twomartens argocd-configuration)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public argocd-configuration-0.2.4
  (package
   (name "argocd-configuration")
   (version "0.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://repo.2martens.de/charts/argocd-configuration-0.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "ArgoCD configuration which requires Argo CD to be installed separately.")
   (description "ArgoCD configuration which requires Argo CD to be installed separately.")
   (license #f)))

(define-public argocd-configuration-0.2.3
  (package
   (name "argocd-configuration")
   (version "0.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://repo.2martens.de/charts/argocd-configuration-0.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "ArgoCD configuration which requires Argo CD to be installed separately.")
   (description "ArgoCD configuration which requires Argo CD to be installed separately.")
   (license #f)))

(define-public argocd-configuration-0.2.2
  (package
   (name "argocd-configuration")
   (version "0.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://repo.2martens.de/charts/argocd-configuration-0.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "ArgoCD configuration which requires Argo CD to be installed separately.")
   (description "ArgoCD configuration which requires Argo CD to be installed separately.")
   (license #f)))

(define-public argocd-configuration-0.2.1
  (package
   (name "argocd-configuration")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://repo.2martens.de/charts/argocd-configuration-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "ArgoCD configuration which requires Argo CD to be installed separately.")
   (description "ArgoCD configuration which requires Argo CD to be installed separately.")
   (license #f)))

(define-public argocd-configuration-0.2.0
  (package
   (name "argocd-configuration")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://repo.2martens.de/charts/argocd-configuration-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "ArgoCD configuration which requires Argo CD to be installed separately.")
   (description "ArgoCD configuration which requires Argo CD to be installed separately.")
   (license #f)))

(define-public argocd-configuration-0.1.6
  (package
   (name "argocd-configuration")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://repo.2martens.de/charts/argocd-configuration-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "ArgoCD configuration which requires Argo CD to be installed separately.")
   (description "ArgoCD configuration which requires Argo CD to be installed separately.")
   (license #f)))

(define-public argocd-configuration-0.1.5
  (package
   (name "argocd-configuration")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://repo.2martens.de/charts/argocd-configuration-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "ArgoCD configuration which requires Argo CD to be installed separately.")
   (description "ArgoCD configuration which requires Argo CD to be installed separately.")
   (license #f)))

(define-public argocd-configuration-0.1.4
  (package
   (name "argocd-configuration")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://repo.2martens.de/charts/argocd-configuration-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "ArgoCD configuration which requires Argo CD to be installed separately.")
   (description "ArgoCD configuration which requires Argo CD to be installed separately.")
   (license #f)))

(define-public argocd-configuration-0.1.3
  (package
   (name "argocd-configuration")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://repo.2martens.de/charts/argocd-configuration-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "ArgoCD configuration which requires Argo CD to be installed separately.")
   (description "ArgoCD configuration which requires Argo CD to be installed separately.")
   (license #f)))

(define-public argocd-configuration-0.1.2
  (package
   (name "argocd-configuration")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://repo.2martens.de/charts/argocd-configuration-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "ArgoCD configuration which requires Argo CD to be installed separately.")
   (description "ArgoCD configuration which requires Argo CD to be installed separately.")
   (license #f)))