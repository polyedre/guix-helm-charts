
(define-module (helm vault-config-operator vault-config-operator)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public vault-config-operator-v0.8.25
  (package
   (name "vault-config-operator")
   (version "v0.8.25")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.25.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.24
  (package
   (name "vault-config-operator")
   (version "v0.8.24")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.24.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.23
  (package
   (name "vault-config-operator")
   (version "v0.8.23")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.23.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.22
  (package
   (name "vault-config-operator")
   (version "v0.8.22")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.22.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.21
  (package
   (name "vault-config-operator")
   (version "v0.8.21")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.21.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.20
  (package
   (name "vault-config-operator")
   (version "v0.8.20")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.20.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.19
  (package
   (name "vault-config-operator")
   (version "v0.8.19")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.18
  (package
   (name "vault-config-operator")
   (version "v0.8.18")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.17
  (package
   (name "vault-config-operator")
   (version "v0.8.17")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.16
  (package
   (name "vault-config-operator")
   (version "v0.8.16")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.15
  (package
   (name "vault-config-operator")
   (version "v0.8.15")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.14
  (package
   (name "vault-config-operator")
   (version "v0.8.14")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.13
  (package
   (name "vault-config-operator")
   (version "v0.8.13")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.12
  (package
   (name "vault-config-operator")
   (version "v0.8.12")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.11
  (package
   (name "vault-config-operator")
   (version "v0.8.11")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.10
  (package
   (name "vault-config-operator")
   (version "v0.8.10")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.9
  (package
   (name "vault-config-operator")
   (version "v0.8.9")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.7
  (package
   (name "vault-config-operator")
   (version "v0.8.7")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.5
  (package
   (name "vault-config-operator")
   (version "v0.8.5")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.4
  (package
   (name "vault-config-operator")
   (version "v0.8.4")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.3
  (package
   (name "vault-config-operator")
   (version "v0.8.3")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.2
  (package
   (name "vault-config-operator")
   (version "v0.8.2")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.8.0
  (package
   (name "vault-config-operator")
   (version "v0.8.0")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.8.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.7.1
  (package
   (name "vault-config-operator")
   (version "v0.7.1")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.7.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.6.5
  (package
   (name "vault-config-operator")
   (version "v0.6.5")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.6.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.6.4
  (package
   (name "vault-config-operator")
   (version "v0.6.4")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.6.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.6.3
  (package
   (name "vault-config-operator")
   (version "v0.6.3")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.6.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.6.2
  (package
   (name "vault-config-operator")
   (version "v0.6.2")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.6.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.6.1
  (package
   (name "vault-config-operator")
   (version "v0.6.1")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.6.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.6.0
  (package
   (name "vault-config-operator")
   (version "v0.6.0")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.6.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.5.1
  (package
   (name "vault-config-operator")
   (version "v0.5.1")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.5.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.5.0
  (package
   (name "vault-config-operator")
   (version "v0.5.0")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.5.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.4.1
  (package
   (name "vault-config-operator")
   (version "v0.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.4.0
  (package
   (name "vault-config-operator")
   (version "v0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.3.1
  (package
   (name "vault-config-operator")
   (version "v0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.3.0
  (package
   (name "vault-config-operator")
   (version "v0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.2.0
  (package
   (name "vault-config-operator")
   (version "v0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.1.2
  (package
   (name "vault-config-operator")
   (version "v0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.1.1
  (package
   (name "vault-config-operator")
   (version "v0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))

(define-public vault-config-operator-v0.1.0
  (package
   (name "vault-config-operator")
   (version "v0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://redhat-cop.github.io/vault-config-operator/vault-config-operator/vault-config-operator-v0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart that deploys vault-config-operator")
   (description "Helm chart that deploys vault-config-operator")
   (license #f)))