
(define-module (helm wallarm wallarm-oob)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public wallarm-oob-0.10.27
  (package
   (name "wallarm-oob")
   (version "0.10.27")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.27.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.26
  (package
   (name "wallarm-oob")
   (version "0.10.26")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.26.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.25
  (package
   (name "wallarm-oob")
   (version "0.10.25")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.25.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.24
  (package
   (name "wallarm-oob")
   (version "0.10.24")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.24.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.23
  (package
   (name "wallarm-oob")
   (version "0.10.23")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.23.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.22
  (package
   (name "wallarm-oob")
   (version "0.10.22")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.22.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.21
  (package
   (name "wallarm-oob")
   (version "0.10.21")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.21.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.20
  (package
   (name "wallarm-oob")
   (version "0.10.20")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.20.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.19
  (package
   (name "wallarm-oob")
   (version "0.10.19")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.19.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.18
  (package
   (name "wallarm-oob")
   (version "0.10.18")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.18.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.17
  (package
   (name "wallarm-oob")
   (version "0.10.17")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.17.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.16
  (package
   (name "wallarm-oob")
   (version "0.10.16")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.16.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.15
  (package
   (name "wallarm-oob")
   (version "0.10.15")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.15.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.14
  (package
   (name "wallarm-oob")
   (version "0.10.14")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.14.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.13
  (package
   (name "wallarm-oob")
   (version "0.10.13")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.13.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.12
  (package
   (name "wallarm-oob")
   (version "0.10.12")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.12.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.11
  (package
   (name "wallarm-oob")
   (version "0.10.11")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.11.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.10
  (package
   (name "wallarm-oob")
   (version "0.10.10")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.10.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.9
  (package
   (name "wallarm-oob")
   (version "0.10.9")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.9.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.8
  (package
   (name "wallarm-oob")
   (version "0.10.8")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.8.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.7
  (package
   (name "wallarm-oob")
   (version "0.10.7")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.6
  (package
   (name "wallarm-oob")
   (version "0.10.6")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.5
  (package
   (name "wallarm-oob")
   (version "0.10.5")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.4
  (package
   (name "wallarm-oob")
   (version "0.10.4")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.3
  (package
   (name "wallarm-oob")
   (version "0.10.3")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.2
  (package
   (name "wallarm-oob")
   (version "0.10.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.1
  (package
   (name "wallarm-oob")
   (version "0.10.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))

(define-public wallarm-oob-0.10.0
  (package
   (name "wallarm-oob")
   (version "0.10.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.wallarm.com/wallarm-oob/wallarm-oob-0.10.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Wallarm out of band for Kubernetes based on eBPF")
   (description "Wallarm out of band for Kubernetes based on eBPF")
   (license #f)))