
(define-module (helm webhookrelay-operator webhookrelay-operator)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public webhookrelay-operator-0.4.1
  (package
   (name "webhookrelay-operator")
   (version "0.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/webhookrelay/webhookrelay-operator")
   (synopsis "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (description "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (license #f)))

(define-public webhookrelay-operator-0.4.0
  (package
   (name "webhookrelay-operator")
   (version "0.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/webhookrelay/webhookrelay-operator")
   (synopsis "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (description "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (license #f)))

(define-public webhookrelay-operator-0.3.1
  (package
   (name "webhookrelay-operator")
   (version "0.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/webhookrelay/webhookrelay-operator")
   (synopsis "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (description "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (license #f)))

(define-public webhookrelay-operator-0.3.0
  (package
   (name "webhookrelay-operator")
   (version "0.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/webhookrelay/webhookrelay-operator")
   (synopsis "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (description "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (license #f)))

(define-public webhookrelay-operator-0.2.0
  (package
   (name "webhookrelay-operator")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/webhookrelay/webhookrelay-operator")
   (synopsis "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (description "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (license #f)))

(define-public webhookrelay-operator-0.1.6
  (package
   (name "webhookrelay-operator")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/webhookrelay/webhookrelay-operator")
   (synopsis "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (description "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (license #f)))

(define-public webhookrelay-operator-0.1.5
  (package
   (name "webhookrelay-operator")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/webhookrelay/webhookrelay-operator")
   (synopsis "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (description "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (license #f)))

(define-public webhookrelay-operator-0.1.4
  (package
   (name "webhookrelay-operator")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/webhookrelay/webhookrelay-operator")
   (synopsis "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (description "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (license #f)))

(define-public webhookrelay-operator-0.1.3
  (package
   (name "webhookrelay-operator")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/webhookrelay/webhookrelay-operator")
   (synopsis "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (description "Webhook Relay Operator provides an easy way to receive webhooks to an internal Kubernetes cluster without configuring public IP or load balancer")
   (license #f)))

(define-public webhookrelay-operator-0.1.1
  (package
   (name "webhookrelay-operator")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Webhook Relay Operator to deploy agents and manage routing configurations")
   (description "Webhook Relay Operator to deploy agents and manage routing configurations")
   (license #f)))

(define-public webhookrelay-operator-0.1.0
  (package
   (name "webhookrelay-operator")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.webhookrelay.com/webhookrelay-operator-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Webhook Relay Operator to deploy agents and manage routing configurations")
   (description "Webhook Relay Operator to deploy agents and manage routing configurations")
   (license #f)))