
(define-module (helm wiremind remoulade)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public remoulade-0.1.7
  (package
   (name "remoulade")
   (version "0.1.7")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/remoulade-0.1.7/remoulade-0.1.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/wiremind/wiremind-helm-charts/tree/main/charts/remoulade")
   (synopsis "A Helm chart to deploy Remoulade and its dependencies")
   (description "A Helm chart to deploy Remoulade and its dependencies")
   (license #f)))

(define-public remoulade-0.1.6
  (package
   (name "remoulade")
   (version "0.1.6")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/remoulade-0.1.6/remoulade-0.1.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/wiremind/wiremind-helm-charts/tree/main/charts/remoulade")
   (synopsis "A Helm chart to deploy Remoulade and its dependencies")
   (description "A Helm chart to deploy Remoulade and its dependencies")
   (license #f)))

(define-public remoulade-0.1.5
  (package
   (name "remoulade")
   (version "0.1.5")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/remoulade-0.1.5/remoulade-0.1.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/wiremind/wiremind-helm-charts/tree/main/charts/remoulade")
   (synopsis "A Helm chart to deploy Remoulade and its dependencies")
   (description "A Helm chart to deploy Remoulade and its dependencies")
   (license #f)))

(define-public remoulade-0.1.4
  (package
   (name "remoulade")
   (version "0.1.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/remoulade-0.1.4/remoulade-0.1.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/wiremind/wiremind-helm-charts/tree/main/charts/remoulade")
   (synopsis "A Helm chart to deploy Remoulade and its dependencies")
   (description "A Helm chart to deploy Remoulade and its dependencies")
   (license #f)))

(define-public remoulade-0.1.3
  (package
   (name "remoulade")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/remoulade-0.1.3/remoulade-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/wiremind/wiremind-helm-charts/tree/main/charts/remoulade")
   (synopsis "A Helm chart to deploy Remoulade and its dependencies")
   (description "A Helm chart to deploy Remoulade and its dependencies")
   (license #f)))

(define-public remoulade-0.1.2
  (package
   (name "remoulade")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/remoulade-0.1.2/remoulade-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/wiremind/wiremind-helm-charts/tree/main/charts/remoulade")
   (synopsis "A Helm chart to deploy Remoulade and its dependencies")
   (description "A Helm chart to deploy Remoulade and its dependencies")
   (license #f)))

(define-public remoulade-0.1.1
  (package
   (name "remoulade")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/remoulade-0.1.1/remoulade-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/wiremind/wiremind-helm-charts/tree/main/charts/remoulade")
   (synopsis "A Helm chart to deploy Remoulade and its dependencies")
   (description "A Helm chart to deploy Remoulade and its dependencies")
   (license #f)))

(define-public remoulade-0.1.0
  (package
   (name "remoulade")
   (version "0.1.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/remoulade-0.1.0/remoulade-0.1.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/wiremind/wiremind-helm-charts/tree/main/charts/remoulade")
   (synopsis "A Helm chart to deploy Remoulade and its dependencies")
   (description "A Helm chart to deploy Remoulade and its dependencies")
   (license #f)))