
(define-module (helm wiremind wiremind-baremetal)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public wiremind-baremetal-1.4.4
  (package
   (name "wiremind-baremetal")
   (version "1.4.4")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/wiremind-baremetal-1.4.4/wiremind-baremetal-1.4.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to use BareMetal Encrypted at Rest disks")
   (description "Helm chart to use BareMetal Encrypted at Rest disks")
   (license #f)))

(define-public wiremind-baremetal-1.4.3
  (package
   (name "wiremind-baremetal")
   (version "1.4.3")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/wiremind-baremetal-1.4.3/wiremind-baremetal-1.4.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to use BareMetal Encrypted at Rest disks")
   (description "Helm chart to use BareMetal Encrypted at Rest disks")
   (license #f)))

(define-public wiremind-baremetal-1.4.2
  (package
   (name "wiremind-baremetal")
   (version "1.4.2")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/wiremind-baremetal-1.4.2/wiremind-baremetal-1.4.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to use BareMetal Encrypted at Rest disks")
   (description "Helm chart to use BareMetal Encrypted at Rest disks")
   (license #f)))

(define-public wiremind-baremetal-1.4.1
  (package
   (name "wiremind-baremetal")
   (version "1.4.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/wiremind-baremetal-1.4.1/wiremind-baremetal-1.4.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to use BareMetal Encrypted at Rest disks")
   (description "Helm chart to use BareMetal Encrypted at Rest disks")
   (license #f)))

(define-public wiremind-baremetal-1.4.0
  (package
   (name "wiremind-baremetal")
   (version "1.4.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/wiremind-baremetal-1.4.0/wiremind-baremetal-1.4.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to use BareMetal Encrypted at Rest disks")
   (description "Helm chart to use BareMetal Encrypted at Rest disks")
   (license #f)))

(define-public wiremind-baremetal-1.3.1
  (package
   (name "wiremind-baremetal")
   (version "1.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/wiremind-baremetal-1.3.1/wiremind-baremetal-1.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to use BareMetal Encrypted at Rest disks")
   (description "Helm chart to use BareMetal Encrypted at Rest disks")
   (license #f)))

(define-public wiremind-baremetal-1.3.0
  (package
   (name "wiremind-baremetal")
   (version "1.3.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/wiremind-baremetal-1.3.0/wiremind-baremetal-1.3.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to use BareMetal Encrypted at Rest disks")
   (description "Helm chart to use BareMetal Encrypted at Rest disks")
   (license #f)))

(define-public wiremind-baremetal-1.2.0
  (package
   (name "wiremind-baremetal")
   (version "1.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://github.com/wiremind/wiremind-helm-charts/releases/download/wiremind-baremetal-1.2.0/wiremind-baremetal-1.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "Helm chart to use BareMetal Encrypted at Rest disks")
   (description "Helm chart to use BareMetal Encrypted at Rest disks")
   (license #f)))