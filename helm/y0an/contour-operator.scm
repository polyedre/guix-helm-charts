
(define-module (helm y0an contour-operator)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public contour-operator-0.2.7
  (package
   (name "contour-operator")
   (version "0.2.7")
   (source (origin
            (method url-fetch)
            (uri "https://charts.bitnami.com/bitnami/contour-operator-0.2.7.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/projectcontour/contour-operator")
   (synopsis "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (description "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (license #f)))

(define-public contour-operator-0.2.5
  (package
   (name "contour-operator")
   (version "0.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://charts.bitnami.com/bitnami/contour-operator-0.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/projectcontour/contour-operator")
   (synopsis "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (description "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (license #f)))

(define-public contour-operator-0.2.4
  (package
   (name "contour-operator")
   (version "0.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://charts.bitnami.com/bitnami/contour-operator-0.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/projectcontour/contour-operator")
   (synopsis "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (description "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (license #f)))

(define-public contour-operator-0.2.3
  (package
   (name "contour-operator")
   (version "0.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://charts.bitnami.com/bitnami/contour-operator-0.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/projectcontour/contour-operator")
   (synopsis "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (description "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (license #f)))

(define-public contour-operator-0.2.2
  (package
   (name "contour-operator")
   (version "0.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.bitnami.com/bitnami/contour-operator-0.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/projectcontour/contour-operator")
   (synopsis "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (description "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (license #f)))

(define-public contour-operator-0.2.1
  (package
   (name "contour-operator")
   (version "0.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.bitnami.com/bitnami/contour-operator-0.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/projectcontour/contour-operator")
   (synopsis "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (description "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (license #f)))

(define-public contour-operator-0.2.0
  (package
   (name "contour-operator")
   (version "0.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://charts.bitnami.com/bitnami/contour-operator-0.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/projectcontour/contour-operator")
   (synopsis "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (description "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (license #f)))

(define-public contour-operator-0.1.3
  (package
   (name "contour-operator")
   (version "0.1.3")
   (source (origin
            (method url-fetch)
            (uri "https://charts.bitnami.com/bitnami/contour-operator-0.1.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/projectcontour/contour-operator")
   (synopsis "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (description "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (license #f)))

(define-public contour-operator-0.1.2
  (package
   (name "contour-operator")
   (version "0.1.2")
   (source (origin
            (method url-fetch)
            (uri "https://charts.bitnami.com/bitnami/contour-operator-0.1.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/projectcontour/contour-operator")
   (synopsis "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (description "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (license #f)))

(define-public contour-operator-0.1.1
  (package
   (name "contour-operator")
   (version "0.1.1")
   (source (origin
            (method url-fetch)
            (uri "https://charts.bitnami.com/bitnami/contour-operator-0.1.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "https://github.com/projectcontour/contour-operator")
   (synopsis "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (description "The Contour Operator extends the Kubernetes API to create, configure and manage instances of Contour on behalf of users.")
   (license #f)))