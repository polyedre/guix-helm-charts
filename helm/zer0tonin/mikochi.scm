
(define-module (helm zer0tonin mikochi)
  #:use-module (guix utils)
  #:use-module (guix gexp)
  #:use-module (guix build utils)
  #:use-module (json)
  #:use-module (gnu packages base)
  #:use-module (guix packages)
  #:use-module (guix download)
  #:use-module (guix build-system copy))


(define-public mikochi-1.4.4
  (package
   (name "mikochi")
   (version "1.4.4")
   (source (origin
            (method url-fetch)
            (uri "https://zer0tonin.github.io/helm-charts//mikochi-1.4.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart for the Mikochi file manager")
   (description "A helm chart for the Mikochi file manager")
   (license #f)))

(define-public mikochi-1.4.3
  (package
   (name "mikochi")
   (version "1.4.3")
   (source (origin
            (method url-fetch)
            (uri "https://zer0tonin.github.io/helm-charts//mikochi-1.4.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart for the Mikochi file manager")
   (description "A helm chart for the Mikochi file manager")
   (license #f)))

(define-public mikochi-1.3.1
  (package
   (name "mikochi")
   (version "1.3.1")
   (source (origin
            (method url-fetch)
            (uri "https://zer0tonin.github.io/helm-charts//mikochi-1.3.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart for the Mikochi file manager")
   (description "A helm chart for the Mikochi file manager")
   (license #f)))

(define-public mikochi-1.2.6
  (package
   (name "mikochi")
   (version "1.2.6")
   (source (origin
            (method url-fetch)
            (uri "https://zer0tonin.github.io/helm-charts//mikochi-1.2.6.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart for the Mikochi file manager")
   (description "A helm chart for the Mikochi file manager")
   (license #f)))

(define-public mikochi-1.2.5
  (package
   (name "mikochi")
   (version "1.2.5")
   (source (origin
            (method url-fetch)
            (uri "https://zer0tonin.github.io/helm-charts//mikochi-1.2.5.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart for the Mikochi file manager")
   (description "A helm chart for the Mikochi file manager")
   (license #f)))

(define-public mikochi-1.2.4
  (package
   (name "mikochi")
   (version "1.2.4")
   (source (origin
            (method url-fetch)
            (uri "https://zer0tonin.github.io/helm-charts//mikochi-1.2.4.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart for the Mikochi file manager")
   (description "A helm chart for the Mikochi file manager")
   (license #f)))

(define-public mikochi-1.2.3
  (package
   (name "mikochi")
   (version "1.2.3")
   (source (origin
            (method url-fetch)
            (uri "https://zer0tonin.github.io/helm-charts//mikochi-1.2.3.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart for the Mikochi file manager")
   (description "A helm chart for the Mikochi file manager")
   (license #f)))

(define-public mikochi-1.2.2
  (package
   (name "mikochi")
   (version "1.2.2")
   (source (origin
            (method url-fetch)
            (uri "https://zer0tonin.github.io/helm-charts//mikochi-1.2.2.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart for the Mikochi file manager")
   (description "A helm chart for the Mikochi file manager")
   (license #f)))

(define-public mikochi-1.2.1
  (package
   (name "mikochi")
   (version "1.2.1")
   (source (origin
            (method url-fetch)
            (uri "https://zer0tonin.github.io/helm-charts//mikochi-1.2.1.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart for the Mikochi file manager")
   (description "A helm chart for the Mikochi file manager")
   (license #f)))

(define-public mikochi-1.2.0
  (package
   (name "mikochi")
   (version "1.2.0")
   (source (origin
            (method url-fetch)
            (uri "https://zer0tonin.github.io/helm-charts//mikochi-1.2.0.tgz")
            (sha256 #f)))
   (build-system copy-build-system)
   (home-page "")
   (synopsis "A helm chart for the Mikochi file manager")
   (description "A helm chart for the Mikochi file manager")
   (license #f)))